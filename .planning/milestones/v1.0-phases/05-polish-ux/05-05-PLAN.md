---
phase: 05-polish-ux
plan: 05
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
  - "05-03"
  - "05-04"
files_modified:
  - src/Editor/ExportSplitButton.tsx
autonomous: false
requirements:
  - POLH-01
  - POLH-02
  - POLH-03
  - POLH-04
  - POLH-05
  - POLH-06
  - POLH-07
  - POLH-08
  - POLH-09
  - POLH-10
  - POLH-11
  - POLH-12

must_haves:
  truths:
    - "All 12 POLH requirements verified in running application"
    - "Exported PNG matches editor preview for gap, radius, background, pan/zoom"
  artifacts: []
  key_links: []
---

<objective>
Wire export call to pass CanvasSettings and perform visual verification of all Phase 5 features.

Purpose: Final integration step: update the export call site to pass canvas settings from editorStore, then verify all 12 POLH requirements visually in the running application.

Output: Updated ExportSplitButton passing settings, full visual verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-polish-ux/05-CONTEXT.md
@.planning/phases/05-polish-ux/05-01-SUMMARY.md
@.planning/phases/05-polish-ux/05-02-SUMMARY.md
@.planning/phases/05-polish-ux/05-03-SUMMARY.md
@.planning/phases/05-polish-ux/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Export with Canvas Settings</name>
  <files>src/Editor/ExportSplitButton.tsx</files>
  <read_first>
    src/Editor/ExportSplitButton.tsx,
    src/lib/export.ts,
    src/store/editorStore.ts
  </read_first>
  <action>
    Update the export trigger in ExportSplitButton.tsx to pass CanvasSettings:

    1. Import `CanvasSettings` from `../lib/export`.
    2. Import `useEditorStore` (if not already imported).
    3. In the export handler (wherever `exportGrid()` is called), read canvas settings from editorStore:
    ```typescript
    const state = useEditorStore.getState();
    const settings: CanvasSettings = {
      gap: state.gap,
      borderRadius: state.borderRadius,
      borderColor: state.borderColor,
      backgroundMode: state.backgroundMode,
      backgroundColor: state.backgroundColor,
      backgroundGradientFrom: state.backgroundGradientFrom,
      backgroundGradientTo: state.backgroundGradientTo,
      backgroundGradientDir: state.backgroundGradientDir,
    };
    ```
    4. Pass `settings` as the last argument to `exportGrid(root, mediaRegistry, format, quality, onStage, settings)`.
    5. Ensure the export button has `data-testid="export-button"` attribute (needed by Ctrl+E keyboard shortcut from Plan 02).
  </action>
  <verify>
    <automated>npx vitest run src/test/phase04-02-task1.test.tsx src/test/canvas-export.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/Editor/ExportSplitButton.tsx imports `CanvasSettings` from export.ts
    - src/Editor/ExportSplitButton.tsx constructs a CanvasSettings object reading from `useEditorStore.getState()`
    - src/Editor/ExportSplitButton.tsx passes settings to `exportGrid` call
    - src/Editor/ExportSplitButton.tsx has `data-testid="export-button"` on the main export button
    - `npx vitest run` exits 0
  </acceptance_criteria>
  <done>
    Export call passes canvas settings. Exported PNG will reflect gap, border radius, border color, background, and pan/zoom.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual Verification of All Phase 5 Features</name>
  <files>n/a</files>
  <action>
    Run `npm run dev` to start the development server. Follow the verification steps below to test all 12 POLH requirements in the browser. Report any issues found.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>All 12 POLH requirements verified visually in the running application. User approved.</done>
  <what-built>
    All Phase 5 Polish and UX features:
    1. Template presets (6 layouts in toolbar popover)
    2. Canvas settings (gap, border radius, border color, background solid/gradient)
    3. Pan/zoom (double-click to enter, drag to pan, scroll to zoom, Escape to exit)
    4. Cell swap (drag handle on filled cells, drop on another cell)
    5. Dark theme (bg #0a0a0a, surfaces #1a1a1a-#222)
    6. Keyboard shortcuts (Ctrl+E, Delete, H, V, F, Escape)
    7. Onboarding overlay (3 steps on first visit)
    8. Responsive sidebar (collapses at narrow widths)
    9. Export integration (PNG reflects all canvas settings)
  </what-built>
  <how-to-verify>
    Start dev server: `npm run dev`

    **POLH-01 Templates:**
    1. Click Templates button in toolbar
    2. Verify 6 presets shown (2x1, 1x2, 2x2, 3-row, L-shape, Mosaic)
    3. Click "2x2 Quad" — canvas should show 4 equal cells
    4. Add an image to one cell, then click a different template — confirm dialog should appear

    **POLH-02/03/04/05 Canvas Settings:**
    5. In sidebar Canvas section, drag gap slider to ~10px — cells should have visible spacing
    6. Drag border radius slider to ~12px — cell corners should be rounded
    7. Change border color — borders should appear on cells
    8. Toggle to Gradient mode, pick two colors — canvas background shows gradient

    **POLH-06 Pan/Zoom:**
    9. Click a cell with an image (select it)
    10. Double-click the same cell — amber ring appears, other cells dim
    11. Drag inside the cell — image repositions
    12. Scroll inside the cell — image zooms in/out
    13. Press Escape — exits pan mode, back to blue selection ring

    **POLH-07 Cell Swap:**
    14. Add images to two different cells
    15. Hover a filled cell — drag handle (grip icon) appears in action bar
    16. Drag the handle and drop on the other filled cell — images swap

    **POLH-08 Dark Theme:**
    17. Verify editor background is very dark (#0a0a0a range)
    18. Toolbar and sidebar are surface dark (#1c1c1c range)
    19. Canvas area background is #0f0f0f

    **POLH-09 Keyboard Shortcuts:**
    20. Select a cell, press H — splits horizontal
    21. Select a cell, press V — splits vertical
    22. Select a cell, press F — toggles fit
    23. Select a cell, press Delete — removes cell
    24. Press Escape — deselects
    25. Press Ctrl+E — triggers export

    **POLH-10 Onboarding:**
    26. Open a new incognito window — 3-step overlay should appear
    27. Click Next through steps, then Done
    28. Refresh — overlay should NOT appear again
    29. Clear localStorage, refresh — overlay appears again; click Skip — dismissed permanently

    **POLH-11 Export Integration:**
    30. Set gap=10, borderRadius=12, gradient background
    31. Add an image, pan/zoom it
    32. Export as PNG, open the downloaded file
    33. Verify gap, rounded corners, gradient background, and image position match editor

    **POLH-12 Responsive:**
    34. Resize browser below 1024px — "StoryGrid works best on desktop" notice appears
    35. Resize back above 1024px — full sidebar returns
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run` — full test suite passes
- Visual verification of all 12 POLH requirements
</verification>

<success_criteria>
- All 12 POLH requirements working in browser
- Export PNG matches editor for all canvas settings
- No regressions from prior phases
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-ux/05-05-SUMMARY.md`
</output>

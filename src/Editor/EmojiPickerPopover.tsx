// Stub implementation — replaced in Task 2 with full lazy-loaded emoji picker.
// Created in Task 1 to satisfy TypeScript imports from AddOverlayMenu.tsx.

interface EmojiPickerPopoverProps {
  onClose: () => void;
}

export function EmojiPickerPopover(_props: EmojiPickerPopoverProps) {
  return null;
}

---
phase: 20-playback-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Editor/PlaybackTimeline.tsx
autonomous: true
requirements:
  - PLAY-01
  - PLAY-02
  - PLAY-03

must_haves:
  truths:
    - "PlaybackTimeline renders a semi-transparent dark background with backdrop blur"
    - "Scrubber track is 3px tall with white/20 background"
    - "Scrubber thumb is white and scales up 1.5x on active drag"
    - "Play/pause icon is white with ghost-circle hover"
    - "Time display is white/70 and legible on dark background"
    - "Zero TypeScript logic changes — only className strings modified"
  artifacts:
    - path: "src/Editor/PlaybackTimeline.tsx"
      provides: "Polished dark PlaybackTimeline component"
      contains: "bg-black/80"
  key_links:
    - from: "src/Editor/PlaybackTimeline.tsx"
      to: "src/Editor/CanvasArea.tsx"
      via: "Visual consistency — both use bg-black/* backdrop-blur-sm dark overlay pattern"
      pattern: "bg-black/80 backdrop-blur-sm"
---

<objective>
Restyle the PlaybackTimeline component with a dark, polished scrubber bar (IG/TikTok aesthetic) using Tailwind class changes only. Zero logic changes.

Purpose: The current PlaybackTimeline uses generic card/muted theme tokens that look out of place against the dark canvas area. This phase aligns it with the existing dark overlay aesthetic (see CanvasArea's drop pill: `bg-black/70 backdrop-blur-sm text-white`).

Output: Updated `src/Editor/PlaybackTimeline.tsx` with new Tailwind classes — same JSX structure, same logic, different visual treatment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-playback-ui-polish/20-CONTEXT.md
@src/Editor/PlaybackTimeline.tsx

<interfaces>
<!-- No new interfaces — this is a CSS-only change to an existing component -->
<!-- The JSX structure, props, hooks, and event handlers remain identical -->

Current className strings to replace (from PlaybackTimeline.tsx):

Container (line 77):
  FROM: "h-12 flex flex-row items-center w-full shrink-0 bg-card border-t border-border px-4 gap-3"
  TO:   "h-12 flex flex-row items-center w-full shrink-0 bg-black/80 backdrop-blur-sm px-4 gap-3"

Play/pause button (line 83):
  FROM: "w-11 h-11 rounded-full flex items-center justify-center hover:bg-muted text-foreground"
  TO:   "w-11 h-11 rounded-full flex items-center justify-center hover:bg-white/10 text-white"

Scrubber input className array (lines 96-115):
  FROM array items → TO array items (see task action for exact replacement)

Time display (line 119):
  FROM: "text-muted-foreground text-xs tabular-nums min-w-[5rem] text-right"
  TO:   "text-white/70 text-xs tabular-nums min-w-[5rem] text-right"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle PlaybackTimeline with dark polished classes</name>
  <files>src/Editor/PlaybackTimeline.tsx</files>
  <read_first>
    - src/Editor/PlaybackTimeline.tsx (current state — verify class strings match expectations before editing)
    - src/Editor/CanvasArea.tsx:100-108 (visual precedent: bg-black/70 backdrop-blur-sm pattern)
  </read_first>
  <action>
Modify ONLY the className strings in PlaybackTimeline.tsx. Do NOT change any TypeScript logic, imports, hooks, event handlers, or JSX structure. The changes are:

**1. Container div (line 77) — per D-01, D-02:**
Replace:
  `"h-12 flex flex-row items-center w-full shrink-0 bg-card border-t border-border px-4 gap-3"`
With:
  `"h-12 flex flex-row items-center w-full shrink-0 bg-black/80 backdrop-blur-sm px-4 gap-3"`

Rationale: Drop `bg-card border-t border-border`, add `bg-black/80 backdrop-blur-sm`. Layout classes (`h-12 flex flex-row items-center w-full shrink-0 px-4 gap-3`) stay identical per D-01.

**2. Play/pause button (line 83) — per D-03, D-06:**
Replace:
  `"w-11 h-11 rounded-full flex items-center justify-center hover:bg-muted text-foreground"`
With:
  `"w-11 h-11 rounded-full flex items-center justify-center hover:bg-white/10 text-white"`

**3. Scrubber input className array (lines 96-115) — per D-03, D-04, D-05:**
Replace the entire array with:
```
[
  'flex-1 appearance-none bg-transparent cursor-pointer',
  '[&::-webkit-slider-runnable-track]:h-[3px]',
  '[&::-webkit-slider-runnable-track]:rounded-full',
  '[&::-webkit-slider-runnable-track]:bg-white/20',
  '[&::-webkit-slider-thumb]:w-4',
  '[&::-webkit-slider-thumb]:h-4',
  '[&::-webkit-slider-thumb]:rounded-full',
  '[&::-webkit-slider-thumb]:bg-white',
  '[&::-webkit-slider-thumb]:appearance-none',
  '[&::-webkit-slider-thumb]:-mt-[6.5px]',
  '[&::-webkit-slider-thumb]:transition-transform',
  '[&::-webkit-slider-thumb]:duration-100',
  '[&::-webkit-slider-thumb]:active:scale-150',
  '[&::-webkit-slider-thumb]:cursor-grab',
  '[&::-webkit-slider-thumb]:active:cursor-grabbing',
  '[&::-moz-range-track]:h-[3px]',
  '[&::-moz-range-track]:rounded-full',
  '[&::-moz-range-track]:bg-white/20',
  '[&::-moz-range-thumb]:w-4',
  '[&::-moz-range-thumb]:h-4',
  '[&::-moz-range-thumb]:rounded-full',
  '[&::-moz-range-thumb]:bg-white',
  '[&::-moz-range-thumb]:border-none',
  '[&::-moz-range-thumb]:transition-transform',
  '[&::-moz-range-thumb]:duration-100',
  '[&::-moz-range-thumb]:active:scale-150',
].join(' ')
```

Changes from current:
- Track: `h-1` -> `h-[3px]`, `bg-muted` -> `bg-white/20` (both webkit and moz) — per D-04, D-03
- Thumb: `bg-[#3b82f6]` -> `bg-white` (both webkit and moz) — per D-03
- Thumb offset: `-mt-1.5` -> `-mt-[6.5px]` (webkit only; calculated: 16px thumb / 2 - 3px track / 2 = 6.5px) — per D-04
- NEW webkit thumb: `transition-transform`, `duration-100`, `active:scale-150`, `cursor-grab`, `active:cursor-grabbing` — per D-05, Claude's discretion
- NEW moz thumb: `transition-transform`, `duration-100`, `active:scale-150` — per D-05

**4. Time display span (line 119) — per D-03:**
Replace:
  `"text-muted-foreground text-xs tabular-nums min-w-[5rem] text-right"`
With:
  `"text-white/70 text-xs tabular-nums min-w-[5rem] text-right"`
  </action>
  <verify>
    <automated>cd /Users/radu/Developer/storygrid && npx tsc --noEmit 2>&1 | head -20 && grep -c 'bg-black/80' src/Editor/PlaybackTimeline.tsx && grep -c 'bg-white/20' src/Editor/PlaybackTimeline.tsx && grep -c 'active:scale-150' src/Editor/PlaybackTimeline.tsx && grep -c 'text-white/70' src/Editor/PlaybackTimeline.tsx</automated>
  </verify>
  <acceptance_criteria>
    - PlaybackTimeline.tsx contains `bg-black/80 backdrop-blur-sm` (container background)
    - PlaybackTimeline.tsx does NOT contain `bg-card` or `border-t border-border`
    - PlaybackTimeline.tsx contains `hover:bg-white/10 text-white` (play button)
    - PlaybackTimeline.tsx does NOT contain `hover:bg-muted` or `text-foreground`
    - PlaybackTimeline.tsx contains `bg-white/20` exactly 2 times (webkit track + moz track)
    - PlaybackTimeline.tsx contains `bg-white` (thumb, both browsers — at least 2 occurrences of `bg-white` total including bg-white/20)
    - PlaybackTimeline.tsx does NOT contain `bg-[#3b82f6]`
    - PlaybackTimeline.tsx contains `h-[3px]` exactly 2 times (webkit + moz track)
    - PlaybackTimeline.tsx does NOT contain `h-1` in slider track context (no `[&::-webkit-slider-runnable-track]:h-1`)
    - PlaybackTimeline.tsx contains `-mt-[6.5px]` (adjusted thumb offset)
    - PlaybackTimeline.tsx contains `active:scale-150` at least 2 times (webkit + moz)
    - PlaybackTimeline.tsx contains `transition-transform` at least 2 times
    - PlaybackTimeline.tsx contains `text-white/70` (time display)
    - PlaybackTimeline.tsx does NOT contain `text-muted-foreground`
    - `npx tsc --noEmit` exits 0 (no type errors — confirms no TS logic was broken)
    - The TypeScript logic (imports, hooks, useEffect, handlePlayPause, handleScrub, seekAll, formatTime) is byte-identical to before — only className strings changed
  </acceptance_criteria>
  <done>
    PlaybackTimeline renders with dark semi-transparent background, white-on-dark controls, 3px track, white thumb with scale-on-drag animation, and legible white/70 time display. All changes are Tailwind class string modifications only. TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected — this is a CSS-only visual change to an existing component with no user input handling, no data flow changes, and no new external dependencies.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | N/A | PlaybackTimeline.tsx | accept | Pure CSS class changes; no attack surface introduced or modified |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes — no TypeScript changes means no new type errors
2. Visual inspection: PlaybackTimeline has dark background, white controls, thin track, thumb scales on drag
3. `grep` confirms all old theme tokens removed (`bg-card`, `border-border`, `bg-muted`, `text-foreground`, `text-muted-foreground`, `bg-[#3b82f6]`) and new dark tokens present
4. Diff shows only className string changes — no logic lines modified
</verification>

<success_criteria>
- PlaybackTimeline visually matches the dark, polished IG/TikTok aesthetic described in CONTEXT.md
- All 6 decisions (D-01 through D-06) are implemented exactly as specified
- Zero TypeScript logic changes — git diff shows only className string modifications
- TypeScript compilation passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/20-playback-ui-polish/20-01-SUMMARY.md`
</output>

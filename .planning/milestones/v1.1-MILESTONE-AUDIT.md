---
milestone: v1.1
audited: 2026-04-08
resolved: 2026-04-08
status: resolved
resolution_phase: "10-restore-cell-controls-sizing-stacking"
resolution_note: "All three gaps (CELL-02, CELL-01, Flow E) closed in Phase 10. Note: during execution, CELL-02 was reframed — the audit assumed 1967219's clamp() revert was a regression, but it was a deliberate architectural pivot to portal-based ActionBar rendering (viewport space, no canvas scale). Phase 10 settled on fixed w-16 h-16 (64px) portal-aware sizing rather than restoring clamp(). See phase 10-01 SUMMARY.md for course-correction rationale and lessons learned about diff-only audits."
scores:
  requirements: 6/8
  phases: 3/3
  integration: 5/8
  flows: 7/8
gaps:
  requirements:
    - id: "CELL-02"
      status: "unsatisfied"
      phase: "07-cell-controls-display-polish"
      claimed_by_plans: ["07-01-PLAN.md"]
      completed_by_plans: ["07-01-SUMMARY.md"]
      verification_status: "passed (stale)"
      evidence: "ActionBar.tsx:50-51 uses ICON_SIZE=32 and btnClass='w-16 h-16'. The clamp(28px,2.2vw,36px) sizing introduced in f7357a4 was reverted in 1967219 for a portal experiment, then the portal was abandoned in 1476df2 leaving the fixed-size revert in place. Phase 7 VERIFICATION.md was written before the revert and is now stale. Buttons are fixed at 64px regardless of viewport — fails REQ that controls be sized via vw/vh."
    - id: "CELL-01"
      status: "partial"
      phase: "07-cell-controls-display-polish"
      claimed_by_plans: ["07-01-PLAN.md"]
      completed_by_plans: ["07-01-SUMMARY.md"]
      verification_status: "passed (stacking-context risk)"
      evidence: "LeafNode.tsx:562 root has 'isolate' which creates a stacking context, so the ActionBar wrapper z-50 (line 683) cannot escape the per-cell stacking context to overflow above sibling LeafNodes. The code comment at line 678 says 'no isolate' — factually incorrect. Phase 7 verification ran in jsdom which does not compute stacking contexts; CELL-01 may visually fail in real browser at very small cell sizes near sibling boundaries."
  integration: []
  flows:
    - flow: "E (resize laptop→4K, ActionBar stays 28-36px)"
      breaks_at: "ActionBar buttons fixed at w-16 h-16 (64px) — never scale with viewport"
      affected_reqs: ["CELL-02"]
tech_debt:
  - phase: 07-cell-controls-display-polish
    items:
      - "Sidebar.tsx:400 file input accept='image/*' blocks video upload via the sidebar Replace button — videos must be uploaded via cell ActionBar or drag/drop. Inconsistent with MEDIA-01 spirit."
      - "LeafNode.tsx:678 comment claims 'no isolate' but root className includes 'isolate' — stale comment to fix alongside the stacking-context decision."
  - phase: 09-improve-cell-movement-and-swapping
    items:
      - "STORE-06 redo round-trip weakened: pre-existing pushSnapshot model stores pre-mutation state, so strict redo round-trip impossible without store-wide rewrite. Documented in 09-02-SUMMARY.md, not a Phase 9 regression."
      - "moveCell does not restore selectedNodeId on Ctrl+Z (D-06 documented behavior); acceptable but worth flagging."
nyquist:
  compliant_phases: ["09-improve-cell-movement-and-swapping"]
  partial_phases: []
  missing_phases: ["07-cell-controls-display-polish", "08-canvas-workspace-ux"]
  overall: "partial"
---

# Milestone v1.1 — UI Polish & Bug Fixes — Audit Report

**Audited:** 2026-04-08
**Status:** ⚠ gaps_found
**Scope:** Phases 7, 8, 9 (3 phases, 9 plans, 8 milestone REQ-IDs)

## Requirements Coverage (3-source cross-reference)

| REQ-ID | Phase | VERIFICATION.md | SUMMARY frontmatter | REQUIREMENTS.md | Final Status |
|--------|-------|-----------------|---------------------|-----------------|--------------|
| CELL-01 | 7 | passed | (no `requirements_completed` field) | `[ ]` | **partial** — `isolate` stacking context risk; jsdom-only verification |
| CELL-02 | 7 | passed (stale) | (no `requirements_completed` field) | `[ ]` | **unsatisfied** — clamp() sizing reverted in 1476df2 |
| CELL-03 | 7 | passed | (no `requirements_completed` field) | `[ ]` | satisfied — `clamp(20px,1.6vw,32px)` confirmed in code |
| MEDIA-01 | 7 | passed | (no `requirements_completed` field) | `[ ]` | satisfied (display path); sidebar upload-input gap noted as tech debt |
| CANVAS-01 | 8 | passed | listed inline | `[ ]` | satisfied |
| TPL-01 | 8 | passed | listed inline | `[ ]` | satisfied |
| DROP-01 | 8 | passed | listed inline | `[ ]` | satisfied |
| DROP-02 | 8 | passed | listed inline | `[ ]` | satisfied |

**Score: 6/8 satisfied, 1 partial, 1 unsatisfied**

> SUMMARY frontmatter convention note: phase summaries use ad-hoc dependency-graph fields rather than the `requirements_completed:` array the matrix expects. This is a process/format gap; satisfaction was determined from VERIFICATION.md tables and direct code inspection.

## Phase Verifications

| Phase | VERIFICATION | Status | Score |
|-------|--------------|--------|-------|
| 07 — Cell Controls & Display Polish | ✓ | passed (now stale on CELL-02 due to post-verification revert) | 9/9 |
| 08 — Canvas & Workspace UX | ✓ | passed | 4/4 |
| 09 — Improve cell movement & swapping | ✓ | passed | 10/10 |

## Cross-Phase Integration

The integration check found that Phase 7/8/9 interactions are mostly clean:

- ✓ Phase 9 cell-id drags vs Phase 8 workspace file-drop ring — `isFileDrag()` Files-type guard prevents collision
- ✓ Phase 7 LeafNode `handleDrop` `stopPropagation` preserved by Phase 9 — workspace handler no longer double-fires
- ✓ Phase 7 thumbnailMap async write does not interact with Phase 9 `moveCell` undo (thumbnailMap excluded from snapshots)
- ✓ Phase 9 `moveCell` clears selection only on structural moves (D-06)
- ⚠ Phase 7 `isolate` on LeafNode root contradicts the documented "no isolate" comment and creates a stacking-context risk for the z-50 ActionBar overflow on sibling boundaries

## E2E Flows

| Flow | Description | Status |
|------|-------------|--------|
| A | Toggle Show Safe Zone → dimmed/striped overlay | ✓ |
| B | Click template → silent apply | ✓ |
| C | Drop file anywhere in workspace → import | ✓ |
| D | Drop video on cell → sidebar thumbnail | ✓ (partial: sidebar upload button can't pick videos) |
| E | Resize laptop→4K → ActionBar 28-36px stable | ✗ buttons fixed at 64px |
| F | Shrink cell → ActionBar visible above boundary | ⚠ jsdom-verified only; real-browser stacking risk |
| G | Drag cell to edge → split-insert + atomic Ctrl+Z | ✓ |
| H | Drag empty cell by handle (EC-06) | ✓ |

**7/8 flows complete; flow E broken; flows D and F have qualifications.**

## Nyquist Coverage

| Phase | VALIDATION.md | Compliant | Action |
|-------|---------------|-----------|--------|
| 07 | missing | n/a | `/gsd:validate-phase 7` (optional) |
| 08 | missing | n/a | `/gsd:validate-phase 8` (optional) |
| 09 | exists | true | — |

Phases 7 and 8 do not have VALIDATION.md files. Workflow has `nyquist_validation: true` so this is a discovery gap, not a hard blocker — Nyquist validation is optional retroactive coverage.

## Tech Debt Summary

**Phase 7:**
- Sidebar `accept="image/*"` blocks sidebar-button video uploads (MEDIA-01 spirit gap)
- Stale comment in LeafNode.tsx:678 ("no isolate") contradicts actual className

**Phase 9:**
- STORE-06 redo round-trip documented limitation (pre-existing, not regression)
- moveCell does not restore selectedNodeId on Ctrl+Z (D-06, intentional)

## Conclusion

The milestone goal — "Fix visual bugs and polish the editing experience" — is **mostly achieved**, but a regression introduced post-verification reverted the centerpiece CELL-02 fix. CELL-01 has a real-browser visual risk from `isolate` that jsdom did not catch.

Recommend a small Phase 9.1 (or quick) to:
1. Restore `BTN_SIZE = clamp(28px, 2.2vw, 36px)` in ActionBar.tsx and remove the fixed `w-16 h-16` / `ICON_SIZE = 32` (CELL-02)
2. Remove `isolate` from LeafNode.tsx:562 root className OR re-verify CELL-01 in a real browser; fix the stale comment at line 678 (CELL-01)
3. Optionally extend Sidebar.tsx:400 `accept` to include `video/*` (MEDIA-01 polish)

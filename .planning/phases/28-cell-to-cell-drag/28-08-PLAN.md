---
phase: 28-cell-to-cell-drag
plan: 08
type: execute
wave: 4
depends_on:
  - 28-01
  - 28-02
  - 28-03
  - 28-04
  - 28-06
files_modified:
  - src/Grid/LeafNode.tsx
autonomous: true
requirements:
  - DRAG-01
  - DRAG-07
  - GHOST-07
  - DROP-01
  - DROP-04
  - DROP-05
must_haves:
  truths:
    - "LeafNode no longer imports useDraggable, useDroppable, or useDndMonitor from '@dnd-kit/core'"
    - "LeafNode no longer imports DragZoneRefContext or useContext (for DnD purposes)"
    - "LeafNode calls useCellDraggable(id) and useCellDropTarget(id) from '../dnd'"
    - "LeafNode renders <DropZoneIndicators zone={activeZone} /> when dragStore.overId === id && status === 'dragging' (D-12)"
    - "Source cell opacity dims to 0.4 when dragStore.sourceId === id && status === 'dragging' (GHOST-07)"
    - "Hovered target cell gains a 2px accent-color outline when dragStore.overId === id (D-17 / DROP-04)"
    - "cursor: grab class applied to LeafNode root unconditionally when not in pan mode (DRAG-01)"
    - "All 5 inline activeZone === 'top'/'bottom'/'left'/'right'/'center' JSX blocks removed"
    - "dragListeners spread LAST on root JSX element (Pitfall 1)"
    - "File-drop handlers (handleFileDragOver / handleFileDragLeave / handleFileDrop) preserved unchanged (D-28)"
    - "Optional: video rAF draw loop pauses when dragStore.sourceId === id (Pitfall 9 belt-and-braces)"
  artifacts:
    - path: "src/Grid/LeafNode.tsx"
      provides: "LeafNode consuming new DnD hooks + rendering DropZoneIndicators conditionally"
      contains: "useCellDraggable"
      contains_also: "useCellDropTarget"
      contains_also: "DropZoneIndicators"
  key_links:
    - from: "src/Grid/LeafNode.tsx"
      to: "src/dnd"
      via: "useCellDraggable + useCellDropTarget + DropZoneIndicators + useDragStore imports"
      pattern: "from '../dnd'"
    - from: "src/Grid/LeafNode.tsx root div"
      to: "dragListeners"
      via: "spread LAST — after all explicit pointer handlers"
      pattern: "\\{\\.\\.\\.(!isPanMode \\? dragListeners : \\{\\})\\}"
---

<objective>
Perform the 15-point surgical edit of `src/Grid/LeafNode.tsx` per D-20: remove all Phase 25 DnD wiring (useDraggable/useDroppable/useDndMonitor, DragZoneRefContext, pointerPosRef, isPendingDrag, 5 inline zone JSX blocks) and replace with calls to the new `useCellDraggable` + `useCellDropTarget` hooks plus a single conditional `<DropZoneIndicators />` render. Add GHOST-07 source dim, D-17 target outline, DRAG-01 cursor:grab. PRESERVE file-drop handlers (D-28) and the Pitfall 1 spread-listeners-last contract.

Purpose: LeafNode becomes a consumer of the Wave 2/3 artifacts — completes the end-to-end wiring for SC-1, SC-2, SC-5.
Output: A slimmer, clearer LeafNode.tsx with all DnD routing delegated to `src/dnd/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/28-cell-to-cell-drag/28-CONTEXT.md
@.planning/phases/28-cell-to-cell-drag/28-PATTERNS.md

@src/Grid/LeafNode.tsx
@src/dnd/index.ts
@src/dnd/useCellDraggable.ts
@src/dnd/useCellDropTarget.ts
@src/dnd/DropZoneIndicators.tsx
@src/dnd/dragStore.ts

<interfaces>
After Plans 01-06 applied:

```typescript
// From '../dnd':
export function useCellDraggable(leafId: string): {
  attributes: Record<string, unknown>;
  listeners: Record<string, unknown>;
  isDragging: boolean;
  setNodeRef: (node: HTMLElement | null) => void;
};
export function useCellDropTarget(leafId: string): {
  isOver: boolean;
  setNodeRef: (node: HTMLElement | null) => void;
};
export function DropZoneIndicators(props: { zone: DropZone | null }): JSX.Element;
export const useDragStore: UseBoundStore<StoreApi<DragState>>;
export type DropZone = 'center' | 'top' | 'bottom' | 'left' | 'right';

// dragStore selectors used by LeafNode (D-12):
const isOverThisCell = useDragStore((s) => s.overId === id && s.status === 'dragging');
const isSourceOfDrag = useDragStore((s) => s.sourceId === id && s.status === 'dragging');
const activeZone = useDragStore((s) => (s.overId === id ? s.activeZone : null));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surgical 15-point edit of LeafNode.tsx per D-20</name>
  <files>src/Grid/LeafNode.tsx</files>

  <read_first>
    - `src/Grid/LeafNode.tsx` (FULL FILE — all lines; surgical edits reference exact line ranges)
    - `.planning/phases/28-cell-to-cell-drag/28-CONTEXT.md` §A4 D-20 (15-point edit map — authoritative)
    - `.planning/phases/28-cell-to-cell-drag/28-CONTEXT.md` §A3 D-12, D-13, D-17 (outline); §A2 D-06, D-11, GHOST-07 source dim 0.4; DRAG-01 cursor:grab
    - `.planning/phases/28-cell-to-cell-drag/28-CONTEXT.md` §A6 D-28 (file-drop handlers preserved)
    - `.planning/phases/28-cell-to-cell-drag/28-CONTEXT.md` Claude's Discretion bullet on video rAF pause (Pitfall 9 belt-and-braces)
    - `.planning/phases/28-cell-to-cell-drag/28-PATTERNS.md` §`src/Grid/LeafNode.tsx (surgical edits)` (Surgical Edit Map table at lines ~502-534 — 15 rows)
    - `.planning/research/PITFALLS.md` Pitfalls 1 (spread LAST), 9 (toDataURL race on videos), 11 (no re-render storm — scoped selectors)
    - `src/dnd/index.ts` (confirm barrel exports all 4 consumed names)
    - `src/dnd/DropZoneIndicators.tsx` (after Plan 06 — confirm prop shape)
  </read_first>

  <action>
    Work through the 15 surgical edits from CONTEXT.md §A4 D-20 + PATTERNS §LeafNode table IN ORDER. Each edit references concrete line numbers from the current LeafNode.tsx — use Read tool to re-check before editing to catch drift.

    **EDIT 1 — Line 1 (React imports):**
    Remove `useContext` (used only for DragZoneRefContext):
    ```typescript
    import React, { useState, useCallback, useRef, useEffect, useLayoutEffect } from 'react';
    ```

    **EDIT 2 — Line 9 (lucide import):**
    Remove `ArrowLeftRight` (moved to DropZoneIndicators — Plan 06):
    ```typescript
    import { ImageIcon } from 'lucide-react';
    ```

    **EDIT 3 — Line 11 (@dnd-kit import) — DELETE ENTIRELY:**
    ```typescript
    import { useDraggable, useDroppable, useDndMonitor } from '@dnd-kit/core';
    ```

    **EDIT 4 — Line 12 (DragZoneRefContext import) — DELETE ENTIRELY:**
    ```typescript
    import { DragZoneRefContext } from './CanvasWrapper';
    ```

    **EDIT 5 — Add new imports (after the remaining imports):**
    ```typescript
    import { useCellDraggable, useCellDropTarget, DropZoneIndicators, useDragStore } from '../dnd';
    ```

    **EDIT 6 — Line 18 (ActiveZone type alias) — DELETE:**
    ```typescript
    type ActiveZone = 'top' | 'bottom' | 'left' | 'right' | 'center' | null;
    ```
    (Inline use is replaced by `DropZone | null` from `../dnd`. We do NOT need to import `DropZone` since we only pass it through to DropZoneIndicators.)

    **EDIT 7 — Lines 58-60 (state declarations) — DELETE:**
    ```typescript
    const [activeZone, setActiveZone] = useState<ActiveZone>(null);   // DELETE
    const [isPendingDrag, setIsPendingDrag] = useState(false);        // DELETE
    const dragZoneRef = useContext(DragZoneRefContext);               // DELETE
    ```
    Keep all OTHER useState/useRef declarations.

    **EDIT 8 — Lines 299-310 (pointerPosRef + document pointermove listener) — DELETE ENTIRELY:**
    ```typescript
    const pointerPosRef = useRef<{ x: number; y: number }>({ x: 0, y: 0 });
    // ...
    useEffect(() => {
      const handler = (e: PointerEvent) => {
        pointerPosRef.current = { x: e.clientX, y: e.clientY };
      };
      document.addEventListener('pointermove', handler);
      return () => document.removeEventListener('pointermove', handler);
    }, []);
    ```
    This is the Pitfall 2 root cause. DELETE.

    **EDIT 9 — Lines 312-318 (useDraggable call) — REPLACE:**
    ```typescript
    // BEFORE
    const {
      setNodeRef: setDragNodeRef,
      listeners: dragListeners,
      isDragging,
      attributes: dragAttributes,
    } = useDraggable({ id, data: { nodeId: id } });
    ```
    ```typescript
    // AFTER — Plan 03's hook
    const {
      setNodeRef: setDragNodeRef,
      listeners: dragListeners,
      isDragging,
      attributes: dragAttributes,
    } = useCellDraggable(id);
    ```

    **EDIT 10 — Line 321 (useDroppable call) — REPLACE:**
    ```typescript
    // BEFORE
    const { setNodeRef: setDropNodeRef } = useDroppable({ id, data: { nodeId: id } });
    ```
    ```typescript
    // AFTER — Plan 04's hook
    const { setNodeRef: setDropNodeRef } = useCellDropTarget(id);
    ```

    **EDIT 11 — Lines 323-328 (setRefs callback) — KEEP STRUCTURE (still merging 3 refs):**
    ```typescript
    const setRefs = useCallback((el: HTMLDivElement | null) => {
      divRef.current = el;
      setDragNodeRef(el);
      setDropNodeRef(el);
    }, [setDragNodeRef, setDropNodeRef]);
    ```
    No change needed — still correct after the hook swap.

    **EDIT 12 — Lines 331-357 (useDndMonitor block) — DELETE ENTIRELY:**
    ```typescript
    useDndMonitor({
      onDragStart(...) { ... },
      onDragOver(...) { setActiveZone ... dragZoneRef.current = zone ... },
      onDragEnd(...) { ... },
      onDragCancel(...) { ... },
    });
    ```
    Zone computation is now handled by CanvasWrapper's `onDragOver` (Plan 07), writing `activeZone` into dragStore. LeafNode reads it via a scoped selector (EDIT 14).

    **EDIT 13 — Lines 361-374 (isPendingDrag native pointer listeners) — DELETE ENTIRELY:**
    ```typescript
    useEffect(() => {
      const el = divRef.current;
      const onDown = () => { if (!isPanModeRef.current) setIsPendingDrag(true); };
      const onUp = () => setIsPendingDrag(false);
      el.addEventListener('pointerdown', onDown, ...);
      // ...
    }, []);
    ```
    The 500ms hold-pulse animation was a Phase 25 visual affordance for the 500ms activation threshold. Phase 28 uses 250ms touch / 8px mouse — no pulse needed. DELETE.

    **EDIT 14 — Add scoped dragStore selectors (place near the other store selectors at the top of the component):**
    ```typescript
    // D-12 scoped primitive selectors — only re-render this leaf when its own drag state changes
    const isOverThisCell = useDragStore((s) => s.overId === id && s.status === 'dragging');
    const isSourceOfDrag = useDragStore((s) => s.sourceId === id && s.status === 'dragging');
    const activeZone = useDragStore((s) => (s.overId === id ? s.activeZone : null));
    ```

    **EDIT 15 — Lines 626-650 (ringClass + root div className/style) — MERGE:**

    Current ringClass logic (line 626-630):
    ```typescript
    const ringClass = isPanMode
      ? 'ring-2 ring-[#f59e0b] ring-inset'
      : isSelected
        ? 'ring-2 ring-[#3b82f6] ring-inset'
        : !mediaUrl ? 'border border-dashed border-[#333333]' : '';
    ```
    CHANGE to include D-17 2px accent outline when hovered as a drop target during drag (takes precedence over not-selected base; below isPanMode/isSelected):
    ```typescript
    const ringClass = isPanMode
      ? 'ring-2 ring-[#f59e0b] ring-inset'
      : isSelected
        ? 'ring-2 ring-[#3b82f6] ring-inset'
        : isOverThisCell
          ? 'ring-2 ring-[#3b82f6] ring-inset'       // D-17 / DROP-04: 2px accent on hovered drop target
          : !mediaUrl ? 'border border-dashed border-[#333333]' : '';
    ```
    (Use the same `#3b82f6` accent token already present in `isSelected` — matches the Tailwind accent token pattern in ActionBar. Claude's Discretion per CONTEXT.md.)

    Current root div style (lines 643-650) has:
    ```typescript
    style={{
      backfaceVisibility: 'hidden',
      touchAction: 'none',
      transition: 'opacity 150ms ease-out, box-shadow 150ms ease-out',
      cursor: isPanMode ? undefined : (isDragging || isPendingDrag ? 'grabbing' : 'grab'),
      ...(isDragging ? { opacity: 0.6, boxShadow: 'inset 0 0 0 3px rgba(255,255,255,0.6)' } : {}),
      ...(isPendingDrag && !isDragging ? { animation: 'drag-hold-pulse 500ms ease-in-out forwards' } : {}),
    }}
    data-hold-pending={isPendingDrag && !isDragging ? 'true' : undefined}
    ```

    CHANGE to:
    ```typescript
    style={{
      backfaceVisibility: 'hidden',
      touchAction: 'none',                       // CROSS-02 is Phase 30; this is pre-existing — keep
      transition: 'opacity 150ms ease-out, box-shadow 150ms ease-out',
      cursor: isPanMode ? undefined : (isDragging ? 'grabbing' : 'grab'),   // DRAG-01: always grab when not in pan mode; DRAG-02 body cursor handled in CanvasWrapper
      ...(isSourceOfDrag ? { opacity: 0.4 } : {}),                           // GHOST-07: 40% source dim
    }}
    ```
    Remove `data-hold-pending` attribute entirely (isPendingDrag is deleted).
    Remove the `isDragging` boxShadow override — GHOST-07 source dim (opacity 0.4) is the new visual; Phase 29 will polish further. Keep the opacity rule only.

    Note: `isDragging` is still a useful local boolean (from `useCellDraggable`'s return), but for source-opacity prefer `isSourceOfDrag` from dragStore — it is the authoritative predicate. `isDragging` is true specifically for THIS cell per dnd-kit's `useDraggable` hook, so the two should align. Using `isSourceOfDrag` keeps symmetry with `isOverThisCell` (both from dragStore).

    **EDIT 16 — Line 662 (listeners spread — Pitfall 1 invariant):**
    The current JSX has:
    ```jsx
    onPointerDown={handlePointerDown}
    onPointerMove={handlePointerMove}
    onPointerUp={handlePointerUp}
    {...(!isPanMode ? dragListeners : {})}
    ```
    KEEP AS-IS — listeners are ALREADY spread LAST (Pitfall 1 correctly honored). Do not reorder.

    **EDIT 17 — Lines 720-756 (5 inline activeZone === 'top'/'bottom'/... JSX blocks) — DELETE ALL, REPLACE WITH:**
    ```jsx
    {isOverThisCell && <DropZoneIndicators zone={activeZone} />}
    ```
    Place this where the 5 blocks currently sit (between the dim/file-drop overlays and the ActionBar). Single line replaces ~37 lines.

    **EDIT 18 — File-drop handlers (PRESERVED per D-28):**
    The following MUST remain unchanged:
    - Lines ~506-543 `handleFileDrop` useCallback
    - Lines ~545-552 `handleFileDragOver` with `e.dataTransfer.types.includes('Files')` guard
    - Lines ~554-556 `handleFileDragLeave`
    - Lines 656-658 JSX `onDragOver={handleFileDragOver} onDragLeave={handleFileDragLeave} onDrop={handleFileDrop}` on the root div

    **EDIT 19 — (Optional, recommended) Video rAF pause during drag (Pitfall 9 belt-and-braces):**
    Around the `rafIdRef`/`drawRef` rAF draw loop setup (roughly lines 220-280 area — use Read tool to find the `requestAnimationFrame` call that drives video frame drawing for this cell), wrap the loop's recurrence check so it short-circuits when `dragStore.sourceId === id`:
    ```typescript
    // Pitfall 9: pause video rAF draw loop when this cell is the drag source.
    // Prevents toDataURL() in CanvasWrapper.onDragStart from racing a mid-frame draw.
    if (useDragStore.getState().sourceId === id && useDragStore.getState().status === 'dragging') {
      // Skip this frame; reschedule.
      rafIdRef.current = requestAnimationFrame(loop);
      return;
    }
    ```
    Place the guard at the top of the per-frame loop function. This is AUTHOR'S DISCRETION per CONTEXT.md — if the existing video draw loop architecture makes this awkward, skip it. Note in the SUMMARY that SC-5 ghost test on video cells may need manual UAT only.

    **EDIT 20 — Test hygiene:**
    This plan does NOT touch any test files. Test rewrites live in Plan 10. Confirm `npm run test -- --run` after this plan still shows the same 9 pre-existing failures from deferred-items.md (they will be fixed in Plan 10). If ADDITIONAL failures appear that are not in deferred-items.md, this is a new regression introduced by this plan — fix before declaring done.

    **Final sanity scan:**
    - No `useDraggable` / `useDroppable` / `useDndMonitor` calls anywhere in LeafNode.
    - No `DragZoneRefContext` references.
    - No `pointerPosRef`, `isPendingDrag`, `activeZone` local state.
    - No inline `edge-line-{top,bottom,left,right}-` or `swap-overlay-` JSX.
    - `<DropZoneIndicators zone={activeZone} />` rendered conditionally exactly once.
    - File-drop triad intact.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
    Also run these grep checks (all must succeed):
    - `! grep -qE '\\buseDraggable\\b|\\buseDroppable\\b|\\buseDndMonitor\\b' src/Grid/LeafNode.tsx`
    - `! grep -q 'DragZoneRefContext' src/Grid/LeafNode.tsx`
    - `! grep -q 'pointerPosRef' src/Grid/LeafNode.tsx`
    - `! grep -q 'isPendingDrag' src/Grid/LeafNode.tsx`
    - `! grep -q "type ActiveZone" src/Grid/LeafNode.tsx`
    - `! grep -qE 'edge-line-(top|bottom|left|right)' src/Grid/LeafNode.tsx`
    - `! grep -q 'swap-overlay-' src/Grid/LeafNode.tsx`
    - `! grep -q 'data-hold-pending' src/Grid/LeafNode.tsx`
    - `grep -q 'useCellDraggable(id)' src/Grid/LeafNode.tsx`
    - `grep -q 'useCellDropTarget(id)' src/Grid/LeafNode.tsx`
    - `grep -q '<DropZoneIndicators zone={activeZone}' src/Grid/LeafNode.tsx`
    - `grep -q 'isOverThisCell' src/Grid/LeafNode.tsx`
    - `grep -q 'isSourceOfDrag' src/Grid/LeafNode.tsx`
    - `grep -q 'opacity: 0.4' src/Grid/LeafNode.tsx` (GHOST-07)
    - `grep -q "cursor: isPanMode ? undefined : (isDragging ? 'grabbing' : 'grab')" src/Grid/LeafNode.tsx` (DRAG-01)
    - `grep -q 'handleFileDrop' src/Grid/LeafNode.tsx` (D-28 preserved)
    - `grep -q 'handleFileDragOver' src/Grid/LeafNode.tsx` (D-28 preserved)
    - `grep -q "dataTransfer.types.includes('Files')" src/Grid/LeafNode.tsx` (D-28 preserved)
    - `grep -q '\\{\\.\\.\\.(!isPanMode ? dragListeners : \\{\\})\\}' src/Grid/LeafNode.tsx` (Pitfall 1: listeners spread LAST — check sequence via Read)
    - `npm run test -- --run` — same 9 pre-existing failures, NO new failures
  </verify>

  <acceptance_criteria>
    - Zero occurrences of `useDraggable`, `useDroppable`, `useDndMonitor`, `DragZoneRefContext`, `pointerPosRef`, `isPendingDrag`, `ActiveZone` in LeafNode.tsx
    - `useCellDraggable(id)` called exactly once
    - `useCellDropTarget(id)` called exactly once
    - `<DropZoneIndicators zone={activeZone} />` rendered exactly once, conditionally on `isOverThisCell`
    - 3 scoped dragStore selectors present: `isOverThisCell`, `isSourceOfDrag`, `activeZone`
    - `opacity: 0.4` applied when `isSourceOfDrag` (GHOST-07)
    - `cursor: 'grab'` / `'grabbing'` applied via inline style (DRAG-01) — no pan-mode exclusion for the grab cursor rule in non-pan mode
    - D-17 ring class addition when `isOverThisCell && !isSelected && !isPanMode` (DROP-04)
    - File-drop triad preserved (handleFileDragOver, handleFileDragLeave, handleFileDrop)
    - `npx tsc --noEmit` exits 0
    - `npm run test -- --run` — no NEW regressions (9 pre-existing failures carried into Plan 10)
  </acceptance_criteria>

  <done>LeafNode.tsx cleanly delegates all DnD to src/dnd/ hooks; renders DropZoneIndicators conditionally; source dims to 40%; drop target gains 2px accent outline; cursor:grab applied; file-drop preserved; no Phase 25 residue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dragStore selectors → LeafNode render | Subscribe-based; Zustand handles equality; no attacker-controlled path. |
| File-drop `dataTransfer.types` check | Browser-validated type array; cannot be forged at this layer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-21 | T | Reintroduce spread-first listeners (`{...listeners} onPointerDown={...}`) — breaks Pitfall 1 | mitigate | Acceptance criterion greps for exact `{...(!isPanMode ? dragListeners : {})}` ordering placed AFTER `onPointerUp={handlePointerUp}`. Phase 27 header in `useCellDraggable.ts` documents the rule; code reviewer must scan JSX prop order. |
| T-28-22 | D | Scoped selector returning object reference triggers rerenders every tick | mitigate | All 3 selectors return primitives (`boolean`, `string|null`) — Object.is equality prevents unnecessary re-renders. ARCHITECTURE §3. |
| T-28-23 | I | File-drop handler accidentally deleted during refactor — files no longer drop onto cells | mitigate | Acceptance criterion greps for `handleFileDrop`, `handleFileDragOver`, `dataTransfer.types.includes('Files')`. SC-4 integration test (Plan 10) also covers regression. |
| T-28-24 | T | `data-testid="leaf-${id}"` spoofed by another element | accept | Client-side only; attacker with DOM write has JS exec. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- All grep checks pass (20+ positive and negative greps)
- Test suite: no new regressions
- File-drop preservation: acceptance criteria include explicit greps for all 3 handlers
</verification>

<success_criteria>
- LeafNode consumes Wave 2 hooks + Wave 3 components; zero direct `@dnd-kit/core` DnD API usage.
- Source dim 40% + target 2px outline + cursor:grab all driven by scoped dragStore selectors.
- 5 inline zone JSX blocks collapsed to one `<DropZoneIndicators>` render.
- File-drop preserved unchanged (D-28).
</success_criteria>

<output>
After completion, create `.planning/phases/28-cell-to-cell-drag/28-08-SUMMARY.md`
</output>

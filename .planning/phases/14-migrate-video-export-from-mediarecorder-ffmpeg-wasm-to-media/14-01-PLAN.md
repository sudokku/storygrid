---
phase: 14-migrate-video-export-from-mediarecorder-ffmpeg-wasm-to-media
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/transcodeToMp4.ts
  - vercel.json
  - public/_headers
autonomous: true
requirements: []

must_haves:
  truths:
    - "@ffmpeg/ffmpeg and @ffmpeg/util are no longer in package.json"
    - "mediabunny and @mediabunny/aac-encoder are in package.json dependencies"
    - "src/lib/transcodeToMp4.ts does not exist"
    - "vercel.json has no COOP/COEP headers"
    - "public/_headers has no COOP/COEP headers"
  artifacts:
    - path: "package.json"
      provides: "mediabunny + aac-encoder deps; no ffmpeg deps"
      contains: "mediabunny"
    - path: "vercel.json"
      provides: "Clean headers config (or empty/deleted)"
    - path: "public/_headers"
      provides: "Clean headers (or empty/deleted)"
  key_links: []
---

<objective>
Remove the ffmpeg.wasm dependency chain and reinstall mediabunny with its AAC encoder extension.

Purpose: Clear the old pipeline dependencies (D-08, D-09, D-10) so Plan 14-02 can build the new Mediabunny pipeline without import conflicts or stale code.

Output: Clean package.json with mediabunny deps, no ffmpeg deps, no COOP/COEP headers, no transcodeToMp4.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-migrate-video-export-from-mediarecorder-ffmpeg-wasm-to-media/14-CONTEXT.md
@.planning/phases/14-migrate-video-export-from-mediarecorder-ffmpeg-wasm-to-media/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove ffmpeg packages, reinstall mediabunny + aac-encoder, delete transcodeToMp4.ts</name>
  <files>package.json, src/lib/transcodeToMp4.ts, src/lib/videoExport.ts</files>
  <read_first>
    - package.json — current dependencies to verify ffmpeg entries and check if mediabunny already present
    - src/lib/transcodeToMp4.ts — confirm file exists before deletion
    - src/lib/videoExport.ts — verify it imports from transcodeToMp4 (the import will be fixed in Plan 14-02)
  </read_first>
  <action>
    1. Run `npm uninstall @ffmpeg/ffmpeg @ffmpeg/util` to remove ffmpeg packages from package.json and node_modules.

    2. Run `npm install mediabunny@^1.40.1 @mediabunny/aac-encoder` to add Mediabunny and its AAC encoder extension.

    3. Delete `src/lib/transcodeToMp4.ts` (per D-09).

    4. In `src/lib/videoExport.ts`, comment out or remove the import line `import { transcodeWebmToMp4 } from './transcodeToMp4';` to prevent build errors. Do NOT rewrite the rest of videoExport.ts — that is Plan 14-02's job. Just remove/comment the import that would cause a missing module error. The file will have TypeScript errors from the transcodeWebmToMp4 usage later in the file — that is expected and will be fixed in Plan 14-02.

    5. Verify `package.json` contains `mediabunny` and `@mediabunny/aac-encoder` in dependencies, and does NOT contain `@ffmpeg/ffmpeg` or `@ffmpeg/util`.
  </action>
  <acceptance_criteria>
    - package.json contains "mediabunny" in dependencies
    - package.json contains "@mediabunny/aac-encoder" in dependencies
    - package.json does NOT contain "@ffmpeg/ffmpeg"
    - package.json does NOT contain "@ffmpeg/util"
    - src/lib/transcodeToMp4.ts does NOT exist
    - src/lib/videoExport.ts does NOT contain `import { transcodeWebmToMp4 }`
  </acceptance_criteria>
  <verify>
    <automated>grep -c '"mediabunny"' /Users/radu/Developer/storygrid/package.json && grep -c '"@mediabunny/aac-encoder"' /Users/radu/Developer/storygrid/package.json && ! grep -q '@ffmpeg/ffmpeg' /Users/radu/Developer/storygrid/package.json && ! test -f /Users/radu/Developer/storygrid/src/lib/transcodeToMp4.ts && echo "PASS"</automated>
  </verify>
  <done>package.json has mediabunny + aac-encoder, no ffmpeg. transcodeToMp4.ts deleted. videoExport.ts import removed.</done>
</task>

<task type="auto">
  <name>Task 2: Remove COOP/COEP headers from vercel.json and public/_headers</name>
  <files>vercel.json, public/_headers</files>
  <read_first>
    - vercel.json — current COOP/COEP header entries
    - public/_headers — current COOP/COEP header entries
  </read_first>
  <action>
    Per D-10: WebCodecs + Mediabunny do not require SharedArrayBuffer, so COOP/COEP headers are no longer needed.

    1. **vercel.json**: The file currently contains ONLY the COOP/COEP headers. Since there is no other configuration, delete the entire file. If there were other Vercel config entries, you would only remove the headers — but the current file has nothing else.

    2. **public/_headers**: The file currently contains ONLY the COOP/COEP headers for Netlify. Since there is no other content, delete the entire file.

    3. Verify neither file exists or that neither contains "Cross-Origin-Opener-Policy" or "Cross-Origin-Embedder-Policy".
  </action>
  <acceptance_criteria>
    - vercel.json does NOT exist OR does NOT contain "Cross-Origin-Opener-Policy"
    - public/_headers does NOT exist OR does NOT contain "Cross-Origin-Opener-Policy"
    - vercel.json does NOT exist OR does NOT contain "Cross-Origin-Embedder-Policy"
    - public/_headers does NOT exist OR does NOT contain "Cross-Origin-Embedder-Policy"
  </acceptance_criteria>
  <verify>
    <automated>! grep -rq 'Cross-Origin-Opener-Policy' /Users/radu/Developer/storygrid/vercel.json /Users/radu/Developer/storygrid/public/_headers 2>/dev/null && echo "PASS"</automated>
  </verify>
  <done>vercel.json and public/_headers deleted (or emptied). No COOP/COEP headers remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan only removes dependencies and config files.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | npm packages | accept | mediabunny is from the same author as mp4-muxer (verified); standard npm install with lockfile |
| T-14-02 | Information Disclosure | COOP/COEP removal | accept | COOP/COEP were only needed for SharedArrayBuffer (ffmpeg.wasm). Removing them does not expose new data — all processing remains client-side with no server communication |
</threat_model>

<verification>
- `npm ls mediabunny` shows installed version
- `npm ls @mediabunny/aac-encoder` shows installed version
- `npm ls @ffmpeg/ffmpeg` returns "not found" / error
- No COOP/COEP strings in vercel.json or public/_headers
- src/lib/transcodeToMp4.ts does not exist
</verification>

<success_criteria>
Package.json reflects the new dependency set (mediabunny in, ffmpeg out). COOP/COEP headers removed. transcodeToMp4.ts deleted. The codebase is ready for Plan 14-02 to rewrite videoExport.ts.
</success_criteria>

<output>
After completion, create `.planning/phases/14-migrate-video-export-from-mediarecorder-ffmpeg-wasm-to-media/14-01-SUMMARY.md`
</output>

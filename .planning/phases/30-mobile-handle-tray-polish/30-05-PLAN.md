---
phase: 30-mobile-handle-tray-polish
plan: 05
type: execute
wave: 3
depends_on:
  - 30-04
files_modified: []
autonomous: false
requirements:
  - CROSS-02
  - CROSS-03
  - CROSS-04
  - CROSS-05
  - CROSS-06
  - CROSS-07
  - CROSS-08

must_haves:
  truths:
    - "Real-device UAT completed on iOS Safari and Android Chrome"
    - "All 10 UAT checklist items verified or explicitly noted as deferred"
    - "Sensor delay discrepancy (500ms vs 250ms DRAG-03) reviewed by human"
    - "Phase 30 declared complete"
  artifacts:
    - path: ".planning/phases/30-mobile-handle-tray-polish/30-05-SUMMARY.md"
      provides: "UAT results documented"
  key_links: []
---

<objective>
Human real-device UAT for Phase 30 mobile interactions. All seven CROSS requirements have been automated-tested in Waves 0–2; this checkpoint verifies the behaviors that cannot be tested in jsdom: haptic pulses, iOS interference suppression, sheet collapse/restore, and touch-action scope preservation.

Purpose: Gate Phase 30 completion on evidence that mobile interactions work correctly on real hardware.
Output: UAT checklist results documented in SUMMARY.md.
</objective>

<execution_context>
@/Users/radu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/radu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-mobile-handle-tray-polish/30-VALIDATION.md
@.planning/phases/30-mobile-handle-tray-polish/30-UI-SPEC.md
@.planning/phases/30-mobile-handle-tray-polish/30-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated suite and TypeScript check</name>
  <read_first>
    - .planning/phases/30-mobile-handle-tray-polish/30-04-SUMMARY.md
  </read_first>
  <files></files>
  <action>
Run the full test suite and TypeScript compile to confirm the automated baseline is clean before UAT:

```bash
cd /Users/radu/Developer/storygrid && npx tsc --noEmit && npx vitest run
```

If either command fails, fix the failures before proceeding to the UAT checkpoint.

Document the final suite result (pass counts, test duration) in the SUMMARY.md.

Also flag the sensor delay discrepancy for human review:
- Current: `activationConstraint: { delay: 500, tolerance: 8 }` in CanvasWrapper.tsx line 67
- DRAG-03 specifies: 250ms + 5px
- Phase 30 did NOT change this — record it as an open item in SUMMARY.md with: "UAT: Is 500ms hold delay intentional or a Phase 28 regression?"
  </action>
  <verify>
    <automated>cd /Users/radu/Developer/storygrid && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0
    - `npx tsc --noEmit` exits 0
    - Sensor delay discrepancy documented in SUMMARY.md
  </acceptance_criteria>
  <done>Automated suite is fully green. Sensor discrepancy documented for human review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All Phase 30 CROSS-02 through CROSS-08 changes have been implemented and automated tests pass:
- CROSS-02/03: `useCellDraggable` returns `touchAction:'none'` and `WebkitTouchCallout:'none'`
- CROSS-04: `document.body.style.userSelect = 'none'` toggled during drag
- CROSS-05: `contextmenu` capture listener attached/removed per drag lifecycle
- CROSS-06: `navigator.vibrate?.(10)` on drag activation
- CROSS-07: `navigator.vibrate?.(15)` on successful drop
- CROSS-08: `dragStore.prevSheetSnapState` saves/restores sheet state; `MobileCellTray` fades out during drag

**OPEN ITEM REQUIRING YOUR DECISION:**
The sensor activation delay in `CanvasWrapper.tsx` line ~67 is currently `{ delay: 500, tolerance: 8 }`.
DRAG-03 specifies 250ms + 5px. This was NOT changed in Phase 30.
Please confirm: is 500ms intentional (based on Android Chrome UAT feedback post-Phase 28), or should it be changed to `{ delay: 250, tolerance: 5 }`?
  </what-built>
  <how-to-verify>
Test on a real iOS device (Safari) and Android device (Chrome). Run `npm run dev` first and open the app on the device via your local network IP.

**UAT Checklist — verify each item:**

| # | Device | Test | Expected | Your Result |
|---|--------|------|----------|-------------|
| 1 | Android Chrome | Long-press a cell for ~500ms (current delay) then drag | 10ms haptic fires at activation; drag starts | |
| 2 | Android Chrome | Drop a dragged cell onto a different cell | 15ms haptic fires after drop; cells swap | |
| 3 | Android Chrome | Drop a cell on itself (origin cell) | No haptic; cell returns to position | |
| 4 | iOS Safari | Long-press a cell with media | Image-action menu (Save/Copy/Share sheet) does NOT appear | |
| 5 | iOS Safari | Long-press and drag | Text selection loupe does NOT appear | |
| 6 | iOS/Android | Start dragging a cell | Bottom sheet collapses to tab-strip; action tray fades out | |
| 7 | iOS/Android | Release drag (any outcome — drop or cancel) | Bottom sheet restores to prior state; action tray reappears | |
| 8 | iOS/Android | Drag a cell, then attempt to scroll the bottom sheet | Sheet scroll should still work when not dragging a cell | |
| 9 | iOS Safari | Pinch-to-zoom on canvas (NOT while dragging) | Pinch-to-zoom still works | |
| 10 | Desktop Chrome | Right-click while dragging a cell | Context menu does NOT appear during drag; appears normally otherwise | |
  </how-to-verify>
  <resume-signal>Type "UAT complete: [PASS/FAIL items]" — list any failing items with notes. If all pass, type "UAT complete: all pass". If you want to skip UAT and mark as deferred, type "UAT deferred".</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Real device → app | UAT on personal device; no credentials or sensitive data involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-09 | Information Disclosure | navigator.vibrate | accept | vibrate() is output-only; no user data read |
</threat_model>

<verification>
All items in the UAT checklist above verified manually on real devices.
Final automated check: `npx vitest run` exits 0.
</verification>

<success_criteria>
- `npx vitest run` exits 0
- `npx tsc --noEmit` exits 0
- UAT checkpoint reviewed (pass, fail with notes, or explicitly deferred)
- Sensor delay discrepancy confirmed or resolved by human decision
- Phase 30 declared complete in SUMMARY.md
</success_criteria>

<output>
After completion, create `.planning/phases/30-mobile-handle-tray-polish/30-05-SUMMARY.md` with:
- UAT checklist results (pass/fail per item)
- Human decision on sensor delay (500ms vs 250ms)
- Any follow-up items for Phase 31 or quick tasks
</output>

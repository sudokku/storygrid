---
phase: 13
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - package.json
  - package-lock.json
  - index.html
  - src/Grid/CanvasWrapper.tsx
  - src/Grid/OverlayLayer.tsx
  - src/Grid/__tests__/OverlayLayer.test.tsx
  - src/Editor/OverlayHandles.tsx
  - src/Editor/__tests__/OverlayHandles.test.tsx
autonomous: true
requirements: [OVL-10, OVL-11, OVL-12, OVL-13]
must_haves:
  truths:
    - "OverlayLayer mounts inside canvas-surface and renders all overlays from useOverlayStore as absolutely-positioned divs in canvas pixel space using CENTER-based coordinates (translate(-50%,-50%))"
    - "User can drag any selected overlay via pointerdown on its body — viewport pixel delta converted to canvas delta via editorStore.canvasScale"
    - "User can resize any selected overlay via a corner handle — proportional width growth"
    - "User can rotate any selected overlay via a top handle — degrees stored in overlay.rotation"
    - "Delete key while an overlay is selected calls overlayStore.deleteOverlay (OVL-13)"
    - "Delete key fired while focus is inside an INPUT/TEXTAREA/contentEditable does NOT delete the selected overlay (security guard for sidebar text editing)"
    - "Google Fonts (Playfair Display, Dancing Script) preloaded via <link> in index.html for D-17/D-18"
    - "emoji-mart + dompurify installed in package.json"
  artifacts:
    - path: "src/Grid/OverlayLayer.tsx"
      provides: "DOM overlay layer rendering all overlays + hosting selection handles"
      exports: ["OverlayLayer"]
    - path: "src/Grid/__tests__/OverlayLayer.test.tsx"
      provides: "unit tests for OverlayLayer: render-from-store, click selection, Delete keydown, input-target guard"
    - path: "src/Editor/OverlayHandles.tsx"
      provides: "drag body + corner resize handle + top rotation handle with setPointerCapture"
      exports: ["OverlayHandles"]
    - path: "index.html"
      provides: "Google Fonts preload for Playfair Display + Dancing Script"
      contains: "Playfair+Display"
    - path: "package.json"
      provides: "emoji-mart, @emoji-mart/react, @emoji-mart/data, dompurify, @types/dompurify dependencies"
      contains: "emoji-mart"
  key_links:
    - from: "src/Grid/CanvasWrapper.tsx"
      to: "src/Grid/OverlayLayer.tsx"
      via: "component mount inside canvas-surface as sibling of GridNode root"
      pattern: "<OverlayLayer"
    - from: "src/Grid/OverlayLayer.tsx"
      to: "src/store/overlayStore.ts"
      via: "useOverlayStore(state => state.overlays) subscription"
      pattern: "useOverlayStore"
    - from: "src/Editor/OverlayHandles.tsx"
      to: "src/store/editorStore.ts"
      via: "canvasScale read for viewport->canvas conversion"
      pattern: "canvasScale"
---

<objective>
Build the DOM overlay layer, drag/resize/rotate handle loop, keyboard delete, and install new dependencies + load Google Fonts. At the end of this plan, a user with a programmatically-added overlay in the store can drag, resize, rotate, and Delete-key-remove it in the live editor.

Purpose: Makes OVL-10/11/12/13 testable end-to-end in the editor. Plan 04 (Add button) and Plan 05 (sidebar + inline text) will plug into this rendering surface.
Output: Two new components, CanvasWrapper integration, dependency install, Google Fonts preload, two test files (OverlayLayer + OverlayHandles).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-text-sticker-overlay-layer/13-CONTEXT.md
@.planning/phases/13-text-sticker-overlay-layer/13-RESEARCH.md
@.planning/phases/13-01-PLAN.md
@.planning/phases/13-text-sticker-overlay-layer/13-01-SUMMARY.md

@src/Grid/CanvasWrapper.tsx
@src/Grid/Divider.tsx
@src/store/editorStore.ts
@src/store/overlayStore.ts
@index.html

<interfaces>
<!-- =================================================================== -->
<!-- CRITICAL OVERRIDE: COORDINATE SEMANTICS                              -->
<!--                                                                       -->
<!-- This plan (and Plans 03 and 05) uses CENTER-BASED positioning:        -->
<!--   overlay.x, overlay.y  =  the VISUAL CENTER of the overlay           -->
<!--   DOM wrapper transform =  translate(-50%, -50%) rotate(...)          -->
<!--   Canvas export        =  converts center -> top-left via             -->
<!--                            drawX = overlay.x - overlay.width / 2      -->
<!--                                                                       -->
<!-- 13-RESEARCH.md §Pattern 2 described a TOP-LEFT positioning model.     -->
<!-- THAT MODEL IS SUPERSEDED for the DOM layer and the export path.       -->
<!-- If RESEARCH.md and this plan disagree on positioning semantics,       -->
<!-- THIS PLAN WINS. Following RESEARCH.md §Pattern 2 here will cause      -->
<!-- OVL-16 WYSIWYG drift — the live DOM preview will not match exports.   -->
<!--                                                                       -->
<!-- Plan 03 drawOverlaysToCanvas and Plan 05 InlineTextEditor both assume -->
<!-- center semantics — they match each other and this plan.               -->
<!-- =================================================================== -->

<!-- Shapes from Plan 01 — already committed. -->
useOverlayStore: exposes `overlays: Overlay[]`, `updateOverlay(id, updates)`, `deleteOverlay(id)`
useEditorStore: exposes `selectedOverlayId: string | null`, `setSelectedOverlayId(id)`, `canvasScale: number`

OverlayHandles props (new component signature this plan defines):
```typescript
export interface OverlayHandlesProps {
  overlay: Overlay;             // from src/types
  canvasScale: number;          // from editorStore
  onUpdate: (updates: Partial<Overlay>) => void;  // wraps updateOverlay(overlay.id, ...)
}
```

CanvasWrapper.tsx canvas-surface structure (VERIFIED from codebase):
```tsx
<div className="canvas-surface" style={{ transform: `scale(${finalScale})`, width: 1080, height: 1920 }}>
  <GridNodeComponent id={rootId} />
  {showSafeZone && <SafeZoneOverlay />}
</div>
```
OverlayLayer must mount inside canvas-surface as a sibling.

Divider.tsx pointer loop (pattern to replicate for drag/resize/rotate):
```tsx
onPointerDown(e) {
  e.currentTarget.setPointerCapture(e.pointerId);
  const startClientX = e.clientX;
  // ... store startClientY, start values
}
onPointerMove(e) {
  if (!e.currentTarget.hasPointerCapture(e.pointerId)) return;
  const deltaViewport = e.clientX - startClientX;
  const deltaCanvas = deltaViewport / canvasScale;
  // updateOverlay(id, { ... })
}
onPointerUp(e) {
  e.currentTarget.releasePointerCapture?.(e.pointerId);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies, preload Google Fonts, scaffold OverlayLayer + targeted OverlayLayer tests</name>
  <files>package.json, package-lock.json, index.html, src/Grid/OverlayLayer.tsx, src/Grid/CanvasWrapper.tsx, src/Grid/__tests__/OverlayLayer.test.tsx</files>
  <read_first>
    - package.json (confirm current deps — avoid duplicate installs; note tailwindcss ^3.4, react ^18.3)
    - index.html (current font preload pattern — match existing Geist load order exactly)
    - src/Grid/CanvasWrapper.tsx (integration point; note `canvas-surface` class/div structure, `finalScale` prop threading)
    - src/store/overlayStore.ts (read Overlay types and state shape from Plan 01)
    - .planning/phases/13-text-sticker-overlay-layer/13-RESEARCH.md §Code Examples (CanvasWrapper integration snippet, OverlayLayer container styles) — BUT treat §Pattern 2 coordinate semantics as SUPERSEDED; use center-based positioning per the override note above
    - .planning/phases/13-text-sticker-overlay-layer/13-CONTEXT.md §Decisions D-09, D-10, D-17
  </read_first>
  <behavior>
    OverlayLayer.test.tsx must contain at minimum these cases:
    - Test 1: Given `useOverlayStore.setState({ overlays: [textOverlay1, textOverlay2] })`, rendering OverlayLayer produces two overlay wrapper elements (assertable via data-testid or by text content match on the TextOverlay's content string).
    - Test 2: Clicking on an overlay's wrapper calls `useEditorStore.getState().setSelectedOverlayId(overlay.id)` — after `fireEvent.pointerDown(element)`, assert `useEditorStore.getState().selectedOverlayId === 'o1'`.
    - Test 3 (OVL-13): With `selectedOverlayId` set to 'o1', dispatching a `keydown` event on `window` with `{ key: 'Delete' }` removes the overlay from the store — after the event, assert `useOverlayStore.getState().overlays.length === 0` and `useEditorStore.getState().selectedOverlayId === null`.
    - Test 4 (OVL-13 input guard — SECURITY CRITICAL): With `selectedOverlayId` set to 'o1' AND an `<input>` element focused (create an input in the test DOM, focus it), dispatch a `keydown` event with `{ key: 'Delete', target: inputElement }` (or use `fireEvent.keyDown(inputElement, { key: 'Delete' })`). Assert the overlay is STILL in the store: `useOverlayStore.getState().overlays.length === 1`. This guarantees users editing text in the sidebar content input do not accidentally nuke their overlay.
    - Test 5 (Backspace input guard): Same as Test 4 but with `{ key: 'Backspace' }` targeting a TEXTAREA element — overlay must remain.
  </behavior>
  <action>
    STEP 1 — Install dependencies. Run exactly:
      `npm install emoji-mart@^5.6.0 @emoji-mart/react@^1.1.1 @emoji-mart/data@^1.2.1 dompurify@^3.3.3`
      `npm install -D @types/dompurify@^3.2.0`
    After install, verify package.json contains `"emoji-mart"`, `"@emoji-mart/react"`, `"@emoji-mart/data"`, `"dompurify"`, and devDependencies contains `"@types/dompurify"`.

    STEP 2 — Preload Google Fonts in index.html. Add ABOVE the existing Geist font link (match position of current preconnect entries):
    ```html
    &lt;link rel="preconnect" href="https://fonts.googleapis.com"&gt;
    &lt;link rel="preconnect" href="https://fonts.gstatic.com" crossorigin&gt;
    &lt;link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&amp;family=Dancing+Script:wght@400;700&amp;display=swap" rel="stylesheet"&gt;
    ```
    Do NOT remove or modify the existing Geist font loading.

    STEP 3 — Create src/Grid/OverlayLayer.tsx. This component:
      - Is a default named export `OverlayLayer`
      - Subscribes to `useOverlayStore(state =&gt; state.overlays)` and `useEditorStore(state =&gt; state.selectedOverlayId)` and `useEditorStore(state =&gt; state.canvasScale)`
      - Renders a container div with exactly these styles (set via inline style so nothing depends on Tailwind config):
        `{ position: 'absolute', inset: 0, pointerEvents: 'none', zIndex: 10 }`
      - Inside the container, `.sort((a,b) =&gt; a.zIndex - b.zIndex)` the overlays, then map each to an overlay element:
        - A `&lt;div data-testid={`overlay-${overlay.id}`}&gt;` with style: `{ position: 'absolute', left: overlay.x, top: overlay.y, width: overlay.width, transformOrigin: 'center center', transform: 'translate(-50%, -50%) rotate(' + overlay.rotation + 'deg)', pointerEvents: 'auto', cursor: 'move' }` — the translate(-50%,-50%) centers the overlay on (x,y) so rotation pivots around the visual center per the coordinate-semantics override above. overlay.x and overlay.y ARE the visual center; do not compute any top-left adjustment here.
        - onPointerDown handler sets `useEditorStore.getState().setSelectedOverlayId(overlay.id)` and stops propagation.
        - For `type === 'text'`: render `&lt;div&gt;` with children = overlay.content, style includes fontFamily, fontSize, color, fontWeight (regular→400, bold→700), textAlign, whiteSpace: 'pre-wrap', userSelect: 'none'.
        - For `type === 'emoji'`: render overlay.char inside a span with fontSize: overlay.width.
        - For `type === 'sticker'`: render `&lt;img src={stickerRegistry[overlay.stickerRegistryId]} /&gt;` with style width: overlay.width, height: 'auto', draggable: false, display: 'block'.
        - If `overlay.id === selectedOverlayId`, render `&lt;OverlayHandles overlay={overlay} canvasScale={canvasScale} onUpdate={(u) =&gt; updateOverlay(overlay.id, u)} /&gt;` as a child (OverlayHandles is stubbed in this task, implemented in Task 2).
      - Add a keydown listener effect on `window` that triggers `deleteOverlay(selectedOverlayId)` and `setSelectedOverlayId(null)` when `e.key === 'Delete' || e.key === 'Backspace'` AND `selectedOverlayId !== null` AND the event target is not an input/textarea/contenteditable. Implement the guard EXACTLY as:
        ```ts
        const target = e.target as HTMLElement | null;
        if (target) {
          const tag = target.tagName;
          if (tag === 'INPUT' || tag === 'TEXTAREA' || target.isContentEditable) return;
        }
        ```
        This guard is security-critical for OVL-13 — users editing sidebar text inputs must not lose their overlay to a stray Delete keypress.

    STEP 4 — Create a STUB file src/Editor/OverlayHandles.tsx that exports a named `OverlayHandles` component taking `{ overlay, canvasScale, onUpdate }` props and returning `null`. Full implementation comes in Task 2 of this plan — this stub lets OverlayLayer compile and render without handles until Task 2.

    STEP 5 — Update src/Grid/CanvasWrapper.tsx to mount `&lt;OverlayLayer /&gt;` INSIDE the canvas-surface div as a sibling of the root GridNode. Import `OverlayLayer` from './OverlayLayer'. Place it AFTER `&lt;GridNodeComponent id={rootId} /&gt;` and BEFORE any SafeZoneOverlay.

    STEP 6 — Create src/Grid/__tests__/OverlayLayer.test.tsx implementing the 5 behavior tests above. Setup pattern per test:
    ```typescript
    import { render, fireEvent, cleanup } from '@testing-library/react';
    import { beforeEach, afterEach, describe, it, expect } from 'vitest';
    import { OverlayLayer } from '../OverlayLayer';
    import { useOverlayStore } from '../../store/overlayStore';
    import { useEditorStore } from '../../store/editorStore';

    const makeText = (id: string, content = 'hello') => ({
      id, type: 'text' as const, x: 540, y: 960, width: 400, rotation: 0, zIndex: 1,
      content, fontFamily: 'Geist', fontSize: 72, color: '#ffffff',
      fontWeight: 'regular' as const, textAlign: 'center' as const,
    });

    beforeEach(() => {
      useOverlayStore.setState({ overlays: [], stickerRegistry: {} });
      useEditorStore.setState({ selectedNodeId: null, selectedOverlayId: null, canvasScale: 1 });
    });
    afterEach(() => cleanup());
    ```
    - Test 1 (render): `useOverlayStore.setState({ overlays: [makeText('o1','A'), makeText('o2','B')] })`; render OverlayLayer; assert `getByTestId('overlay-o1')` and `getByTestId('overlay-o2')` exist.
    - Test 2 (click select): render; `fireEvent.pointerDown(getByTestId('overlay-o1'))`; assert `useEditorStore.getState().selectedOverlayId === 'o1'`.
    - Test 3 (Delete removes): seed `overlays: [makeText('o1')]`, `selectedOverlayId: 'o1'`; render; `fireEvent.keyDown(window, { key: 'Delete' })`; assert `useOverlayStore.getState().overlays.length === 0` and `selectedOverlayId === null`.
    - Test 4 (INPUT guard): seed one overlay + selectedOverlayId; render `<><OverlayLayer /><input data-testid="sidebar-input" /></>`; focus the input; `fireEvent.keyDown(getByTestId('sidebar-input'), { key: 'Delete' })`; assert `useOverlayStore.getState().overlays.length === 1`.
    - Test 5 (TEXTAREA + Backspace guard): same setup but `<textarea data-testid="sidebar-ta" />`; `fireEvent.keyDown(getByTestId('sidebar-ta'), { key: 'Backspace' })`; assert overlay still present.

    STEP 7 — Run `npx tsc --noEmit` and `npx vitest run src/Grid/__tests__/OverlayLayer.test.tsx` and then `npx vitest run` for the full regression. All green. Commit: `feat(13-02): install overlay deps, preload fonts, scaffold OverlayLayer + CanvasWrapper mount + Delete/input-guard tests`.
  </action>
  <verify>
    <automated>npx vitest run src/Grid/__tests__/OverlayLayer.test.tsx &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - grep `"emoji-mart"` in package.json returns match
    - grep `"@emoji-mart/react"` in package.json returns match
    - grep `"@emoji-mart/data"` in package.json returns match
    - grep `"dompurify"` in package.json returns match (dependencies block)
    - grep `"@types/dompurify"` in package.json returns match (devDependencies block)
    - grep `Playfair+Display` in index.html returns match
    - grep `Dancing+Script` in index.html returns match
    - File src/Grid/OverlayLayer.tsx exists and contains `export function OverlayLayer` OR `export const OverlayLayer`
    - grep `useOverlayStore` in src/Grid/OverlayLayer.tsx returns match
    - grep `selectedOverlayId` in src/Grid/OverlayLayer.tsx returns match
    - grep `translate(-50%, -50%)` in src/Grid/OverlayLayer.tsx returns match (rotation pivot fix / center coordinate semantics)
    - grep `'Delete'` OR `'Backspace'` in src/Grid/OverlayLayer.tsx returns match (OVL-13 keyboard delete)
    - grep `INPUT` in src/Grid/OverlayLayer.tsx returns match (input-target guard)
    - grep `isContentEditable` in src/Grid/OverlayLayer.tsx returns match
    - grep `<OverlayLayer` in src/Grid/CanvasWrapper.tsx returns match
    - File src/Editor/OverlayHandles.tsx exists (stub)
    - File src/Grid/__tests__/OverlayLayer.test.tsx exists and contains at least 5 `it(` or `test(` blocks
    - grep `'INPUT guard'` OR `input guard` OR `sidebar-input` in src/Grid/__tests__/OverlayLayer.test.tsx returns match
    - grep `Backspace` in src/Grid/__tests__/OverlayLayer.test.tsx returns match
    - `npx vitest run src/Grid/__tests__/OverlayLayer.test.tsx` exits 0 — all 5 tests GREEN
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` exits 0
    - Commit exists matching `feat(13-02)`
  </acceptance_criteria>
  <done>Deps installed, fonts preloaded, OverlayLayer mounted inside canvas-surface using center-based coordinates, clicking an overlay selects it via editorStore, Delete key removes selected overlay, Delete key inside INPUT/TEXTAREA/contentEditable is a no-op, 5 targeted OverlayLayer tests green, TypeScript + full vitest suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement drag / resize / rotate handles with setPointerCapture</name>
  <files>src/Editor/OverlayHandles.tsx, src/Editor/__tests__/OverlayHandles.test.tsx</files>
  <read_first>
    - src/Editor/OverlayHandles.tsx (the stub from Task 1 — replace with real implementation)
    - src/Grid/Divider.tsx (canonical setPointerCapture pointer-event loop — mirror structure)
    - src/store/editorStore.ts (canvasScale is the viewport→canvas scalar)
    - .planning/phases/13-text-sticker-overlay-layer/13-RESEARCH.md §Pattern 3 (drag/resize/rotate math — copy the math verbatim). REMINDER: Pattern 2 coordinate semantics are SUPERSEDED — use center-based overlay.x/overlay.y throughout (see override box in <interfaces> above).
    - .planning/phases/13-text-sticker-overlay-layer/13-CONTEXT.md §Decisions D-11
    - src/Grid/__tests__ (test setup conventions — React import, jsdom pointer-event polyfills)
  </read_first>
  <behavior>
    - Test 1 (OVL-10 drag): render OverlayHandles with a text overlay at x=100, y=100, width=200, rotation=0; simulate pointerdown on the drag body at clientX=0,clientY=0, then pointermove to clientX=50,clientY=25 (with canvasScale=1); onUpdate should be called with { x: 150, y: 125 }
    - Test 2 (OVL-10 drag with scale): same as above but canvasScale=0.5; pointermove delta 50,25 viewport = 100,50 canvas; onUpdate called with { x: 200, y: 150 }
    - Test 3 (OVL-11 resize corner): render with width=200; pointerdown on corner handle, pointermove by Math.hypot delta 40 at canvasScale=1; onUpdate called with { width: 240 } (min clamp 40)
    - Test 4 (OVL-12 rotate): render with x=100,y=100,width=200; fake pointerdown on rotation handle, fake pointermove to a position 90° clockwise from overlay center; onUpdate called with rotation approx 0 (handle points at 12 o'clock after -90 offset)
    - Test 5: jsdom pointer capture guards — `setPointerCapture` on a handle element does not throw when the method is undefined (wrap in if-exists check per Phase 05 precedent)
  </behavior>
  <action>
    Replace the stub in src/Editor/OverlayHandles.tsx with a full implementation:

    STEP 1 — Component accepts `{ overlay, canvasScale, onUpdate }: OverlayHandlesProps`. Return a fragment containing:

    (a) A transparent "drag body" div absolutely positioned to cover the overlay bounds:
        `{ position: 'absolute', left: 0, top: 0, width: '100%', height: '100%', cursor: 'move', pointerEvents: 'auto' }`
        onPointerDown: record startClientX=e.clientX, startClientY=e.clientY, startX=overlay.x, startY=overlay.y. Call `e.currentTarget.setPointerCapture?.(e.pointerId)`. Stop propagation. Store these on a ref (useRef) called `dragStateRef`.
        onPointerMove: if dragStateRef.current is null return. deltaViewportX = e.clientX - startClientX. deltaCanvasX = deltaViewportX / canvasScale. Call `onUpdate({ x: startX + deltaCanvasX, y: startY + deltaCanvasY })`.
        onPointerUp: `e.currentTarget.releasePointerCapture?.(e.pointerId)`; dragStateRef.current = null.

    (b) A corner resize handle: `&lt;div&gt;` positioned at bottom-right of overlay bounds, size 12x12px (use scale(1/canvasScale) transform to keep visually stable matching the ActionBar pattern from Phase 07). Style: `{ position: 'absolute', right: -6, bottom: -6, width: 12, height: 12, background: '#ffffff', border: '1px solid #3b82f6', cursor: 'nwse-resize', pointerEvents: 'auto', transform: 'scale(' + (1/canvasScale) + ')', transformOrigin: 'center' }`.
        onPointerDown: record startClientX, startClientY, startWidth=overlay.width. setPointerCapture. Stop propagation. Ref: `resizeStateRef`.
        onPointerMove: `const dx = e.clientX - startClientX; const dy = e.clientY - startClientY; const viewportDelta = Math.hypot(dx, dy) * Math.sign(dx + dy); const canvasDelta = viewportDelta / canvasScale; const newWidth = Math.max(40, startWidth + canvasDelta); onUpdate({ width: newWidth });`
        onPointerUp: release + clear ref.

    (c) A rotation handle: `&lt;div&gt;` positioned above the overlay's top edge (e.g. top: -24px, left: 50%, translate(-50%, 0)). 12x12 circle. Color distinct from resize handle. Style scaled by 1/canvasScale like the corner handle.
        onPointerDown: compute the overlay's center point in viewport coordinates. Since the handle's parent is the overlay wrapper div inside canvas-surface, use `handleElement.closest('.canvas-surface')?.getBoundingClientRect()` to find canvas-surface origin, then `overlayCenterViewportX = (overlay.x) * canvasScale + surfaceRect.left` and similarly for Y. (Note: overlay.x IS the center per the coordinate-semantics override.) Store center + startRotation=overlay.rotation. setPointerCapture.
        onPointerMove: compute `angleRad = Math.atan2(e.clientY - centerViewportY, e.clientX - centerViewportX); angleDeg = angleRad * (180 / Math.PI); onUpdate({ rotation: angleDeg + 90 });` — the +90 offset accounts for the handle sitting at 12 o'clock (-90° in atan2 convention).
        onPointerUp: release + clear ref.

    All setPointerCapture and releasePointerCapture calls must use optional chaining (`?.`) because jsdom lacks these APIs — follow the Phase 05 precedent for test compatibility.

    STEP 2 — Create src/Editor/__tests__/OverlayHandles.test.tsx with the 5 tests in the behavior block. Use @testing-library/react's render + fireEvent.pointerDown / pointerMove / pointerUp. Mock the overlay prop with a plain TextOverlay object. Use vi.fn() for onUpdate and assert calls with toHaveBeenCalledWith using expect.objectContaining for floating-point tolerance where needed. For rotation test, use `expect(onUpdate).toHaveBeenCalledWith(expect.objectContaining({ rotation: expect.any(Number) }))` AND inspect the actual call to verify angle is within ±2° of expected.

    STEP 3 — Run `npx vitest run src/Editor/__tests__/OverlayHandles.test.tsx`. All tests must pass. Commit: `feat(13-02): implement OverlayHandles drag/resize/rotate with setPointerCapture`.
  </action>
  <verify>
    <automated>npx vitest run src/Editor/__tests__/OverlayHandles.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep `setPointerCapture` in src/Editor/OverlayHandles.tsx returns match
    - grep `releasePointerCapture` in src/Editor/OverlayHandles.tsx returns match
    - grep `Math.atan2` in src/Editor/OverlayHandles.tsx returns match (rotation math)
    - grep `Math.hypot` in src/Editor/OverlayHandles.tsx returns match (resize math)
    - grep `canvasScale` in src/Editor/OverlayHandles.tsx returns at least 3 matches (drag, resize, rotate all use it)
    - grep `Math.max(40` in src/Editor/OverlayHandles.tsx returns match (min width clamp)
    - File src/Editor/__tests__/OverlayHandles.test.tsx contains at least 5 `it(` or `test(` blocks
    - grep `pointerdown` OR `pointerDown` in the test file returns match
    - `npx vitest run src/Editor/__tests__/OverlayHandles.test.tsx` exits 0
    - `npx tsc --noEmit` exits 0
    - Commit exists matching `feat(13-02): implement OverlayHandles`
  </acceptance_criteria>
  <done>OverlayHandles provides drag, proportional resize, and rotation on selected overlays via raw pointer events; unit tests verify the three interaction math paths; jsdom guards match Phase 05 precedent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google Fonts CDN | External resource loaded at build time; mitigated by `display=swap` fallback |
| pointer event → store | Untrusted pointer coordinates converted via canvasScale; no user data exfiltration |
| keyboard event target → overlay delete | User-focused input/textarea elements must not trigger overlay deletion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-04 | DoS | keydown Delete handler | medium | mitigate | Delete handler ignores events when target is INPUT, TEXTAREA, or contentEditable so sidebar text editing cannot accidentally trigger overlay deletion; guard is verified by Task 1 Test 4 and Test 5 (INPUT + TEXTAREA + Backspace) |
| T-13-05 | Tampering | overlay drag math | low | accept | Pointer coordinates client-side only; worst case a malicious user moves their own overlay outside canvas bounds (no security impact) |
</threat_model>

<verification>
- `npx vitest run` full suite green after both tasks
- `npx tsc --noEmit` green after both tasks
- Delete key in a text input does NOT delete a selected overlay — verified by OverlayLayer.test.tsx Test 4 (INPUT) and Test 5 (TEXTAREA + Backspace)
- Click-to-select behavior verified by OverlayLayer.test.tsx Test 2
- Store-to-DOM render verified by OverlayLayer.test.tsx Test 1
</verification>

<success_criteria>
- OverlayLayer mounts inside canvas-surface; overlays seeded programmatically via `useOverlayStore.getState().addOverlay(...)` render correctly in the editor
- Clicking an overlay selects it; corner handle resizes; top handle rotates; drag body moves; Delete removes; Delete inside INPUT is a no-op
- emoji-mart + dompurify available to Plan 04; Google Fonts loaded for Plan 05 font picker + Plan 03 export draw pass
- Coordinate semantics (center-based) consistent with Plan 03 export path — no WYSIWYG drift risk
</success_criteria>

<output>
After completion, create `.planning/phases/13-text-sticker-overlay-layer/13-02-SUMMARY.md`
</output>
</content>
</invoke>
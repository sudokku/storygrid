---
phase: 02-grid-rendering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/index.ts
  - src/lib/tree.ts
  - src/Grid/CanvasWrapper.tsx
  - src/Grid/SafeZoneOverlay.tsx
  - src/Grid/index.ts
  - src/Editor/CanvasArea.tsx
  - src/Editor/EditorShell.tsx
  - src/Editor/Toolbar.tsx
  - src/Editor/Sidebar.tsx
  - src/index.css
  - tailwind.config.js
  - src/test/grid-rendering.test.tsx
  - src/test/divider.test.tsx
  - src/test/canvas-wrapper.test.tsx
  - components.json
  - src/lib/utils.ts
  - src/components/ui/tooltip.tsx
autonomous: true
requirements:
  - REND-07
  - REND-08
  - REND-10

must_haves:
  truths:
    - "Canvas area shows a 9:16 aspect ratio surface that auto-scales to fit the editor viewport"
    - "Safe zone overlay shows dashed guides at 250px from top and bottom when toggled on"
    - "shadcn is initialized and Tooltip component is available for import"
    - "LeafNode type includes objectPosition field"
    - "Test scaffolds exist for all REND requirements"
  artifacts:
    - path: "src/Grid/CanvasWrapper.tsx"
      provides: "1080x1920 canvas with ResizeObserver auto-scale + zoom multiplier"
      exports: ["CanvasWrapper"]
    - path: "src/Grid/SafeZoneOverlay.tsx"
      provides: "Dashed guide overlay at safe zone positions"
      exports: ["SafeZoneOverlay"]
    - path: "src/types/index.ts"
      provides: "LeafNode with objectPosition?: string"
      contains: "objectPosition"
    - path: "src/lib/utils.ts"
      provides: "cn() utility from shadcn init"
      exports: ["cn"]
    - path: "src/test/grid-rendering.test.tsx"
      provides: "Test scaffolds for REND-01,02,04,05,06,08,09,10"
    - path: "src/test/divider.test.tsx"
      provides: "Test scaffolds for REND-03"
    - path: "src/test/canvas-wrapper.test.tsx"
      provides: "Test scaffolds for REND-07"
  key_links:
    - from: "src/Grid/CanvasWrapper.tsx"
      to: "src/store/editorStore.ts"
      via: "useEditorStore(s => s.zoom) and useEditorStore(s => s.showSafeZone)"
      pattern: "useEditorStore"
    - from: "src/Editor/CanvasArea.tsx"
      to: "src/Grid/CanvasWrapper.tsx"
      via: "import and render"
      pattern: "import.*CanvasWrapper"
---

<objective>
Foundation: type gap fix, shadcn initialization, dark theme shell, CanvasWrapper with ResizeObserver scaling, SafeZoneOverlay, and Wave 0 test scaffolds for all REND requirements.

Purpose: Establish the type system extension (objectPosition), UI library (shadcn/Radix), dark theme baseline, canvas scaling infrastructure, and test files that Plan 02 components will render into and be tested against.
Output: CanvasWrapper rendering a scaled 1080x1920 canvas, SafeZoneOverlay, dark-themed editor shell, shadcn Tooltip available, test scaffolds for all 10 REND requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-grid-rendering/02-CONTEXT.md
@.planning/phases/02-grid-rendering/02-RESEARCH.md
@.planning/phases/02-grid-rendering/02-UI-SPEC.md
@.planning/phases/02-grid-rendering/02-VALIDATION.md
@src/types/index.ts
@src/lib/tree.ts
@src/store/gridStore.ts
@src/store/editorStore.ts
@src/Editor/CanvasArea.tsx
@src/Editor/EditorShell.tsx
@src/Editor/Toolbar.tsx
@src/Editor/Sidebar.tsx
@src/index.css
@tailwind.config.js

<interfaces>
<!-- Existing store interfaces consumed by this plan -->

From src/store/editorStore.ts:
```typescript
export const useEditorStore = create<EditorState>()(set => ({
  selectedNodeId: null,
  zoom: 1,
  showSafeZone: false,
  activeTool: 'select',
  setSelectedNode: (id) => set({ selectedNodeId: id }),
  setZoom: (z) => set({ zoom: Math.min(1.5, Math.max(0.5, z)) }),
  toggleSafeZone: () => set((s) => ({ showSafeZone: !s.showSafeZone })),
  setActiveTool: (tool) => set({ activeTool: tool }),
}));
```

From src/store/gridStore.ts:
```typescript
export const useGridStore = create<GridStoreState>()(immer((set) => ({
  root: initialTree,
  mediaRegistry: {},
  // ... actions: split, merge, remove, resize, setMedia, updateCell, addMedia, removeMedia, undo, redo
})));
```

From src/types/index.ts (current — objectPosition MISSING):
```typescript
export type LeafNode = {
  type: 'leaf';
  id: string;
  mediaId: string | null;
  fit: 'cover' | 'contain';
};
export type ContainerNode = {
  type: 'container';
  id: string;
  direction: SplitDirection;
  sizes: number[];
  children: GridNode[];
};
export type GridNode = ContainerNode | LeafNode;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Type fix, shadcn init, dark theme shell, and CSS variables</name>
  <files>src/types/index.ts, src/lib/tree.ts, components.json, src/lib/utils.ts, src/components/ui/tooltip.tsx, tailwind.config.js, src/index.css, src/Editor/EditorShell.tsx, src/Editor/CanvasArea.tsx, src/Editor/Toolbar.tsx, src/Editor/Sidebar.tsx</files>
  <read_first>src/types/index.ts, src/lib/tree.ts, tailwind.config.js, src/index.css, src/Editor/EditorShell.tsx, src/Editor/CanvasArea.tsx, src/Editor/Toolbar.tsx, src/Editor/Sidebar.tsx, package.json</read_first>
  <action>
**Step 1 — Type gap fix (per D-13, Pitfall 6):**
Add `objectPosition?: string` to the `LeafNode` type in `src/types/index.ts`:
```typescript
export type LeafNode = {
  type: 'leaf';
  id: string;
  mediaId: string | null;
  fit: 'cover' | 'contain';
  objectPosition?: string;
};
```

Update `createLeaf()` in `src/lib/tree.ts` to include the default:
```typescript
export function createLeaf(): LeafNode {
  return { type: 'leaf', id: nanoid(), mediaId: null, fit: 'cover', objectPosition: 'center center' };
}
```

**Step 2 — shadcn init (per D-16, UI-SPEC):**
Run `npx shadcn@latest init` — when prompted:
- Style: Default
- Base color: Neutral
- CSS variables: Yes

Then run `npx shadcn@latest add tooltip`.

**CRITICAL post-init verification:**
1. Check `package.json` — React must still be `^18.3.1` (NOT upgraded to 19). If shadcn upgraded it, revert: `npm install react@^18.3.1 react-dom@^18.3.1`
2. Check `tailwind.config.js` — `content` array must still include `'./index.html', './src/**/*.{js,ts,jsx,tsx}'`. If shadcn changed it, restore those paths.
3. Check `src/index.css` — the `:root` block with `--canvas-width: 1080px`, `--canvas-height: 1920px`, `--safe-zone-top: 250px`, `--safe-zone-bottom: 250px` must still be present. If shadcn overwrote the file, re-add these variables inside `:root`.

After shadcn init, `src/lib/utils.ts` should exist with a `cn()` export (clsx + tailwind-merge). If shadcn placed it elsewhere, move it to `src/lib/utils.ts`.

**Step 3 — Dark theme editor shell (per UI-SPEC Color section):**
Update `src/Editor/EditorShell.tsx`:
- Change `bg-neutral-100` to `bg-[#111111]`
- The flex layout structure stays the same

Update `src/Editor/Toolbar.tsx`:
- Change `bg-white border-b border-neutral-200` to `bg-[#1c1c1c] border-b border-[#2a2a2a]`
- Change text color from `text-neutral-400` to `text-neutral-500`

Update `src/Editor/Sidebar.tsx`:
- Change `bg-white border-l border-neutral-200` to `bg-[#1c1c1c] border-l border-[#2a2a2a]`
- Change text color from `text-neutral-400` to `text-neutral-500`

Update `src/Editor/CanvasArea.tsx` — replace the entire placeholder with a minimal dark wrapper that Plan 02's Task 3 will integrate CanvasWrapper into:
```typescript
import { CanvasWrapper } from '../Grid/CanvasWrapper';

export function CanvasArea() {
  return (
    <main className="flex flex-1 items-start justify-center overflow-hidden bg-[#111111] p-8">
      <CanvasWrapper />
    </main>
  );
}
```
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/types/index.ts contains `objectPosition?: string` inside LeafNode type
    - src/lib/tree.ts createLeaf() returns object with `objectPosition: 'center center'`
    - components.json exists in project root (shadcn initialized)
    - src/components/ui/tooltip.tsx exists (shadcn tooltip component)
    - src/lib/utils.ts exports `cn` function
    - package.json `react` version starts with `18` (NOT `19`)
    - tailwind.config.js content array includes `./src/**/*.{js,ts,jsx,tsx}`
    - src/index.css contains `--canvas-width: 1080px`
    - src/Editor/EditorShell.tsx contains `bg-[#111111]`
    - src/Editor/Toolbar.tsx contains `bg-[#1c1c1c]` and `border-[#2a2a2a]`
    - src/Editor/Sidebar.tsx contains `bg-[#1c1c1c]` and `border-[#2a2a2a]`
    - src/Editor/CanvasArea.tsx imports CanvasWrapper from '../Grid/CanvasWrapper'
    - All existing 79 tests still pass
  </acceptance_criteria>
  <done>LeafNode type extended with objectPosition, shadcn initialized with Tooltip available, dark theme applied to editor shell (EditorShell, Toolbar, Sidebar, CanvasArea), React 18 and Tailwind v3 config preserved, all existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: CanvasWrapper + SafeZoneOverlay + Wave 0 test scaffolds</name>
  <files>src/Grid/CanvasWrapper.tsx, src/Grid/SafeZoneOverlay.tsx, src/Grid/index.ts, src/test/grid-rendering.test.tsx, src/test/divider.test.tsx, src/test/canvas-wrapper.test.tsx</files>
  <read_first>src/store/editorStore.ts, src/store/gridStore.ts, src/index.css, src/Grid/index.ts, src/Editor/CanvasArea.tsx, .planning/phases/02-grid-rendering/02-UI-SPEC.md, .planning/phases/02-grid-rendering/02-VALIDATION.md</read_first>
  <action>
**Step 1 — CanvasWrapper (REND-07, D-08, D-09, D-10):**
Create `src/Grid/CanvasWrapper.tsx`:

```typescript
import React, { useRef, useState, useEffect, useCallback } from 'react';
import { useEditorStore } from '../store/editorStore';
import { useGridStore } from '../store/gridStore';
import { SafeZoneOverlay } from './SafeZoneOverlay';
import { GridNodeComponent } from './GridNode';

const CANVAS_W = 1080;
const CANVAS_H = 1920;

function debounce<T extends (...args: unknown[]) => void>(fn: T, ms: number): T {
  let timer: ReturnType<typeof setTimeout>;
  return ((...args: unknown[]) => {
    clearTimeout(timer);
    timer = setTimeout(() => fn(...args), ms);
  }) as T;
}

export const CanvasWrapper = React.memo(function CanvasWrapper() {
  const containerRef = useRef<HTMLDivElement>(null);
  const [autoFitScale, setAutoFitScale] = useState(1);
  const zoom = useEditorStore(s => s.zoom);
  const showSafeZone = useEditorStore(s => s.showSafeZone);
  const rootId = useGridStore(s => s.root.id);
  const setSelectedNode = useEditorStore(s => s.setSelectedNode);

  useEffect(() => {
    const el = containerRef.current;
    if (!el) return;
    const handleResize = debounce((entries: ResizeObserverEntry[]) => {
      const { height, width } = entries[0].contentRect;
      const scaleByH = (height * 0.9) / CANVAS_H;
      const scaleByW = (width * 0.9) / CANVAS_W;
      setAutoFitScale(Math.min(scaleByH, scaleByW));
    }, 50);
    const ro = new ResizeObserver(handleResize);
    ro.observe(el);
    return () => ro.disconnect();
  }, []);

  const finalScale = autoFitScale * zoom;

  const handleBgClick = useCallback((e: React.MouseEvent<HTMLDivElement>) => {
    // Only deselect if the click target is the wrapper itself (D-12)
    if (e.target === e.currentTarget) {
      setSelectedNode(null);
    }
  }, [setSelectedNode]);

  return (
    <div
      ref={containerRef}
      className="flex flex-1 h-full items-start justify-center overflow-hidden"
      data-testid="canvas-container"
    >
      <div
        className="relative group mt-8 flex-shrink-0"
        style={{
          width: CANVAS_W,
          height: CANVAS_H,
          transform: `scale(${finalScale})`,
          transformOrigin: 'top center',
        }}
        onClick={handleBgClick}
        data-testid="canvas-surface"
      >
        <GridNodeComponent id={rootId} />
        {showSafeZone && <SafeZoneOverlay />}
      </div>
    </div>
  );
});
```

Key details:
- The outer `div` (containerRef) is what ResizeObserver observes. It has stable dimensions (flex-1, h-full).
- The inner `div` has literal 1080x1920 dimensions and `transform: scale()` applied. Per Pitfall 2, the transform does not affect layout box so ResizeObserver does not loop.
- Scale formula uses `Math.min(scaleByH, scaleByW)` for robustness on wide viewports (research recommendation).
- The inner div has `group` class for Tailwind group-hover pattern (D-01 divider visibility).
- `mt-8` provides top padding so the canvas doesn't touch the toolbar.
- `onClick` on the inner div handles deselect-on-background-click (D-12) — only fires when click target is the div itself, not a child.
- `rootId` subscription is a primitive (string) for stable memo identity.

**Step 2 — SafeZoneOverlay (REND-08):**
Create `src/Grid/SafeZoneOverlay.tsx`:

```typescript
export function SafeZoneOverlay() {
  return (
    <div className="absolute inset-0 pointer-events-none" data-testid="safe-zone-overlay">
      <div
        className="absolute inset-x-0 border-t border-dashed border-white/20"
        style={{ top: 'var(--safe-zone-top)' }}
      />
      <div
        className="absolute inset-x-0 border-b border-dashed border-white/20"
        style={{ bottom: 'var(--safe-zone-bottom)' }}
      />
    </div>
  );
}
```

**Step 3 — Update `src/Grid/index.ts`:**
```typescript
export { CanvasWrapper } from './CanvasWrapper';
export { SafeZoneOverlay } from './SafeZoneOverlay';
// GridNodeComponent, ContainerNode, LeafNode, Divider, ActionBar added in Plan 02
```

Note: CanvasWrapper imports GridNodeComponent from `./GridNode` which does not exist yet. Create a placeholder `src/Grid/GridNode.tsx`:
```typescript
import React from 'react';

export const GridNodeComponent = React.memo(function GridNodeComponent({ id }: { id: string }) {
  return <div data-testid={`grid-node-${id}`} className="w-full h-full bg-[#1c1c1c]" />;
});
```
This placeholder renders a dark surface. Plan 02 replaces it with the real dispatcher.

**Step 4 — Wave 0 test scaffolds (from VALIDATION.md):**

Create `src/test/grid-rendering.test.tsx`:
```typescript
import { describe, it, expect } from 'vitest';

describe('GridNode dispatcher (REND-01)', () => {
  it.todo('renders ContainerNode for container type nodes');
  it.todo('renders LeafNode for leaf type nodes');
  it.todo('is wrapped in React.memo');
});

describe('ContainerNode flex layout (REND-02)', () => {
  it.todo('renders flex-row for horizontal direction');
  it.todo('renders flex-col for vertical direction');
  it.todo('applies flex: {size} to each child wrapper');
  it.todo('renders Divider between each pair of siblings');
});

describe('LeafNode empty state (REND-04)', () => {
  it.todo('renders dashed border with upload prompt text');
  it.todo('renders upload icon centered in cell');
  it.todo('shows "Drop image or click to upload" text');
});

describe('LeafNode media state (REND-05)', () => {
  it.todo('renders img element with object-fit: cover by default');
  it.todo('renders img element with object-fit: contain when leaf.fit is contain');
  it.todo('applies objectPosition from leaf.objectPosition');
});

describe('LeafNode selection and action bar (REND-06)', () => {
  it.todo('shows blue ring-2 border when selected');
  it.todo('shows no selection border when not selected');
  it.todo('reveals action bar on hover');
  it.todo('action bar has Split H, Split V, Remove, Toggle Fit buttons');
});

describe('SafeZoneOverlay (REND-08)', () => {
  it.todo('renders dashed lines at safe zone positions');
  it.todo('has pointer-events: none so clicks pass through');
  it.todo('is only rendered when showSafeZone is true');
});

describe('Per-node memo and selectors (REND-09)', () => {
  it.todo('GridNodeComponent is React.memo wrapped');
  it.todo('ContainerNode is React.memo wrapped');
  it.todo('LeafNode is React.memo wrapped');
  it.todo('LeafNode subscribes only to its own node slice');
});

describe('Safari isolation fix (REND-10)', () => {
  it.todo('LeafNode wrapper has isolation: isolate (Tailwind isolate class)');
});
```

Create `src/test/divider.test.tsx`:
```typescript
import { describe, it, expect } from 'vitest';

describe('Divider drag interaction (REND-03)', () => {
  it.todo('renders with 8px hit area and 2px visible line');
  it.todo('calls setPointerCapture on pointerdown');
  it.todo('updates local sizes during pointermove (no store write)');
  it.todo('commits resize to store on pointerup');
  it.todo('shows grab handle on hover');
  it.todo('uses col-resize cursor for vertical containers');
  it.todo('uses row-resize cursor for horizontal containers');
});
```

Create `src/test/canvas-wrapper.test.tsx`:
```typescript
import { describe, it, expect } from 'vitest';

describe('CanvasWrapper scaling (REND-07)', () => {
  it.todo('renders a 1080x1920 inner div');
  it.todo('applies transform: scale() based on container size');
  it.todo('uses Math.min(scaleByH, scaleByW) for scale calculation');
  it.todo('multiplies autoFitScale by zoom from editorStore');
  it.todo('uses transform-origin: top center');
  it.todo('deselects node when clicking canvas background');
});
```
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/Grid/CanvasWrapper.tsx exists and exports CanvasWrapper
    - src/Grid/CanvasWrapper.tsx contains `ResizeObserver`
    - src/Grid/CanvasWrapper.tsx contains `Math.min(scaleByH, scaleByW)`
    - src/Grid/CanvasWrapper.tsx contains `transform: scale`
    - src/Grid/CanvasWrapper.tsx contains `transformOrigin: 'top center'`
    - src/Grid/CanvasWrapper.tsx contains `data-testid="canvas-surface"`
    - src/Grid/SafeZoneOverlay.tsx exists and contains `pointer-events-none`
    - src/Grid/SafeZoneOverlay.tsx contains `var(--safe-zone-top)`
    - src/Grid/GridNode.tsx exists (placeholder for Plan 02)
    - src/Grid/index.ts exports CanvasWrapper and SafeZoneOverlay
    - src/test/grid-rendering.test.tsx exists with todo tests for REND-01,02,04,05,06,08,09,10
    - src/test/divider.test.tsx exists with todo tests for REND-03
    - src/test/canvas-wrapper.test.tsx exists with todo tests for REND-07
    - All existing 79 tests still pass (todo tests are skipped by Vitest)
    - `npm run dev` starts without errors
  </acceptance_criteria>
  <done>CanvasWrapper renders a scaled 1080x1920 canvas surface with ResizeObserver auto-fit and zoom multiplier. SafeZoneOverlay shows dashed guides when toggled. GridNode placeholder renders a dark surface. All Wave 0 test scaffold files created with todo stubs for every REND requirement. Existing tests still pass.</done>
</task>

</tasks>

<verification>
1. `npx vitest run` — all 79 existing tests pass, new todo tests are skipped
2. `npm run dev` — editor shows dark theme shell with scaled canvas surface
3. `grep 'objectPosition' src/types/index.ts` — field exists
4. `test -f components.json` — shadcn initialized
5. `test -f src/components/ui/tooltip.tsx` — Tooltip component available
6. `grep '"react":' package.json` — version starts with "18" or "^18"
</verification>

<success_criteria>
- Dark-themed editor shell visible in browser
- 1080x1920 canvas surface rendered and auto-scaled to viewport
- Safe zone overlay toggleable via editorStore.showSafeZone
- LeafNode type includes objectPosition?: string
- shadcn initialized, Tooltip component importable
- All 79 existing tests pass; 3 new test scaffold files exist with todo stubs
- React 18 and Tailwind v3 config intact after shadcn init
</success_criteria>

<output>
After completion, create `.planning/phases/02-grid-rendering/02-01-SUMMARY.md`
</output>

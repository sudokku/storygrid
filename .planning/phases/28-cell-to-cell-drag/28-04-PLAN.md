---
phase: 28-cell-to-cell-drag
plan: 04
type: execute
wave: 2
depends_on:
  - 28-01
  - 28-02
files_modified:
  - src/dnd/useCellDropTarget.ts
autonomous: true
requirements:
  - DROP-01
  - DROP-04
  - DROP-07
must_haves:
  truths:
    - "useCellDropTarget(leafId) returns { isOver, setNodeRef } using @dnd-kit/core useDroppable"
    - "Return shape matches Phase 27's locked UseCellDropTargetResult type exactly"
    - "Hook does NOT attach a document-level pointermove listener (Pitfall 2 single-pointer-source rule)"
    - "Hook does NOT call computeDropZone — zone resolution happens in adapter onDragOver (Plan 07)"
    - "Hook no longer throws — Phase 27 stub replaced"
  artifacts:
    - path: "src/dnd/useCellDropTarget.ts"
      provides: "Live useCellDropTarget hook body — Phase 27 skeleton filled"
      contains: "useDroppable"
  key_links:
    - from: "src/dnd/useCellDropTarget.ts"
      to: "@dnd-kit/core"
      via: "useDroppable import"
      pattern: "from '@dnd-kit/core'"
---

<objective>
Fill the body of `useCellDropTarget` using `useDroppable` from `@dnd-kit/core`. Preserve the locked `UseCellDropTargetResult` return-type and the Pitfall 2 header block verbatim. Zone computation (`computeDropZone`) intentionally lives in the adapter's `onDragOver` callback (Plan 07), NOT here — that keeps a single source of pointer truth.

Purpose: LeafNode (Plan 08) consumes this hook; integration tests (Plan 10) mount it in a DndContext harness.
Output: Working hook that returns dnd-kit's droppable props; no throw; no parallel pointer listener.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/28-cell-to-cell-drag/28-CONTEXT.md
@.planning/phases/28-cell-to-cell-drag/28-PATTERNS.md
@.planning/research/PITFALLS.md

@src/dnd/useCellDropTarget.ts
@src/dnd/dragStore.ts
@src/dnd/computeDropZone.ts
@node_modules/@dnd-kit/core/dist/hooks/useDroppable.d.ts

<interfaces>
From `src/dnd/useCellDropTarget.ts` (Phase 27 — locked return type):
```typescript
export type UseCellDropTargetResult = {
  isOver: boolean;
  setNodeRef: (node: HTMLElement | null) => void;
};
```

From `@dnd-kit/core`'s `useDroppable` (reference):
```typescript
function useDroppable(args: { id: UniqueIdentifier; data?: object; disabled?: boolean }): {
  active: ...;
  isOver: boolean;
  node: ...;
  over: ...;
  rect: ...;
  setNodeRef: (node: HTMLElement | null) => void;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement useCellDropTarget body using useDroppable from @dnd-kit/core</name>
  <files>src/dnd/useCellDropTarget.ts</files>

  <read_first>
    - `src/dnd/useCellDropTarget.ts` (full file — Pitfall 2 header lines 1-15 is LOCKED verbatim; only replace body at line 22-24)
    - `.planning/phases/28-cell-to-cell-drag/28-CONTEXT.md` §A3 D-12 (conditional zone render — adapter writes overId), D-16 (getBoundingClientRect in adapter), §A5 Pitfall 2 (single pointer source)
    - `.planning/phases/28-cell-to-cell-drag/28-PATTERNS.md` §`src/dnd/useCellDropTarget.ts (hook)` (body pattern at lines ~156-166)
    - `.planning/research/PITFALLS.md` Pitfall 2 (two parallel pointer sources = root cause of Phase 25 zone flakiness)
    - `node_modules/@dnd-kit/core/dist/hooks/useDroppable.d.ts` (confirm return shape)
    - `src/dnd/computeDropZone.ts` (read header — this hook does NOT call computeDropZone; adapter does in Plan 07)
  </read_first>

  <action>
    Replace the body of `useCellDropTarget(_leafId: string)` at `src/dnd/useCellDropTarget.ts` line ~22-24. PRESERVE the 15-line header block (Pitfall 2 single-source-of-truth contract — Phase 27 verification asserts this).

    1. **Add import at top of file (below header, above `UseCellDropTargetResult` type):**
       ```typescript
       import { useDroppable } from '@dnd-kit/core';
       ```

    2. **Replace the function body** — currently `throw new Error('useCellDropTarget: implementation lands in Phase 28');`:
       ```typescript
       export function useCellDropTarget(leafId: string): UseCellDropTargetResult {
         const { setNodeRef, isOver } = useDroppable({
           id: leafId,
           data: { nodeId: leafId },
         });
         return { setNodeRef, isOver };
       }
       ```
       Notes:
       - Removed the leading underscore from `_leafId` — we USE the parameter now.
       - `data: { nodeId: leafId }` mirrors the legacy Phase 25 call at `LeafNode.tsx:321` so `over.data.current.nodeId` is readable in adapter handlers if needed (belt-and-braces — the adapter primarily uses `over.id`).

    3. **DO NOT add:**
       - A call to `computeDropZone(...)` — zone resolution happens in Plan 07's adapter `onDragOver` callback (single source of truth per Pitfall 2).
       - A `dragStore.setOver(leafId, zone)` call — also adapter-owned in Plan 07.
       - A `document.addEventListener('pointermove', ...)` or equivalent — this is EXACTLY the Phase 25 Pitfall 2 root cause (frame-stale coords from two pointer sources). The header block lines 8-14 document this; do not violate it.
       - A `getBoundingClientRect()` read inside this hook — the adapter reads it per-event with fresh coords (Plan 07).

    4. **PRESERVED invariants:**
       - Header block lines 1-15 — byte-identical.
       - Return type `UseCellDropTargetResult` at lines 17-20 — unchanged.

    5. Task-level TDD context: Plan 10 will write `src/dnd/__tests__/useCellDropTarget.test.tsx` with a rect-mock helper (per D-30 + PATTERNS §Tests). This plan unblocks that — do NOT pre-write the test here.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
    Also run these grep checks (all must succeed):
    - `grep -q "import { useDroppable } from '@dnd-kit/core'" src/dnd/useCellDropTarget.ts`
    - `grep -q "useDroppable({" src/dnd/useCellDropTarget.ts`
    - `grep -q "id: leafId" src/dnd/useCellDropTarget.ts`
    - `grep -q "data: { nodeId: leafId }" src/dnd/useCellDropTarget.ts`
    - `! grep -q "throw new Error" src/dnd/useCellDropTarget.ts`
    - `! grep -q "document.addEventListener" src/dnd/useCellDropTarget.ts` (Pitfall 2 invariant)
    - `! grep -q "computeDropZone" src/dnd/useCellDropTarget.ts` (adapter-owned)
    - `! grep -q "getBoundingClientRect" src/dnd/useCellDropTarget.ts` (adapter reads per-event)
    - `grep -q "PITFALLS.md Pitfall 2" src/dnd/useCellDropTarget.ts` (header preserved)
  </verify>

  <acceptance_criteria>
    - File imports `useDroppable` from `@dnd-kit/core`
    - File's body uses `useDroppable({ id: leafId, data: { nodeId: leafId } })`
    - File does NOT throw
    - File does NOT call `computeDropZone`, `getBoundingClientRect`, or `document.addEventListener`
    - Pitfall 2 header block (single-source-of-truth reminder) preserved verbatim
    - `npx tsc --noEmit` exits 0
    - Existing test suites still pass: `npm run test -- --run` shows no NEW regressions
  </acceptance_criteria>

  <done>Hook body compiles; returns dnd-kit's useDroppable output; no parallel pointer source; zone logic stays in adapter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `leafId: string` argument → dnd-kit id | Same as Plan 03 — internal nanoid, non-user-input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-09 | I | Reintroducing document-level pointermove would create parallel event source | mitigate | Acceptance criterion: `! grep -q document.addEventListener`. Header block documents Pitfall 2. Plan 04 source-readback test in future regression suite would enforce. |
| T-28-10 | D | Repeated useDroppable mount/unmount on every re-render | mitigate | LeafNode is `React.memo`; useDroppable internally memoized by dnd-kit. |
</threat_model>

<verification>
- `npx tsc --noEmit` — clean
- All grep acceptance criteria pass
</verification>

<success_criteria>
- `useCellDropTarget(leafId)` returns functional dnd-kit droppable props.
- No parallel pointer listener; no zone math; adapter remains single source of truth.
</success_criteria>

<output>
After completion, create `.planning/phases/28-cell-to-cell-drag/28-04-SUMMARY.md`
</output>

---
phase: 05.1-mobile-first-ui
plan: 03
subsystem: testing, documentation
tags: [gap-closure, react-import, adr, d-16, cell-swap]
depends_on:
  requires: [05.1-01, 05.1-02]
  provides: [passing Phase 05.1 test suite, D-16 architectural decision record]
  affects: [src/test, src/Grid/LeafNode.tsx, .planning/decisions]
tech_stack:
  added: []
  patterns: [React import in test files, ADR in .planning/decisions/]
key_files:
  created:
    - .planning/decisions/adr-cell-swap-touch.md
  modified:
    - src/test/phase05-p03-responsive.test.tsx
    - src/test/toolbar.test.tsx
    - src/test/sidebar.test.tsx
    - src/test/editor-shell.test.tsx
    - src/test/phase05-p03-onboarding.test.tsx
    - src/test/phase05-p03-dark-theme.test.tsx
    - src/Grid/LeafNode.tsx
decisions:
  - "React import added to all src/test/*.tsx files — fixes ReferenceError: React is not defined across Phase 05.1 tests"
  - "D-16 (cell swap touch) formally deferred: native HTML5 drag events do not fire on iOS/Android; dnd-kit TouchSensor would require non-trivial refactor"
metrics:
  duration: 8min
  completed: 2026-04-03
  tasks_completed: 2
  files_modified: 7
---

# Phase 05.1 Plan 03: Gap Closure — React Import Fix + D-16 ADR Summary

**One-liner:** React import added to 6 test files enabling 5 Phase 05.1 tests to pass; D-16 cell swap touch deferral documented in LeafNode.tsx comment and formal ADR.

## What Was Built

### Task 1: Fix React import in src/test/*.tsx

All `src/test/*.tsx` files that render JSX but lacked `import React from 'react'` were updated:

- `src/test/phase05-p03-responsive.test.tsx` — primary target; 5 new Phase 05.1 tests were failing with `ReferenceError: React is not defined`
- `src/test/toolbar.test.tsx` — same pre-existing gap
- `src/test/sidebar.test.tsx` — same pre-existing gap
- `src/test/editor-shell.test.tsx` — same pre-existing gap
- `src/test/phase05-p03-onboarding.test.tsx` — same pre-existing gap
- `src/test/phase05-p03-dark-theme.test.tsx` — same pre-existing gap

Result: Full test suite — 30 test files, 365 tests pass, 2 skipped, 0 failures.

### Task 2: Document D-16 deferral

**Code comment in `src/Grid/LeafNode.tsx`** (above `handleDrop` cell swap logic):

```typescript
// D-16 (MVP scope): Cell swap uses native HTML5 drag events (dataTransfer text/cell-id).
// Native HTML5 drag events do not fire on iOS/Android touch — cell swap is desktop-only.
// To support touch, a DndContext with useSensor(TouchSensor) would need to wrap the grid
// and the drag-handle approach would need to switch from native ondragstart to useDraggable.
// Deferred to post-MVP. See .planning/decisions/adr-cell-swap-touch.md
```

**ADR at `.planning/decisions/adr-cell-swap-touch.md`** (52 lines): covers context (why CONTEXT.md assumption was wrong), decision (desktop-only for v1), rationale (native drag vs dnd-kit TouchSensor), consequences, and future implementation path.

## Verification

- `npx vitest run src/test/phase05-p03-responsive.test.tsx` — 5/5 pass
- `npx vitest run` — 365 pass, 0 fail, 2 skip across 30 files
- `grep "D-16" src/Grid/LeafNode.tsx` — comment present at line 262
- `test -f .planning/decisions/adr-cell-swap-touch.md` — ADR exists, 52 lines

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing critical functionality] React import added to 5 additional src/test/*.tsx files**
- **Found during:** Task 1 — plan specified checking for the same issue in other files
- **Issue:** 5 additional test files (toolbar, sidebar, editor-shell, onboarding, dark-theme) also lacked `import React from 'react'` and rendered JSX
- **Fix:** Added `import React from 'react'` as first line of each file
- **Files modified:** toolbar.test.tsx, sidebar.test.tsx, editor-shell.test.tsx, phase05-p03-onboarding.test.tsx, phase05-p03-dark-theme.test.tsx
- **Commit:** e7285b7

This was explicitly described in the plan's action section ("check these other files") — not truly a deviation.

## Known Stubs

None. This plan makes no functional UI changes. All changes are test infrastructure and documentation.

## Commits

| Task | Hash | Message |
|------|------|---------|
| Task 1 | e7285b7 | fix(05.1-03): add missing React import to src/test/*.tsx files |
| Task 2 | 294c25b | docs(05.1-03): document D-16 cell swap touch deferral in code and ADR |

## Self-Check: PASSED

- src/test/phase05-p03-responsive.test.tsx — FOUND
- src/Grid/LeafNode.tsx — FOUND
- .planning/decisions/adr-cell-swap-touch.md — FOUND
- .planning/phases/05.1-mobile-first-ui/05.1-03-SUMMARY.md — FOUND
- Commit e7285b7 — FOUND
- Commit 294c25b — FOUND

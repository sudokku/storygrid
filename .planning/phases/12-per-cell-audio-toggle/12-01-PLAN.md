---
phase: 12-per-cell-audio-toggle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/index.ts
  - src/lib/tree.ts
  - src/store/gridStore.ts
  - src/store/gridStore.test.ts
  - src/test/tree-functions.test.ts
  - src/test/action-bar.test.tsx
  - src/Grid/__tests__/ActionBar.test.tsx
  - src/test/sidebar.test.tsx
autonomous: true
requirements: [AUD-01, AUD-08, AUD-09]
must_haves:
  truths:
    - "LeafNode type requires audioEnabled: boolean (not optional)"
    - "createLeaf() returns a node with audioEnabled: true"
    - "toggleAudioEnabled(nodeId) flips audioEnabled and pushes exactly one history snapshot"
    - "Every existing test fixture that constructs a LeafNode includes audioEnabled"
    - "npm run build and vitest are green after the type change"
  artifacts:
    - path: "src/types/index.ts"
      provides: "LeafNode with required audioEnabled: boolean"
      contains: "audioEnabled: boolean"
    - path: "src/lib/tree.ts"
      provides: "createLeaf() default audioEnabled: true"
      contains: "audioEnabled: true"
    - path: "src/store/gridStore.ts"
      provides: "toggleAudioEnabled action"
      contains: "toggleAudioEnabled"
    - path: "src/store/gridStore.test.ts"
      provides: "toggleAudioEnabled describe block"
      contains: "toggleAudioEnabled"
  key_links:
    - from: "src/store/gridStore.ts"
      to: "src/lib/tree.ts updateLeaf"
      via: "updateLeaf(current(state.root), nodeId, { audioEnabled: !leaf.audioEnabled })"
      pattern: "updateLeaf.*audioEnabled"
    - from: "src/store/gridStore.ts"
      to: "pushSnapshot"
      via: "single snapshot per toggle"
      pattern: "pushSnapshot\\(state\\)"
---

<objective>
Foundation for Phase 12: add required `audioEnabled: boolean` field to `LeafNode`, default it to `true` in `createLeaf()`, add a `toggleAudioEnabled(nodeId)` store action that flips the field with one history snapshot, and migrate all existing test fixtures so the TypeScript build stays green. This plan also creates the Wave 0 test scaffolds for Plan 02 and Plan 03 to consume.

Purpose: All downstream Phase 12 work (UI toggles in ActionBar/Sidebar, Web Audio graph in export) reads from this single field and this single action. Consolidating the data model + store + fixture migration into one plan eliminates the scavenger hunt and leaves Plans 02 and 03 free to run in parallel against stable contracts.

Output: Typed `LeafNode` with `audioEnabled`, working `toggleAudioEnabled` action with test coverage, migrated fixtures, TypeScript build green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-per-cell-audio-toggle/12-CONTEXT.md
@.planning/phases/12-per-cell-audio-toggle/12-RESEARCH.md

@src/types/index.ts
@src/lib/tree.ts
@src/store/gridStore.ts
@src/store/gridStore.test.ts

<interfaces>
Key contracts this plan creates (downstream plans consume these):

```typescript
// src/types/index.ts — updated LeafNode
export type LeafNode = {
  type: 'leaf';
  id: string;
  mediaId: string | null;
  fit: 'cover' | 'contain';
  objectPosition?: string;
  backgroundColor: string | null;
  panX: number;
  panY: number;
  panScale: number;
  effects: EffectSettings;
  audioEnabled: boolean; // NEW — required, default true
};

// src/store/gridStore.ts — new action on GridStoreState
toggleAudioEnabled: (nodeId: string) => void;
```

Reference pattern from `src/store/gridStore.ts` (existing `updateCell`):
```typescript
updateCell: (nodeId, updates) =>
  set(state => {
    pushSnapshot(state);
    state.root = updateLeaf(current(state.root), nodeId, updates);
  }),
```

Phase 11 precedent for required-field migration: `effects: EffectSettings` was added as a required field in the same shape. The commit introducing it modified `src/types/index.ts`, `src/lib/tree.ts` (createLeaf), `src/store/gridStore.ts`, and ~25 test fixture files. Follow the same sequence.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add audioEnabled field to LeafNode type, createLeaf factory, and toggleAudioEnabled store action (with unit tests)</name>
  <files>src/types/index.ts, src/lib/tree.ts, src/store/gridStore.ts, src/store/gridStore.test.ts, src/test/tree-functions.test.ts</files>
  <read_first>
    - src/types/index.ts (current LeafNode definition — find the `type LeafNode = { ... }` block)
    - src/lib/tree.ts (createLeaf factory around line 94, specifically the `effects: { ...DEFAULT_EFFECTS }` line that is the insertion point)
    - src/store/gridStore.ts (updateCell action around line 211, setEffects / commitEffects patterns, the GridStoreState type, findNode import, updateLeaf import, pushSnapshot helper)
    - src/store/gridStore.test.ts (existing describe blocks for updateCell / setEffects — mirror their structure)
    - src/test/tree-functions.test.ts (existing createLeaf tests — mirror for audioEnabled default)
    - .planning/phases/12-per-cell-audio-toggle/12-CONTEXT.md (decisions D-01, D-02, D-03, D-24, D-25, D-26)
    - .planning/phases/12-per-cell-audio-toggle/12-RESEARCH.md §Pattern 1 and §Pattern 2 (exact code shape)
  </read_first>
  <behavior>
    - Test 1 (tree-functions.test.ts): `createLeaf()` returns an object with `audioEnabled === true`.
    - Test 2 (gridStore.test.ts): Calling `toggleAudioEnabled(nodeId)` on a leaf with `audioEnabled: true` flips it to `false`.
    - Test 3 (gridStore.test.ts): Calling `toggleAudioEnabled(nodeId)` twice returns the leaf to its original audioEnabled state.
    - Test 4 (gridStore.test.ts): `toggleAudioEnabled(nodeId)` increases `history.length` by exactly 1 (pushes one snapshot).
    - Test 5 (gridStore.test.ts): `toggleAudioEnabled(nodeId)` on a non-existent nodeId is a no-op (no throw, no history change).
    - Test 6 (gridStore.test.ts): After `toggleAudioEnabled(nodeId)`, calling `undo()` restores the previous audioEnabled value (AUD-08 proxy — field survives undo/redo).
  </behavior>
  <action>
    Step 1 — `src/types/index.ts`: Locate the `export type LeafNode = { ... }` block. Add `audioEnabled: boolean;` as a new required field immediately after the `effects: EffectSettings;` line. Do NOT make it optional (no `?`). Do NOT add a default-value comment; the factory is the single init site.

    Step 2 — `src/lib/tree.ts`: Locate `createLeaf()` around line 94. It currently ends with `effects: { ...DEFAULT_EFFECTS },`. Add a new line immediately after:
    ```typescript
    audioEnabled: true,
    ```
    No other changes to `createLeaf()`.

    Step 3 — `src/store/gridStore.ts`:
    (a) Find the `GridStoreState` type (or equivalent `type Store = {...}`) and add:
    ```typescript
    toggleAudioEnabled: (nodeId: string) => void;
    ```
    Place it immediately after `updateCell` in the type definition.

    (b) Find the store implementation (the `create<...>()(immer((set, get) => ({ ... })))` block). Add the new action immediately after `updateCell`:
    ```typescript
    toggleAudioEnabled: (nodeId) =>
      set(state => {
        const leaf = findNode(current(state.root), nodeId);
        if (!leaf || leaf.type !== 'leaf') return;
        pushSnapshot(state);
        state.root = updateLeaf(current(state.root), nodeId, {
          audioEnabled: !leaf.audioEnabled,
        });
      }),
    ```
    Use the already-imported `findNode`, `updateLeaf`, `current` (from immer), and `pushSnapshot` helpers — do NOT add new imports unless one is missing.

    Step 4 — `src/store/gridStore.test.ts`: Add a new `describe('toggleAudioEnabled', () => { ... })` block. Inside:
    - `it('defaults audioEnabled to true for new leaves')` — calls `createLeaf()` (or uses the store's initial root) and asserts `leaf.audioEnabled === true`.
    - `it('flips audioEnabled from true to false')` — constructs a store with a leaf, calls `toggleAudioEnabled(leafId)`, asserts `leaf.audioEnabled === false`.
    - `it('flips audioEnabled back to true on second call')` — two toggles, asserts round-trip.
    - `it('pushes exactly one history snapshot per toggle')` — reads `store.getState().history.length` before, calls `toggleAudioEnabled`, asserts `history.length` is exactly +1.
    - `it('is a no-op for non-existent nodeId')` — reads history length before, calls `toggleAudioEnabled('does-not-exist')`, asserts history length unchanged and no throw.
    - `it('undo restores previous audioEnabled value')` — toggle, call undo, assert audioEnabled is back to true.
    Use the existing test helpers in `gridStore.test.ts` (makeStore, initial-root builders). Do NOT introduce new test utilities.

    Step 5 — `src/test/tree-functions.test.ts`: In the existing `describe('createLeaf', ...)` block (or add one if none exists), add:
    - `it('initializes audioEnabled to true')` — asserts `createLeaf().audioEnabled === true`.

    Per D-01, D-02, D-03, D-24, D-25, D-26. Reference: 12-RESEARCH.md §Pattern 1, §Pattern 2.
  </action>
  <verify>
    <automated>npx vitest run src/store/gridStore.test.ts src/test/tree-functions.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "audioEnabled: boolean" src/types/index.ts` returns 1 match inside the `LeafNode` type
    - `grep -n "audioEnabled: true" src/lib/tree.ts` returns at least 1 match inside `createLeaf`
    - `grep -n "toggleAudioEnabled" src/store/gridStore.ts` returns at least 2 matches (type + implementation)
    - `grep -n "pushSnapshot" src/store/gridStore.ts | wc -l` shows toggleAudioEnabled uses pushSnapshot (the new action contains exactly one `pushSnapshot(state)` call in its body)
    - `grep -n "describe.*toggleAudioEnabled" src/store/gridStore.test.ts` returns 1 match
    - `grep -c "audioEnabled" src/store/gridStore.test.ts` returns at least 6
    - `npx vitest run src/store/gridStore.test.ts -t "toggleAudioEnabled"` exits 0 with 6+ passing tests
    - `npx vitest run src/test/tree-functions.test.ts -t "createLeaf"` exits 0 with the new audioEnabled test passing
  </acceptance_criteria>
  <done>toggleAudioEnabled action exists, has ≥6 passing tests covering flip, round-trip, one-snapshot, no-op, undo-restore, and default value; createLeaf initializes audioEnabled to true with a test; LeafNode type has required audioEnabled field.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all test fixtures to include audioEnabled on every LeafNode literal (restore TypeScript build + test green)</name>
  <files>src/test/action-bar.test.tsx, src/Grid/__tests__/ActionBar.test.tsx, src/test/sidebar.test.tsx, and every other test file where `type: 'leaf'` literals appear</files>
  <read_first>
    - src/test/action-bar.test.tsx (makeLeaf helper around line 19 — primary target)
    - src/Grid/__tests__/ActionBar.test.tsx (fixture construction)
    - src/test/sidebar.test.tsx (fixture construction)
    - .planning/phases/12-per-cell-audio-toggle/12-CONTEXT.md D-28 (fixture migration rule)
    - .planning/phases/12-per-cell-audio-toggle/12-RESEARCH.md §Pitfall 6 (Phase 11 effects precedent)
  </read_first>
  <action>
    Step 1 — Run `grep -rn "type: 'leaf'" src/` to enumerate every file constructing a `LeafNode` literal. Expected count: approximately 25 files per RESEARCH.md §Validation Architecture.

    Step 2 — For EACH file found, locate every object literal with `type: 'leaf'` (both `makeLeaf` helpers and inline literals) and add `audioEnabled: true,` as a new field. Place it immediately after the `effects: { ...DEFAULT_EFFECTS },` line (or after `effects:` whatever shape it takes). If a test file has a `makeLeaf` helper (like `src/test/action-bar.test.tsx:19`), update the helper ONCE and all its callers inherit the default automatically.

    Step 3 — Do NOT modify production code in this task. Only test files and test helpers.

    Step 4 — If any test is specifically about the muted-audio case (for example a test asserting the ActionBar shows VolumeX), use `audioEnabled: false` in that specific fixture. For all other tests, use `audioEnabled: true` (matches the default).

    Step 5 — After all fixtures are updated, run `npx tsc --noEmit` and fix any remaining TypeScript errors by adding `audioEnabled` to LeafNode literals the grep may have missed (e.g., literals that span multiple lines or use unusual formatting).

    Per D-28. Matches Phase 11 effects fixture migration approach.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 with zero errors
    - `grep -rn "type: 'leaf'" src/ | wc -l` returns the original count (no literals were deleted)
    - `grep -rn "audioEnabled" src/test src/Grid/__tests__ src/Editor/__tests__ 2>/dev/null | wc -l` returns a number ≥ the count of fixture files touched (every fixture has the field)
    - `npx vitest run` exits 0 with full suite green
    - No production source files under `src/` were modified except those touched in Task 1
  </acceptance_criteria>
  <done>Every test file constructing a LeafNode literal includes audioEnabled, TypeScript compiles cleanly, full vitest suite passes.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero errors
2. `npx vitest run` — full suite green
3. `grep -n "audioEnabled: boolean" src/types/index.ts` — field exists in LeafNode
4. `grep -n "audioEnabled: true" src/lib/tree.ts` — default in createLeaf
5. `grep -n "toggleAudioEnabled" src/store/gridStore.ts` — action exists
</verification>

<success_criteria>
- LeafNode type has required `audioEnabled: boolean` field (AUD-01)
- `createLeaf()` defaults audioEnabled to true (AUD-01)
- `toggleAudioEnabled(nodeId)` action exists, flips field, pushes one snapshot (foundation for AUD-03)
- Undo/redo preserves audioEnabled changes (AUD-08 data-model side; Phase 14 handles file serialization)
- All ~25 test fixture files include audioEnabled
- `npx tsc --noEmit` and `npx vitest run` both green
- AUD-09 is no-op (browser gate already enforced by existing MediaRecorder check)
</success_criteria>

<output>
After completion, create `.planning/phases/12-per-cell-audio-toggle/12-01-SUMMARY.md` summarizing:
- LeafNode type change and createLeaf default
- toggleAudioEnabled action shape and history behavior
- Fixture migration scope (file count, makeLeaf helpers updated)
- Any unexpected fixture files found outside RESEARCH.md §Validation Architecture estimate
</output>

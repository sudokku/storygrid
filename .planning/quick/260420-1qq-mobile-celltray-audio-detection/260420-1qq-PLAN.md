---
phase: quick
plan: 260420-1qq
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Editor/MobileCellTray.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "After uploading a video via MobileCellTray, hasAudioTrack reflects the actual audio presence"
    - "The Audio button is enabled (not disabled) for videos that have an audio track"
    - "Silent videos leave the Audio button disabled — no false positives"
    - "Audio export produces sound on mobile after a video upload through the tray"
  artifacts:
    - path: "src/Editor/MobileCellTray.tsx"
      provides: "detectAudioTrack call after video upload in handleFileChange"
      contains: "detectAudioTrack"
  key_links:
    - from: "MobileCellTray.handleFileChange"
      to: "gridStore.setHasAudioTrack"
      via: "detectAudioTrack(file) result"
      pattern: "setHasAudioTrack.*nodeId"
---

<objective>
Fix missing audio track detection in MobileCellTray video upload path so hasAudioTrack is
correctly set after mobile uploads, enabling the Audio toggle and producing audio in video exports.

Purpose: Mobile users uploading videos via MobileCellTray get hasAudioTrack=false permanently
because handleFileChange never calls detectAudioTrack. The desktop path (LeafNode.handleFileDrop)
calls detectAudioTrack correctly. This brings the mobile path to parity.

Output: MobileCellTray.tsx with detectAudioTrack call mirroring LeafNode.tsx lines 465–469.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

<interfaces>
<!-- From src/lib/media.ts -->
```typescript
export async function detectAudioTrack(file: File): Promise<boolean>;
```

<!-- From src/store/gridStore.ts -->
```typescript
setHasAudioTrack: (nodeId: string, hasAudio: boolean) => void;
```

<!-- LeafNode.tsx reference pattern (lines 465–469) -->
```typescript
const hasAudio = file.type.startsWith('video/')
  ? await detectAudioTrack(file)
  : false;
setHasAudioTrack(id, hasAudio);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Call detectAudioTrack in MobileCellTray.handleFileChange after video upload</name>
  <files>src/Editor/MobileCellTray.tsx</files>
  <behavior>
    - After setMedia(nodeId, newId), detect audio and update store for video files
    - detectAudioTrack is called only when file.type.startsWith('video/') — not for images
    - setHasAudioTrack(nodeId, result) is called with the detection result for videos
    - setHasAudioTrack(nodeId, false) is called for image uploads
    - setHasAudioTrack is pulled from useGridStore at the top of the component (beside toggleAudioEnabled)
    - detectAudioTrack is imported from '../lib/media' at the top of the file
  </behavior>
  <action>
1. Add `detectAudioTrack` to the existing import from `'../lib/media'` at the top of the file.
   Currently there is no import from '../lib/media' in MobileCellTray.tsx (fileToBase64 is
   dynamically imported inside handleFileChange). Add a static top-level import:
   ```typescript
   import { detectAudioTrack } from '../lib/media';
   ```

2. Destructure `setHasAudioTrack` from `useGridStore` alongside `toggleAudioEnabled` (line ~79).
   Add after `const toggleAudioEnabled = useGridStore(s => s.toggleAudioEnabled);`:
   ```typescript
   const setHasAudioTrack = useGridStore(s => s.setHasAudioTrack);
   ```

3. In `handleFileChange`, after `setMedia(nodeId, newId)` (currently the last statement in the
   callback), add audio detection mirroring LeafNode.tsx lines 465–469:
   ```typescript
   setMedia(nodeId, newId);
   const hasAudio = file.type.startsWith('video/')
     ? await detectAudioTrack(file)
     : false;
   setHasAudioTrack(nodeId, hasAudio);
   ```

4. Add `setHasAudioTrack` to the `useCallback` dependency array for `handleFileChange`
   (currently `[addMedia, setMedia]` → becomes `[addMedia, setMedia, setHasAudioTrack]`).

Do NOT restructure the rest of the function. Do NOT change the video/image branching logic above
the setMedia call. The only addition is the three lines after setMedia.
  </action>
  <verify>
    <automated>cd /Users/radu/Developer/storygrid && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    TypeScript compiles clean. MobileCellTray.handleFileChange calls detectAudioTrack for video
    uploads and setHasAudioTrack for both video and image uploads after setMedia.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user file input → detectAudioTrack | File object from browser file picker; no network crossing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1qq-01 | Denial of Service | detectAudioTrack | accept | detectAudioTrack already has a 5s loadedmetadata timeout per the Phase 19 implementation; no new attack surface |
</threat_model>

<verification>
Manual smoke test on mobile (or Chrome DevTools mobile emulation):
1. Open the app, select an empty cell
2. Tap the Upload button in MobileCellTray
3. Select a video file that has an audio track
4. Verify the Audio button in MobileCellTray becomes enabled (not greyed out)
5. Upload a silent video — Audio button should remain disabled
</verification>

<success_criteria>
- TypeScript build passes with no new errors
- Audio button in MobileCellTray is enabled after uploading a video with audio
- Audio button remains disabled after uploading a silent video
- Video exports from mobile contain audio when Audio toggle is on
</success_criteria>

<output>
After completion, create `.planning/quick/260420-1qq-mobile-celltray-audio-detection/260420-1qq-SUMMARY.md`
</output>

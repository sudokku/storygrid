---
phase: 02-grid-rendering
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/test/canvas-wrapper.test.tsx
  - src/test/grid-rendering.test.tsx
autonomous: false
requirements:
  - REND-07
  - REND-08

must_haves:
  truths:
    - "Canvas scales correctly to fill ~90% of available space"
    - "Safe zone overlay toggles on and off correctly"
    - "Full visual grid experience works end-to-end in the browser"
  artifacts:
    - path: "src/test/canvas-wrapper.test.tsx"
      provides: "Integration tests for REND-07 canvas scaling"
    - path: "src/test/grid-rendering.test.tsx"
      provides: "Complete test coverage for all REND requirements"
  key_links:
    - from: "src/Grid/CanvasWrapper.tsx"
      to: "src/Grid/GridNode.tsx"
      via: "renders GridNodeComponent at root"
      pattern: "<GridNodeComponent"
---

<objective>
Final integration: implement CanvasWrapper and SafeZoneOverlay tests, fill any remaining test gaps, and visually verify the complete grid rendering experience in the browser.

Purpose: Close the loop on all REND requirements with automated tests and a human visual checkpoint confirming the grid looks and interacts correctly.
Output: Complete test coverage for REND-07 and REND-08, visual confirmation of the grid rendering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-grid-rendering/02-CONTEXT.md
@.planning/phases/02-grid-rendering/02-UI-SPEC.md
@.planning/phases/02-grid-rendering/02-01-SUMMARY.md
@.planning/phases/02-grid-rendering/02-02-SUMMARY.md
@src/Grid/CanvasWrapper.tsx
@src/Grid/SafeZoneOverlay.tsx
@src/Grid/GridNode.tsx
@src/Grid/ContainerNode.tsx
@src/Grid/LeafNode.tsx
@src/Grid/Divider.tsx
@src/Grid/ActionBar.tsx
@src/store/editorStore.ts
@src/store/gridStore.ts
@src/test/canvas-wrapper.test.tsx
@src/test/grid-rendering.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: CanvasWrapper integration tests and remaining test gaps</name>
  <files>src/test/canvas-wrapper.test.tsx, src/test/grid-rendering.test.tsx</files>
  <read_first>src/Grid/CanvasWrapper.tsx, src/Grid/SafeZoneOverlay.tsx, src/store/editorStore.ts, src/store/gridStore.ts, src/test/canvas-wrapper.test.tsx, src/test/grid-rendering.test.tsx</read_first>
  <action>
**Step 1 — CanvasWrapper tests (REND-07):**
Replace the todo stubs in `src/test/canvas-wrapper.test.tsx` with real tests.

Since jsdom does not support ResizeObserver natively, mock it:
```typescript
class MockResizeObserver {
  callback: ResizeObserverCallback;
  constructor(cb: ResizeObserverCallback) { this.callback = cb; }
  observe() {
    // Simulate an initial observation with a known size
    this.callback([{
      contentRect: { width: 800, height: 900 } as DOMRectReadOnly,
    }] as ResizeObserverEntry[], this as unknown as ResizeObserver);
  }
  unobserve() {}
  disconnect() {}
}
globalThis.ResizeObserver = MockResizeObserver as unknown as typeof ResizeObserver;
```

Tests to implement:
1. **Inner div has 1080x1920 dimensions:** Render CanvasWrapper, find `data-testid="canvas-surface"`, assert `style.width === '1080px'` or `1080` and `style.height === '1920px'` or `1920`.
2. **Transform scale is applied:** After ResizeObserver fires with `{ width: 800, height: 900 }`, assert the canvas-surface element has a `transform` style containing `scale(`. The expected scale = `Math.min((900 * 0.9) / 1920, (800 * 0.9) / 1080)` = `Math.min(0.421875, 0.6667)` = `0.421875`. With zoom=1, assert transform contains approximately that value.
3. **Transform origin is top center:** Assert canvas-surface element style contains `transformOrigin: 'top center'`.
4. **Zoom multiplier:** Set `useEditorStore.setState({ zoom: 1.5 })`, re-render, assert scale changes (should be ~0.632).
5. **Deselect on background click:** Set `selectedNodeId` to a known leaf id, click the canvas-surface wrapper (not a child), assert `selectedNodeId` is now null.

**Step 2 — SafeZoneOverlay and remaining gap tests in `src/test/grid-rendering.test.tsx`:**
Ensure these tests are real (not todo):
- REND-08: Render the grid with `showSafeZone: true` — assert `safe-zone-overlay` testid is in DOM. Then set `showSafeZone: false` — assert it is NOT in DOM.
- Any remaining todo stubs from the Wave 0 scaffold should be either implemented or removed if covered by a different test.

Run `npx vitest run` and confirm all tests pass.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/test/canvas-wrapper.test.tsx has at least 4 passing (non-todo) tests
    - src/test/canvas-wrapper.test.tsx contains `MockResizeObserver` or similar mock
    - src/test/canvas-wrapper.test.tsx tests scale calculation with Math.min
    - src/test/canvas-wrapper.test.tsx tests zoom multiplier effect
    - src/test/grid-rendering.test.tsx has no remaining `.todo` test stubs for REND-08
    - `npx vitest run` passes ALL tests (zero failures)
    - No test files contain only `.todo` stubs — every scaffold has been either implemented or intentionally removed
  </acceptance_criteria>
  <done>CanvasWrapper integration tests verify scaling formula, zoom multiplier, transform origin, and background click deselect. SafeZoneOverlay test confirms conditional rendering. All REND requirement tests implemented and passing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of grid rendering</name>
  <files>none</files>
  <read_first>src/Grid/CanvasWrapper.tsx, src/Grid/LeafNode.tsx, src/Grid/ActionBar.tsx</read_first>
  <action>
Run `npm run dev` and open the browser. Visually verify the complete grid rendering experience. This is a human verification checkpoint — no code changes needed. The executor should present the verification steps to the user and pause for approval.

Verification steps for the user:
1. **Dark theme:** Editor background is dark (#111111), toolbar and sidebar are darker surfaces (#1c1c1c)
2. **Canvas:** A 9:16 canvas is centered in the editor area, auto-scaled to fit
3. **Initial state:** Two vertically stacked cells with dashed borders and "Drop image or click to upload" text
4. **Dividers:** Hover anywhere on the canvas — a 2px line appears between the two cells. Hover directly on the line — a small pill grab handle appears.
5. **Drag divider:** Click and drag the divider up/down — both cells resize live. Release — sizes commit.
6. **Selection:** Click a cell — blue border appears. Click the same cell again — deselects. Click canvas background — deselects.
7. **Action bar:** Hover a cell — floating icon bar fades in at top-center. Four buttons: Split H, Split V, trash (red), Toggle Fit.
8. **Tooltips:** Hover each action bar button — tooltip appears with label text.
9. **Split:** Click "Split horizontal" — cell splits into two side-by-side cells with a vertical divider. Click "Split vertical" on one of those — it splits into two stacked cells.
10. **Remove:** Click "Remove cell" on a leaf — it disappears and the sibling expands.
11. **Resize browser window:** Canvas re-scales to fit the new viewport size.
  </action>
  <verify>
    <automated>npm run dev -- --host 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - User confirms dark theme is applied
    - User confirms canvas auto-scales to viewport
    - User confirms divider drag works smoothly
    - User confirms cell selection and deselection work
    - User confirms action bar appears on hover with correct buttons
    - User confirms split and remove actions work
  </acceptance_criteria>
  <done>User has visually verified the complete grid rendering system works correctly in the browser. All REND requirements are confirmed working end-to-end.</done>
</task>

</tasks>

<verification>
1. `npx vitest run` — all tests pass (entire test suite)
2. `npx vitest run --coverage` — grid rendering files have meaningful coverage
3. Visual confirmation from human checkpoint
</verification>

<success_criteria>
- All REND-01 through REND-10 requirements have automated test coverage
- Human confirms: grid renders correctly, dividers drag smoothly, selection works, action bar is functional
- Full test suite passes with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/02-grid-rendering/02-03-SUMMARY.md`
</output>

---
phase: 17-data-model-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/types/index.ts
  - src/lib/tree.ts
  - src/test/videoExport-audio.test.ts
  - src/test/canvas-export.test.ts
  - src/test/phase17-has-audio-track.test.ts
autonomous: true
requirements: [MUTE-04]

must_haves:
  truths:
    - "LeafNode has a required hasAudioTrack boolean field"
    - "createLeaf() returns hasAudioTrack: true by default"
    - "Undo/redo correctly restores hasAudioTrack after a snapshot-pushing action"
    - "Defensive read leaf.hasAudioTrack ?? true returns true on legacy objects missing the field"
    - "All existing tests pass with no regressions"
  artifacts:
    - path: "src/types/index.ts"
      provides: "LeafNode with hasAudioTrack: boolean"
      contains: "hasAudioTrack: boolean"
    - path: "src/lib/tree.ts"
      provides: "createLeaf() with hasAudioTrack: true default"
      contains: "hasAudioTrack: true"
    - path: "src/test/phase17-has-audio-track.test.ts"
      provides: "SC1 + SC2 + SC4 tests for MUTE-04"
      min_lines: 30
  key_links:
    - from: "src/types/index.ts"
      to: "src/lib/tree.ts"
      via: "LeafNode type used in createLeaf return type"
      pattern: "hasAudioTrack"
    - from: "src/store/gridStore.ts"
      to: "src/types/index.ts"
      via: "pushSnapshot structuredClone captures all LeafNode fields"
      pattern: "structuredClone"
---

<objective>
Add `hasAudioTrack: boolean` to `LeafNode`, set the default in `createLeaf()`, update both test factories, and write tests proving the field survives undo/redo snapshot round-trips and the `?? true` defensive read pattern works on legacy objects.

Purpose: Deliver the data model foundation that unblocks all v1.3 feature phases (18-21). Phase 19 will add detection logic and store actions; this phase is type + default + test only.
Output: Updated LeafNode type, updated createLeaf(), updated test factories, new phase-17 test file with SC1/SC2/SC4 coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-data-model-foundation/17-CONTEXT.md
@.planning/phases/17-data-model-foundation/17-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/types/index.ts (current LeafNode — hasAudioTrack will be added after audioEnabled):
```typescript
export type LeafNode = {
  type: 'leaf';
  id: string;
  mediaId: string | null;
  fit: 'cover' | 'contain';
  objectPosition?: string;
  backgroundColor: string | null;
  panX: number;
  panY: number;
  panScale: number;
  effects: EffectSettings;
  audioEnabled: boolean;
  // hasAudioTrack: boolean  <-- Phase 17 adds this here
};
```

From src/lib/tree.ts:83 (current createLeaf — add hasAudioTrack: true after audioEnabled):
```typescript
export function createLeaf(): LeafNode {
  return {
    type: 'leaf',
    id: nanoid(),
    mediaId: null,
    fit: 'cover',
    objectPosition: 'center center',
    backgroundColor: null,
    panX: 0,
    panY: 0,
    panScale: 1,
    effects: { ...DEFAULT_EFFECTS },
    audioEnabled: true,
    // hasAudioTrack: true,  <-- add this
  };
}
```

From src/store/gridStore.ts (snapshot mechanism — NO changes needed):
```typescript
// pushSnapshot uses structuredClone on full root tree — automatically captures hasAudioTrack
// undo() restores via structuredClone(plainSnap.root)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for hasAudioTrack (RED phase)</name>
  <files>src/test/phase17-has-audio-track.test.ts</files>
  <read_first>
    - src/types/index.ts (current LeafNode shape — confirm hasAudioTrack absent)
    - src/lib/tree.ts (current createLeaf — lines 83-97)
    - src/store/gridStore.ts (pushSnapshot at ~line 132, undo at ~line 352, split action)
    - src/test/videoExport-audio.test.ts (makeLeaf factory pattern at line 12)
  </read_first>
  <behavior>
    - SC1: createLeaf() returns an object with hasAudioTrack === true
    - SC2: After splitNode + undo(), every leaf in the restored tree has hasAudioTrack === true (not undefined)
    - SC4: A leaf constructed via createLeaf() then mutated with `delete (leaf as any).hasAudioTrack` yields undefined for .hasAudioTrack, but `leaf.hasAudioTrack ?? true` yields true
  </behavior>
  <action>
Create `src/test/phase17-has-audio-track.test.ts` with three test cases covering SC1, SC2, and SC4.

**SC1 test (createLeaf default):**
- Import `createLeaf` from `../../lib/tree`
- Call `createLeaf()`, assert `leaf.hasAudioTrack` is `true`
- Assert `typeof leaf.hasAudioTrack` is `'boolean'`

**SC2 test (undo/redo round-trip — per D-04):**
- Import `useGridStore` from store
- Import `ContainerNode`, `LeafNode` from types
- Reset store: `useGridStore.setState(useGridStore.getInitialState(), true)`
- Get initial root, find first leaf id
- Call `useGridStore.getState().split(leafId, 'horizontal')` (snapshot-pushing action)
- Call `useGridStore.getState().undo()`
- Get restored root, collect all leaves recursively
- Assert each leaf has `hasAudioTrack === true` (not undefined)

**SC4 test (legacy snapshot defensive read — per D-05):**
- Create a leaf via `createLeaf()`
- Cast to `any`, delete `hasAudioTrack`
- Assert `legacy.hasAudioTrack` is `undefined`
- Assert `legacy.hasAudioTrack ?? true` is `true`

Run tests — they MUST FAIL (RED) because `hasAudioTrack` does not exist on `LeafNode` yet. The SC1 test will fail because `createLeaf()` does not include the field. The SC2 test will fail because the field is absent from leaves. Commit the failing tests.

Note: The TypeScript compiler will show errors for `leaf.hasAudioTrack` since the field does not exist yet. Use `(leaf as any).hasAudioTrack` for SC1 and SC2 assertions so the test FILE compiles, but the runtime assertions will still fail (returning undefined !== true).
  </action>
  <verify>
    <automated>cd /Users/radu/Developer/storygrid && npx vitest run src/test/phase17-has-audio-track.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/test/phase17-has-audio-track.test.ts exists with 3 test cases (SC1, SC2, SC4)
    - All 3 tests FAIL (RED phase) because hasAudioTrack does not exist on LeafNode yet
    - The test file compiles without TypeScript errors (uses `as any` casts for pre-existence assertions)
  </acceptance_criteria>
  <done>Three failing tests committed covering SC1 (createLeaf default), SC2 (undo round-trip), SC4 (legacy defensive read)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add hasAudioTrack to LeafNode + createLeaf + test factories (GREEN phase)</name>
  <files>src/types/index.ts, src/lib/tree.ts, src/test/videoExport-audio.test.ts, src/test/canvas-export.test.ts, src/test/phase17-has-audio-track.test.ts</files>
  <read_first>
    - src/types/index.ts (LeafNode type — add field after audioEnabled)
    - src/lib/tree.ts (createLeaf at line 83 — add field after audioEnabled: true)
    - src/test/videoExport-audio.test.ts (makeLeaf factory at line 12 — add hasAudioTrack: true)
    - src/test/canvas-export.test.ts (makeTestLeaf factory at line 13 — add hasAudioTrack: true)
    - src/test/phase17-has-audio-track.test.ts (update SC1/SC2 assertions from `as any` to direct property access)
  </read_first>
  <behavior>
    - After changes: SC1 passes (createLeaf().hasAudioTrack === true)
    - After changes: SC2 passes (undo restores hasAudioTrack on all leaves)
    - After changes: SC4 passes (defensive read on legacy object)
    - After changes: npx tsc --noEmit exits 0 (no compile errors from missing field)
    - After changes: Full test suite passes (no regressions)
  </behavior>
  <action>
**Step 1 — Type definition (per D-01):**
Edit `src/types/index.ts` line 21 — add `hasAudioTrack: boolean;` after `audioEnabled: boolean;` in the `LeafNode` type.

**Step 2 — Factory default (per D-02):**
Edit `src/lib/tree.ts` line 96 — add `hasAudioTrack: true,` after `audioEnabled: true,` in `createLeaf()`.

**Step 3 — Run `npx tsc --noEmit`** to find all compile errors from the new required field. Fix each one:

**Step 4 — Test factory: makeLeaf (per D-03):**
Edit `src/test/videoExport-audio.test.ts` — add `hasAudioTrack: true,` after `audioEnabled: true,` in the `makeLeaf()` factory (around line 12).

**Step 5 — Test factory: makeTestLeaf (per D-03):**
Edit `src/test/canvas-export.test.ts` — add `hasAudioTrack: true,` after `audioEnabled: true,` in the `makeTestLeaf()` factory (around line 13).

**Step 6 — Fix any other tsc errors:** Run `npx tsc --noEmit` again. If any other files have inline LeafNode literals missing the field, add `hasAudioTrack: true` to them. Common locations: test files with manual leaf construction, moveLeafToEdge content copy (check if it constructs a full LeafNode literal — if so, add the field).

**Step 7 — Update phase-17 test file:**
Edit `src/test/phase17-has-audio-track.test.ts` — remove `as any` casts from SC1 and SC2 assertions now that the field exists on the type. SC1 should directly assert `leaf.hasAudioTrack === true`. SC2 should directly assert each restored leaf's `.hasAudioTrack === true`.

**Step 8 — Run all tests:** `npx vitest run` — all tests must pass including the 3 new SC1/SC2/SC4 tests and all existing tests (no regressions).

Commit the GREEN implementation.
  </action>
  <verify>
    <automated>cd /Users/radu/Developer/storygrid && npx tsc --noEmit && npx vitest run 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/types/index.ts LeafNode has `hasAudioTrack: boolean` as a required field after audioEnabled
    - src/lib/tree.ts createLeaf() returns `hasAudioTrack: true`
    - src/test/videoExport-audio.test.ts makeLeaf() includes `hasAudioTrack: true`
    - src/test/canvas-export.test.ts makeTestLeaf() includes `hasAudioTrack: true`
    - `npx tsc --noEmit` exits 0 (zero compile errors)
    - `npx vitest run` — all tests pass, including 3 new phase-17 tests, zero failures
    - SC1: createLeaf().hasAudioTrack === true
    - SC2: after split + undo, restored leaves have hasAudioTrack === true
    - SC4: (legacy as any) with deleted field, ?? true returns true
  </acceptance_criteria>
  <done>hasAudioTrack added to type + factory + both test factories, all 3 phase-17 tests GREEN, full suite GREEN, tsc clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This phase adds an internal boolean field to an in-memory data model. No user input, no network calls, no storage changes.

| Boundary | Description |
|----------|-------------|
| None | Phase 17 is purely internal type + test work with no external surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | LeafNode.hasAudioTrack | accept | Field is in-memory only, no persist layer, no user-facing input vector. Value set programmatically by createLeaf() (Phase 17) and detection logic (Phase 19). No ASVS L1 control applies. |
| T-17-02 | Information Disclosure | Snapshot history | accept | History is ephemeral in-memory array, never transmitted or persisted. hasAudioTrack is a non-sensitive boolean. No ASVS L1 control applies. |

</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0 — no TypeScript errors from the new required field
2. `npx vitest run src/test/phase17-has-audio-track.test.ts` — 3 tests pass (SC1, SC2, SC4)
3. `npx vitest run` — full suite passes with zero regressions
4. `grep -n 'hasAudioTrack' src/types/index.ts` shows the field on LeafNode
5. `grep -n 'hasAudioTrack' src/lib/tree.ts` shows the default in createLeaf()
</verification>

<success_criteria>
- LeafNode type has `hasAudioTrack: boolean` (required, not optional) — per D-01
- createLeaf() returns `hasAudioTrack: true` — per D-02
- Both test factories (makeLeaf, makeTestLeaf) include `hasAudioTrack: true` — per D-03
- SC2 test proves undo restores hasAudioTrack via splitNode + undo pattern — per D-04
- SC4 test proves `?? true` defensive read on legacy object — per D-05
- Full test suite green, tsc clean
- MUTE-04 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/17-data-model-foundation/17-01-SUMMARY.md`
</output>

---
phase: 07-cell-controls-display-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Grid/LeafNode.tsx
  - src/Grid/ActionBar.tsx
autonomous: true
requirements: [CELL-01, CELL-02, CELL-03]
must_haves:
  truths:
    - "ActionBar remains fully visible and clickable when cell width is smaller than the bar"
    - "ActionBar buttons appear at a stable physical size across small and large displays (clamped 28-36px)"
    - "Empty cell ImageIcon scales with viewport via clamp() (not fixed 24px)"
    - "Empty cell label is hidden when cell rendered height < 80px"
    - "Media canvas remains correctly clipped inside the cell boundary even after cell overflow becomes visible"
  artifacts:
    - path: "src/Grid/LeafNode.tsx"
      provides: "Overflow-visible cell root with overflow-hidden canvas wrapper, ResizeObserver-driven isTooSmall state, scaled empty placeholder"
      contains: "overflow-visible"
    - path: "src/Grid/ActionBar.tsx"
      provides: "Clamp-based button and icon sizing"
      contains: "clamp("
  key_links:
    - from: "src/Grid/LeafNode.tsx"
      to: "ResizeObserver"
      via: "divRef contentRect height observation -> isTooSmall state"
      pattern: "ResizeObserver"
    - from: "src/Grid/LeafNode.tsx canvas wrapper"
      to: "canvas element"
      via: "absolute inset-0 overflow-hidden inner div"
      pattern: "overflow-hidden"
---

<objective>
Fix CELL-01 (ActionBar overflow clipping), CELL-02 (px-fixed ActionBar sizing), and CELL-03 (px-fixed empty cell placeholder) by isolating canvas clipping from cell overflow and replacing fixed pixel sizes with clamp()-based responsive sizing.

Purpose: Cell controls must always be reachable at any cell size, and visual elements must scale consistently across displays from 1366px laptops to 4K monitors.
Output: Modified LeafNode.tsx and ActionBar.tsx implementing decisions D-01 through D-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cell-controls-display-polish/07-CONTEXT.md
@.planning/phases/07-cell-controls-display-polish/07-UI-SPEC.md

<interfaces>
<!-- ActionBar.tsx current button class (line 50) -->
const btnClass = 'flex items-center justify-center w-8 h-8 rounded hover:bg-white/10 transition-colors';
<!-- All icon usages: <GripVertical|Upload|SplitSquareHorizontal|SplitSquareVertical|Maximize2|Minimize2|ImageOff|Trash2 size={16} className="text-white" /> -->
<!-- ActionBar component signature: ActionBar({ nodeId, fit, hasMedia, onUploadClick }) -->

<!-- LeafNode.tsx root div (line 522-547) -->
<!--   className: relative w-full h-full isolate overflow-hidden select-none ${ringClass} ${hasMedia ? '' : 'bg-[#1c1c1c]'} -->
<!-- LeafNode.tsx canvas (line 559-563): <canvas ref={canvasRef} className="absolute inset-0 w-full h-full" style={{display: mediaUrl ? 'block' : 'none'}} /> -->
<!-- LeafNode.tsx empty placeholder (line 565-570):
       <div className="flex flex-col items-center justify-center w-full h-full gap-2">
         <ImageIcon size={24} className="text-[#666666]" />
         <span className="text-sm text-[#666666]">Drop image or use Upload button</span>
       </div>
-->
<!-- LeafNode.tsx ActionBar wrapper (line 586-596) — has transform scale(1/canvasScale) — DO NOT remove -->
<!-- divRef is already declared at top of LeafNode component and attached to root div -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ActionBar clamp-based sizing (CELL-02)</name>
  <files>src/Grid/ActionBar.tsx, src/Grid/__tests__/ActionBar.test.tsx</files>
  <read_first>
    - src/Grid/ActionBar.tsx (current button class at line 50, all 7 icon usages with size={16})
    - src/Grid/LeafNode.tsx (lines 586-596: ActionBar wrapper has transform scale(1/canvasScale) — sizing must remain authored at logical scale per D-05)
    - .planning/phases/07-cell-controls-display-polish/07-UI-SPEC.md (Component Inventory > ActionBar.tsx section)
    - src/Grid/__tests__/ActionBar.test.tsx (if exists — patterns for existing tests)
  </read_first>
  <behavior>
    - Test 1: ActionBar root container renders with flex/gap classes preserved
    - Test 2: Each button element has inline style width and height equal to "clamp(28px, 2.2vw, 36px)"
    - Test 3: Each lucide icon receives a size prop equal to a number (computed from clamp midpoint, not 16)
    - Test 4: All 7 buttons (grip, upload, split-h, split-v, fit-toggle, clear-media, remove) still render with correct aria-labels
  </behavior>
  <action>
    Modify src/Grid/ActionBar.tsx to replace the fixed `w-8 h-8` button size with clamp-based sizing per D-04, D-05, D-06.

    Concrete changes:

    1. Define button size constant at top of component body:
       ```ts
       const BTN_SIZE = 'clamp(28px, 2.2vw, 36px)';
       const ICON_SIZE = 16; // logical icon size (kept as number for lucide)
       ```

       Note: Keep ICON_SIZE = 16 as the lucide-react `size` prop value. The button container scales via clamp(), and the icon stays proportional because the button's flex centering keeps it visually centered. Per D-05, sizes are authored at logical (unscaled) canvas units; the parent `scale(1/canvasScale)` transform handles physical pixel stability across zoom.

    2. Change btnClass (line 50) — REMOVE `w-8 h-8`:
       ```ts
       const btnClass = 'flex items-center justify-center rounded hover:bg-white/10 transition-colors';
       ```

    3. Apply inline style to EVERY button element (all 7 buttons via TooltipTrigger render prop). Add `style={{ width: BTN_SIZE, height: BTN_SIZE }}` to each `<button>` rendered inside `TooltipTrigger render={...}`. Buttons to update:
       - grip button (line 67-72) — but this one is rendered directly, not via TooltipTrigger; add style there too
       - upload button (line 77)
       - split-h button (line 84)
       - split-v button (line 91)
       - fit-toggle button (line 98)
       - clear-media button (line 111)
       - remove button (line 119)

    4. Do NOT change icon sizes (size={16}). Do NOT remove the `scale(1/canvasScale)` transform in LeafNode.tsx — that transform must remain to keep physical sizes stable across zoom (D-05).

    5. Add tests in src/Grid/__tests__/ActionBar.test.tsx covering the 4 behaviors above. If file does not exist, create it. Use existing test patterns from sibling tests (CanvasArea, LeafNode).

    Implements: CELL-02 per D-04, D-05, D-06.
  </action>
  <verify>
    <automated>npm test -- ActionBar.test 2>&1 | tail -20 &amp;&amp; grep -c "clamp(28px, 2.2vw, 36px)" src/Grid/ActionBar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "clamp(28px, 2.2vw, 36px)" src/Grid/ActionBar.tsx` returns >= 7 (one per button) OR a single `BTN_SIZE` constant referenced 7 times — verify with `grep -c "width: BTN_SIZE" src/Grid/ActionBar.tsx` returns 7
    - `grep "w-8 h-8" src/Grid/ActionBar.tsx` returns no matches
    - `grep "BTN_SIZE = 'clamp(28px, 2.2vw, 36px)'" src/Grid/ActionBar.tsx` returns 1 match
    - `npm test -- ActionBar.test` exits 0 with all 4 behavior tests passing
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    ActionBar buttons sized via clamp(28px, 2.2vw, 36px); fixed w-8 h-8 removed; tests pass; build succeeds.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LeafNode overflow isolation + empty cell scaling (CELL-01, CELL-03)</name>
  <files>src/Grid/LeafNode.tsx, src/Grid/__tests__/LeafNode.test.tsx</files>
  <read_first>
    - src/Grid/LeafNode.tsx (lines 510-599: full render block — divRef, ringClass, root div, canvas, empty placeholder, ActionBar wrapper)
    - src/Grid/LeafNode.tsx (any existing ResizeObserver usage — find with grep for 'ResizeObserver' and 'divRef')
    - src/Grid/CanvasWrapper.tsx (verify no parent overflow-hidden reintroduces clipping above LeafNode)
    - src/Grid/ContainerNode.tsx (verify no overflow-hidden on container divs)
    - .planning/phases/07-cell-controls-display-polish/07-UI-SPEC.md (LeafNode.tsx section, Interaction States > Empty Cell Placeholder)
    - src/Grid/__tests__/LeafNode.test.tsx (existing patterns)
    - src/test/setup.ts (ResizeObserver polyfill — must already exist per Phase 2 decisions)
  </read_first>
  <behavior>
    - Test 1: Root cell div has className containing "overflow-visible" (not "overflow-hidden")
    - Test 2: A child wrapper div with className containing "absolute inset-0 overflow-hidden" wraps the canvas element
    - Test 3: Empty placeholder ImageIcon receives style.width and style.height === "clamp(20px, 1.6vw, 32px)" (or equivalent inline style)
    - Test 4: Empty placeholder span has className containing "text-[clamp(10px,0.7vw,14px)]"
    - Test 5: When ResizeObserver fires with contentRect.height < 80, the label span has className containing "hidden"
    - Test 6: When height >= 80, the label is visible (no "hidden" class)
    - Test 7: Sibling integration — CanvasArea/CanvasWrapper render does not reintroduce overflow-hidden above LeafNode root
  </behavior>
  <action>
    Modify src/Grid/LeafNode.tsx to implement overflow isolation (D-01, D-02, D-03) and empty placeholder scaling + label hiding (D-07, D-08).

    Concrete changes:

    1. **Overflow isolation (D-01, D-02, D-03):**

       a. Change root div className (line 526). REMOVE `overflow-hidden`, ADD `overflow-visible`:
          ```tsx
          className={`
            relative w-full h-full isolate overflow-visible select-none
            ${ringClass}
            ${hasMedia ? '' : 'bg-[#1c1c1c]'}
          `}
          ```

       b. Wrap the existing `<canvas ref={canvasRef} ...>` element (lines 559-563) in a new clipping div. The wrapper carries `overflow-hidden`, the canvas keeps its existing classes:
          ```tsx
          <div className="absolute inset-0 overflow-hidden" style={{
            borderRadius: borderRadius > 0 ? `${borderRadius}px` : undefined
          }}>
            <canvas
              ref={canvasRef}
              className="absolute inset-0 w-full h-full"
              style={{ display: mediaUrl ? 'block' : 'none' }}
            />
          </div>
          ```
          NOTE: Move borderRadius from root div style to the canvas wrapper div so clipping respects rounded corners. Remove borderRadius from root div style block (line 530-533) — keep `backfaceVisibility: 'hidden'` only.

       c. Verify no intermediate parents re-clip. After making the change, run `grep -n "overflow-hidden" src/Grid/CanvasWrapper.tsx src/Grid/ContainerNode.tsx`. If any intermediate ancestor between CanvasWrapper and LeafNode has `overflow-hidden`, remove it from those wrappers — but the canvas/media clipping must remain intact via the new inner wrapper. Document any changes in the SUMMARY.

    2. **Empty placeholder scaling + label hiding (D-07, D-08):**

       a. Add a local state for "too small" detection near other useState/useRef declarations in LeafNode:
          ```tsx
          const [isTooSmall, setIsTooSmall] = useState(false);
          ```

       b. Add a useEffect with ResizeObserver on divRef.current (after existing refs/effects). The observer sets isTooSmall = true when contentRect.height < 80, false otherwise. If a ResizeObserver effect already exists in LeafNode, INTEGRATE the new check into that callback instead of duplicating the observer:
          ```tsx
          useEffect(() => {
            const el = divRef.current;
            if (!el) return;
            const ro = new ResizeObserver((entries) => {
              const h = entries[0]?.contentRect.height ?? 0;
              setIsTooSmall(h < 80);
            });
            ro.observe(el);
            return () => ro.disconnect();
          }, []);
          ```

       c. Replace empty placeholder block (lines 565-570) with clamp-sized icon and conditionally hidden label:
          ```tsx
          {!mediaUrl && (
            <div className="flex flex-col items-center justify-center w-full h-full gap-2">
              <ImageIcon
                style={{ width: 'clamp(20px, 1.6vw, 32px)', height: 'clamp(20px, 1.6vw, 32px)' }}
                className="text-[#666666]"
              />
              <span className={`text-[clamp(10px,0.7vw,14px)] text-[#666666] ${isTooSmall ? 'hidden' : ''}`}>
                Drop image or use Upload button
              </span>
            </div>
          )}
          ```
          NOTE: lucide-react ImageIcon accepts width/height via style prop. If type errors, use a wrapper span with the inline style and put `size={undefined}` or just remove the size prop entirely (lucide uses currentColor sizing via CSS when style.width/height set).

    3. Add/update tests in src/Grid/__tests__/LeafNode.test.tsx covering all 7 behaviors above. Use existing setup.ts ResizeObserver polyfill (already present per Phase 2 decisions). Trigger isTooSmall state by mocking ResizeObserver callback or by directly invoking the polyfill's trigger.

    Implements: CELL-01 per D-01/D-02/D-03; CELL-03 per D-07/D-08.
  </action>
  <verify>
    <automated>npm test -- LeafNode.test 2>&1 | tail -30 &amp;&amp; grep -n "overflow-visible" src/Grid/LeafNode.tsx &amp;&amp; grep -n "clamp(20px, 1.6vw, 32px)" src/Grid/LeafNode.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "overflow-visible select-none" src/Grid/LeafNode.tsx` returns 1 match
    - `grep -c "overflow-hidden" src/Grid/LeafNode.tsx` returns >= 1 (the new canvas wrapper) — confirms clipping moved, not removed
    - `grep "absolute inset-0 overflow-hidden" src/Grid/LeafNode.tsx` returns at least 1 match (canvas wrapper)
    - `grep "clamp(20px, 1.6vw, 32px)" src/Grid/LeafNode.tsx` returns 1+ match (icon style)
    - `grep "text-\[clamp(10px,0.7vw,14px)\]" src/Grid/LeafNode.tsx` returns 1 match (label class)
    - `grep "isTooSmall" src/Grid/LeafNode.tsx` returns >= 3 matches (state declaration, setter, conditional class)
    - `grep "new ResizeObserver" src/Grid/LeafNode.tsx` returns >= 1 match
    - `npm test -- LeafNode.test` exits 0 with all 7 behavior tests passing
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    Cell root uses overflow-visible; canvas clipped via inner wrapper; empty placeholder uses clamp() sizing; label hides when height < 80px; all tests pass; build succeeds.
  </done>
</task>

</tasks>

<verification>
- ActionBar buttons sized via clamp(28px, 2.2vw, 36px) — `grep` confirmed
- LeafNode root has overflow-visible — `grep` confirmed
- Canvas wrapper has overflow-hidden — clipping preserved
- Empty placeholder uses clamp() inline styles
- isTooSmall state hides label below 80px
- Full test suite passes: `npm test 2>&1 | tail -10`
- Production build succeeds: `npm run build`
</verification>

<success_criteria>
- CELL-01: ActionBar visible regardless of cell size (verified by grep + manual: build app, resize cell tiny, hover — bar shows above)
- CELL-02: ActionBar buttons use clamp() not px (grep confirmed)
- CELL-03: Empty placeholder uses clamp() and hides label below 80px (grep + tests confirmed)
- All Phase 7 unit tests pass
- npm run build succeeds with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-cell-controls-display-polish/07-01-SUMMARY.md` documenting:
- Final clamp values used
- Whether any intermediate ancestor needed overflow-hidden removed
- Final isTooSmall threshold (80px) and any tweaks
- Test file locations added/modified
</output>

---
phase: quick
plan: 260402-7j0
type: execute
wave: 1
depends_on: []
files_modified: [src/Grid/LeafNode.tsx]
autonomous: true
requirements: [FIX-IMG-RENDER, FIX-IMG-SELECT]
must_haves:
  truths:
    - "Images render at correct aspect ratio without being constrained by Tailwind Preflight max-width"
    - "Images cannot be selected as text by clicking/dragging on cell content"
  artifacts:
    - path: "src/Grid/LeafNode.tsx"
      provides: "Fixed LeafNode with maxWidth override and select-none"
  key_links: []
---

<objective>
Fix two rendering bugs in LeafNode: (1) Tailwind Preflight's max-width:100% overrides explicit inline width on absolutely-positioned images, causing wrong aspect ratio and misalignment; (2) images are selectable as text.

Purpose: Correct visual fidelity of image rendering in grid cells.
Output: Updated src/Grid/LeafNode.tsx with both fixes.
</objective>

<execution_context>
@.planning/quick/260402-7j0-fix-image-rendering-in-leafnode-add-maxw/260402-7j0-PLAN.md
</execution_context>

<context>
@src/Grid/LeafNode.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix image maxWidth and add select-none to LeafNode</name>
  <files>src/Grid/LeafNode.tsx</files>
  <action>
Two targeted edits in src/Grid/LeafNode.tsx:

1. In renderMedia(), find the explicit-sizing img element (around line 264-278) with the inline style object containing position/width/height/left/top. Add `maxWidth: 'none'` to that style object. This overrides Tailwind Preflight's `max-width: 100%` rule that constrains the absolutely-positioned image width below the intended scaledW value.

2. In the return JSX, find the outer cell div (around line 298-299) with className containing `relative w-full h-full isolate overflow-hidden`. Add `select-none` to that className string. This applies `user-select: none` to the entire cell, preventing images and placeholder text from being selectable.

Do NOT change any other logic, props, or files.
  </action>
  <verify>
    <automated>cd /Users/radu/Developer/storygrid && npx tsc --noEmit && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
  - The explicit-sizing img style object includes maxWidth: 'none'
  - The outer cell div className includes select-none
  - TypeScript compiles cleanly
  - All existing tests pass
  </done>
</task>

</tasks>

<verification>
- grep for `maxWidth: 'none'` in src/Grid/LeafNode.tsx confirms the Preflight override is present
- grep for `select-none` in the outer div className confirms text selection prevention
- `npx tsc --noEmit` passes
- `npx vitest run` passes all tests
</verification>

<success_criteria>
- Images in grid cells render at correct aspect ratio without horizontal constraint from Preflight
- Cell content (images and placeholder text) is not selectable
- No regressions in TypeScript compilation or test suite
</success_criteria>

<output>
After completion, update .planning/quick/260402-7j0-fix-image-rendering-in-leafnode-add-maxw/260402-7j0-SUMMARY.md
</output>

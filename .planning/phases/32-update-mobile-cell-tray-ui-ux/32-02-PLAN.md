---
phase: 32-update-mobile-cell-tray-ui-ux
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Editor/Toolbar.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Mobile app header outer element uses h-11 (44px), down from h-12 (48px)"
    - "Desktop header is unchanged (h-12 on the desktop branch is not touched)"
    - "All mobile header buttons remain at w-11 h-11 (44px) — no button height changes"
    - "TypeScript build passes with no errors"
  artifacts:
    - path: "src/Editor/Toolbar.tsx"
      provides: "Mobile branch header compacted from h-12 to h-11"
      contains: "h-11 px-2 gap-2 bg-[var(--card)] border-b"
  key_links:
    - from: "MobileSheet.tsx SNAP_TRANSLATE.full"
      to: "Toolbar.tsx mobile header height"
      via: "44px header value in both — must stay in sync"
      pattern: "h-11"
---

<objective>
Compact the mobile app header from h-12 (48px) to h-11 (44px) — recovering 4px of vertical canvas space on phones while keeping all button touch targets at exactly 44px (Apple HIG minimum). This is the D-11 change in CONTEXT.md.

Purpose: The 4px reduction is small but adds up with the tab strip going from 60px to 48px — together they recover 16px of canvas space on mobile, making the collage more prominent.

Output: Toolbar.tsx mobile branch `<header>` class updated. Desktop branch untouched.
</objective>

<execution_context>
@/Users/radu/Developer/storygrid/.claude/get-shit-done/workflows/execute-plan.md
@/Users/radu/Developer/storygrid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/radu/Developer/storygrid/.planning/phases/32-update-mobile-cell-tray-ui-ux/32-CONTEXT.md
@/Users/radu/Developer/storygrid/.planning/phases/32-update-mobile-cell-tray-ui-ux/32-UI-SPEC.md

<interfaces>
<!-- Toolbar.tsx current state — extracted from codebase. -->

Mobile branch (line 43–86):
  if (isMobile) {
    return (
      <header className="flex items-center justify-around h-12 px-2 gap-2 bg-[var(--card)] border-b border-[var(--border)] shrink-0">
        {/* Undo */}
        <button className="w-11 h-11 ...">  ← these STAY at h-11 (44px)

Desktop branch (line 89–):
  return (
    <header className="flex items-center gap-1 h-12 px-4 bg-[#1c1c1c] ...">  ← do NOT touch this line
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update mobile header height h-12 → h-11 in Toolbar.tsx</name>
  <read_first>
    - src/Editor/Toolbar.tsx lines 43–90 (mobile branch header and desktop branch header — to confirm which line to change and which to leave)
  </read_first>
  <files>src/Editor/Toolbar.tsx</files>
  <action>
Make a single targeted class change in Toolbar.tsx. Read lines 43–90 before editing to confirm exact line number.

**Change:**
In the mobile branch `<header>` element (inside `if (isMobile) { return ... }`), change:
```
h-12
```
to:
```
h-11
```

The full class string changes from:
```
"flex items-center justify-around h-12 px-2 gap-2 bg-[var(--card)] border-b border-[var(--border)] shrink-0"
```
to:
```
"flex items-center justify-around h-11 px-2 gap-2 bg-[var(--card)] border-b border-[var(--border)] shrink-0"
```

**Do NOT change:**
- The desktop branch `<header>` on line ~90: `className="flex items-center gap-1 h-12 px-4 bg-[#1c1c1c] ..."` — this h-12 must remain unchanged.
- Any button sizes (`w-11 h-11`) — buttons stay at 44px.

**Rationale (D-11, RESEARCH.md Pitfall 4):** The outer header shrinks by 4px. Buttons are w-11 h-11 (44px) which exactly fits in h-11 (44px) outer with zero overflow. Using h-10 (40px) would clip the 44px buttons — h-11 is the minimum safe value per Apple HIG.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/Editor/Toolbar.tsx contains: `h-11 px-2 gap-2 bg-[var(--card)] border-b` (mobile branch updated)
    - src/Editor/Toolbar.tsx does NOT contain: `h-12 px-2 gap-2 bg-[var(--card)]` (old mobile branch class — gone)
    - src/Editor/Toolbar.tsx DOES contain: `h-12 px-4 bg-[#1c1c1c]` (desktop branch unchanged)
    - npm run build exits 0 (TypeScript compile clean)
  </acceptance_criteria>
  <done>Mobile header is h-11 (44px). Desktop header untouched at h-12. Build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | This plan changes only a Tailwind class on a static header element. No data flows or trust boundaries are affected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-06 | Denial of Service | Mobile header compaction | accept | CSS class change only; no logic, no user data, no attack surface |
</threat_model>

<verification>
```bash
npm run build 2>&1 | tail -10
```

Build must exit 0. No TypeScript errors expected (class-only change).

Visual check (done during plan 01 human verification):
- Open app on mobile viewport; confirm header appears slimmer (44px, not 48px)
- Confirm all header buttons (Undo, Redo, Templates, Export, Clear) are fully tappable with no clipping
</verification>

<success_criteria>
- Toolbar.tsx mobile branch `<header>` has class `h-11` (not `h-12`)
- Desktop branch `<header>` still has `h-12`
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/32-update-mobile-cell-tray-ui-ux/32-02-SUMMARY.md`
</output>

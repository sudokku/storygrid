---
phase: 22-mobile-header-touch-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/phase22-mobile-header.test.tsx
  - src/test/phase22-touch-polish.test.tsx
  - src/Editor/Toolbar.tsx
  - src/Editor/ExportSplitButton.tsx
  - src/index.css
  - src/Editor/CanvasArea.tsx
autonomous: true
requirements: [HEADER-01, HEADER-02, SCROLL-01, SCROLL-02]

must_haves:
  truths:
    - "Mobile header shows 5 icon buttons: Undo, Redo, Templates, Export (split), Clear"
    - "Every mobile header button has a 44x44px tap target with 8px gaps"
    - "Pulling down on canvas does not trigger browser pull-to-refresh"
    - "Tapping interactive elements responds immediately without 300ms delay"
    - "Mobile Clear button calls clearGrid without confirmation dialog"
    - "Mobile ExportSplitButton shows icon-only form with format dropdown"
  artifacts:
    - path: "src/Editor/Toolbar.tsx"
      provides: "Mobile header toolbar with 5 icon buttons"
      contains: "mobile-undo"
    - path: "src/Editor/ExportSplitButton.tsx"
      provides: "Mobile-adapted export split button"
      contains: "isMobile"
    - path: "src/index.css"
      provides: "Global touch-action and overscroll rules"
      contains: "touch-action: manipulation"
    - path: "src/Editor/CanvasArea.tsx"
      provides: "Canvas overscroll-behavior: contain"
      contains: "overscrollBehavior"
    - path: "src/test/phase22-mobile-header.test.tsx"
      provides: "Tests for HEADER-01 and HEADER-02"
    - path: "src/test/phase22-touch-polish.test.tsx"
      provides: "Tests for SCROLL-01 and SCROLL-02"
  key_links:
    - from: "src/Editor/Toolbar.tsx"
      to: "src/Editor/ExportSplitButton.tsx"
      via: "isMobile prop"
      pattern: "ExportSplitButton.*isMobile"
    - from: "src/index.css"
      to: "all interactive elements"
      via: "global CSS rule"
      pattern: "touch-action:\\s*manipulation"
---

<objective>
Rebuild the mobile header toolbar to expose all 5 primary actions (Undo, Redo, Templates, Export, Clear) as icon-only buttons with 44x44px touch targets, and add global CSS rules to eliminate pull-to-refresh interference and the 300ms tap delay.

Purpose: Mobile users currently only see an Export button in the header. This plan gives them full editor control from the header bar and fixes two pervasive touch interaction issues.
Output: Updated Toolbar.tsx mobile branch, ExportSplitButton with isMobile prop, global CSS touch polish in index.css and CanvasArea.tsx, plus comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-mobile-header-touch-polish/22-CONTEXT.md
@.planning/phases/22-mobile-header-touch-polish/22-RESEARCH.md
@.planning/phases/22-mobile-header-touch-polish/22-VALIDATION.md

@src/Editor/Toolbar.tsx
@src/Editor/ExportSplitButton.tsx
@src/Editor/CanvasArea.tsx
@src/index.css
@src/hooks/useMediaQuery.ts
@src/components/TemplatesPopover.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/Editor/Toolbar.tsx:
- `useMediaQuery('(max-width: 767px)')` returns boolean `isMobile`
- `useGridStore(s => s.undo)`, `useGridStore(s => s.redo)`, `useGridStore(s => s.clearGrid)` — action functions
- `useGridStore(s => s.historyIndex > 0)` — canUndo boolean
- `useGridStore(s => s.historyIndex < s.history.length - 1)` — canRedo boolean
- `useEditorStore(s => s.isExporting)` — boolean
- Lines 94-109 are the mobile branch to replace
- Lines 41-45 are the desktop handleClearGrid with window.confirm()

From src/Editor/ExportSplitButton.tsx:
- `export function ExportSplitButton()` — currently takes no props
- Returns a `<div>` with export button + chevron popover + toast + metrics panel
- `handleExport` callback is internal to the component

From src/Editor/CanvasArea.tsx:
- `<main>` element at line 83 with `style={sheetOpen ? { touchAction: 'none' } : undefined}`
- `data-testid="workspace-main"` on the main element

From src/index.css:
- `@layer base` block starting at line 15
- `body` rule at line 89-91
- No existing touch-action or overscroll-behavior rules

From src/test/setup.ts:
- Contains `window.matchMedia` polyfill for jsdom
- Pattern: mock matchMedia to return `matches: true` for mobile tests

From src/Editor/__tests__/phase05.1-p01-foundation.test.tsx:
- Contains `mockMatchMedia` helper pattern for mobile test mocking
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): Create test stubs for HEADER and SCROLL requirements</name>
  <files>src/test/phase22-mobile-header.test.tsx, src/test/phase22-touch-polish.test.tsx</files>
  <read_first>
    - src/Editor/__tests__/phase05.1-p01-foundation.test.tsx (mockMatchMedia pattern)
    - src/test/setup.ts (global test setup, matchMedia polyfill)
    - src/Editor/Toolbar.tsx (current mobile branch structure)
    - src/Editor/CanvasArea.tsx (current style prop on main)
    - src/index.css (current CSS rules)
  </read_first>
  <action>
Create two test files using the existing mockMatchMedia pattern from Phase 5.1 tests.

**`src/test/phase22-mobile-header.test.tsx`** — Tests for HEADER-01 and HEADER-02:

1. Import React, render, screen from @testing-library/react, and Toolbar from `../Editor/Toolbar`.
2. Create a `mockMatchMedia` helper that sets `window.matchMedia` to return `{ matches: true, ... }` for `(max-width: 767px)`. Use the exact pattern from `phase05.1-p01-foundation.test.tsx`.
3. In `beforeEach`, call `mockMatchMedia()`. In `afterEach`, restore original matchMedia.
4. Test: "HEADER-01: mobile header renders all 5 action buttons" — render Toolbar, assert presence of elements with `data-testid="mobile-undo"`, `data-testid="mobile-redo"`, `data-testid="mobile-clear"`, `data-testid="export-button"`, and the TemplatesPopover (query by aria-label or testid). Expect exactly 5 interactive buttons/controls.
5. Test: "HEADER-01: mobile header does NOT render wordmark" — render Toolbar, assert no element contains text "StoryGrid".
6. Test: "HEADER-02: all mobile header buttons have w-11 h-11 classes" — render Toolbar, query all buttons with `data-testid` matching `mobile-*` or `export-button`, assert each has class `w-11` and `h-11` (or their parent wrapper does).
7. Test: "HEADER-02: mobile header uses gap-2 spacing" — render Toolbar, assert the header element has `gap-2` in its className.
8. Test: "Clear button calls clearGrid without confirmation" — mock `window.confirm`, render Toolbar, click `mobile-clear`, assert `clearGrid` was called and `window.confirm` was NOT called.

**`src/test/phase22-touch-polish.test.tsx`** — Tests for SCROLL-01 and SCROLL-02:

1. Import and read `src/index.css` as a string (use `import css from '../index.css?raw'` — Vite raw import).
2. Test: "SCROLL-01: index.css body has overscroll-behavior: contain" — assert the CSS string contains `overscroll-behavior: contain` within the body rule.
3. Test: "SCROLL-02: index.css has touch-action: manipulation for interactive elements" — assert the CSS string contains `touch-action: manipulation`.
4. Test: "SCROLL-01: CanvasArea main has overscrollBehavior contain" — render CanvasArea, query `[data-testid='workspace-main']`, assert its inline style includes `overscrollBehavior: 'contain'` (or check via `style.overscrollBehavior`).

All tests should initially FAIL (RED state) because the implementation does not exist yet.
  </action>
  <verify>
    <automated>cd /Users/radu/Developer/storygrid && npx vitest run src/test/phase22-mobile-header.test.tsx src/test/phase22-touch-polish.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/test/phase22-mobile-header.test.tsx exists and contains `data-testid="mobile-undo"` in a query
    - src/test/phase22-mobile-header.test.tsx contains `data-testid="mobile-redo"` in a query
    - src/test/phase22-mobile-header.test.tsx contains `data-testid="mobile-clear"` in a query
    - src/test/phase22-mobile-header.test.tsx contains `window.confirm` assertion (called 0 times)
    - src/test/phase22-mobile-header.test.tsx contains `w-11` string assertion
    - src/test/phase22-mobile-header.test.tsx contains `gap-2` string assertion
    - src/test/phase22-touch-polish.test.tsx exists and contains `overscroll-behavior: contain` assertion
    - src/test/phase22-touch-polish.test.tsx contains `touch-action: manipulation` assertion
    - src/test/phase22-touch-polish.test.tsx contains `overscrollBehavior` assertion for CanvasArea
    - Tests run without import/syntax errors (they may fail assertions — that is expected at Wave 0)
  </acceptance_criteria>
  <done>Both test files exist with specific test cases for all 4 requirements. Tests compile and run (failures expected at this stage).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 1): Replace mobile header with 5-button icon toolbar and adapt ExportSplitButton</name>
  <files>src/Editor/Toolbar.tsx, src/Editor/ExportSplitButton.tsx</files>
  <read_first>
    - src/Editor/Toolbar.tsx (full file — current mobile branch lines 94-109 to replace)
    - src/Editor/ExportSplitButton.tsx (full file — needs isMobile prop)
    - src/components/TemplatesPopover.tsx (understand its button size for wrapping)
    - src/test/phase22-mobile-header.test.tsx (the tests this task must make pass)
  </read_first>
  <behavior>
    - HEADER-01: Mobile Toolbar renders 5 controls: Undo (Undo2 icon), Redo (Redo2 icon), TemplatesPopover, ExportSplitButton, Clear (Trash2 icon)
    - HEADER-01: No "StoryGrid" wordmark text on mobile
    - HEADER-02: Each button/wrapper has w-11 h-11 (44x44px) classes
    - HEADER-02: Header container uses gap-2 (8px) between buttons
    - Clear button calls clearGrid() directly — no window.confirm()
    - ExportSplitButton accepts optional isMobile prop (default false)
    - When isMobile=true, ExportSplitButton renders icon-only: Download icon on left half, ChevronDown on right half, total w-11 h-11 container
    - Desktop ExportSplitButton unchanged (no isMobile prop passed = false)
  </behavior>
  <action>
**Toolbar.tsx changes** (mobile branch only, lines 94-109):

Replace the entire `if (isMobile) { return ... }` block. The new mobile return:
- `<header>` with classes: `flex items-center justify-around h-12 px-2 gap-2 bg-[var(--card)] border-b border-[var(--border)] shrink-0`
- 5 children in order:
  1. Undo button: `<button className="w-11 h-11 flex items-center justify-center rounded-lg text-neutral-300 disabled:opacity-40" onClick={undo} disabled={!canUndo} aria-label="Undo" data-testid="mobile-undo"><Undo2 size={20} /></button>`
  2. Redo button: same pattern with `redo`, `!canRedo`, `aria-label="Redo"`, `data-testid="mobile-redo"`, `<Redo2 size={20} />`
  3. TemplatesPopover wrapper: `<div className="w-11 h-11 flex items-center justify-center"><TemplatesPopover /></div>` — wraps the existing component to meet 44px target without modifying TemplatesPopover internals
  4. ExportSplitButton: `<ExportSplitButton isMobile />`
  5. Clear button: `<button className="w-11 h-11 flex items-center justify-center rounded-lg text-neutral-300" onClick={clearGrid} aria-label="Clear canvas" data-testid="mobile-clear"><Trash2 size={20} /></button>` — calls `clearGrid` directly, NO `handleClearGrid` (which has window.confirm), NO window.confirm

Remove the old `handleExport` callback (lines 47-92) ONLY if it is no longer used. Check: `handleExport` was only used in the old mobile branch (line 104). The desktop branch uses ExportSplitButton's internal handler. So `handleExport` and its related imports (`exportGrid`, `downloadDataUrl`, `hasVideoCell`, `CanvasSettings`) can be removed from Toolbar.tsx since they are only needed by the old mobile export button. Keep all store subscriptions that the mobile branch still uses (undo, redo, clearGrid, canUndo, canRedo, isExporting for ExportSplitButton).

Actually — `isExporting` and `setIsExporting` are used by `handleExport` only. If removing `handleExport`, also remove `isExporting`/`setIsExporting` subscriptions from Toolbar. Also remove the `CanvasSettings` type import and `exportGrid`/`downloadDataUrl`/`hasVideoCell` imports if no longer used.

**ExportSplitButton.tsx changes:**

1. Add `isMobile?: boolean` prop: `export function ExportSplitButton({ isMobile = false }: { isMobile?: boolean })`.
2. Add a mobile render path. Before the existing desktop return (`return <> ...`), add:
```typescript
if (isMobile) {
  return (
    <>
      <div ref={containerRef} className="relative flex items-center w-11 h-11">
        {/* Left: export trigger */}
        <button
          onClick={handleExport}
          disabled={isExporting}
          aria-label={exportLabel}
          data-testid="export-button"
          className="flex items-center justify-center h-11 flex-1 rounded-l-lg text-neutral-300 disabled:opacity-40"
        >
          <Download size={20} />
        </button>
        {/* Divider */}
        <div className="w-px h-5 bg-white/20 self-center" />
        {/* Right: dropdown toggle */}
        <button
          onClick={() => !isExporting && setPopoverOpen(!popoverOpen)}
          disabled={isExporting}
          aria-label="Export settings"
          aria-expanded={popoverOpen}
          className="flex items-center justify-center w-4 h-11 rounded-r-lg text-neutral-400 disabled:opacity-40"
        >
          <ChevronDown size={12} />
        </button>
        {/* Reuse existing popover */}
        {popoverOpen && (
          <div
            ref={popoverRef}
            className="absolute top-full right-0 mt-1 w-56 bg-[#1c1c1c] border border-white/10 rounded-md shadow-lg p-2 z-50"
            role="dialog"
            aria-label="Export settings"
          >
            {/* Same popover content as desktop — copy the hasVideos ternary */}
          </div>
        )}
      </div>
      <Toast state={toastState} encodingPercent={encodingPercent} onRetry={handleExport} onDismiss={() => setToastState(null)} />
      {METRICS_ENABLED && <ExportMetricsPanel ... />}
    </>
  );
}
```

The popover content inside the mobile branch should be identical to the desktop popover content (the `hasVideos ? ... : ...` ternary). Extract the popover JSX into a local variable or inline-copy to avoid duplication. Use a local variable approach:
```typescript
const popoverContent = popoverOpen && (
  <div ref={popoverRef} className="..." role="dialog" aria-label="Export settings">
    {hasVideos ? ( ... ) : ( ... )}
  </div>
);
```
Then reference `{popoverContent}` in both mobile and desktop returns. This avoids copy-paste of the popover internals.

3. Desktop return is unchanged — no `isMobile` prop passed from desktop Toolbar means `false`.
  </action>
  <verify>
    <automated>cd /Users/radu/Developer/storygrid && npx vitest run src/test/phase22-mobile-header.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Toolbar.tsx mobile branch contains `data-testid="mobile-undo"`
    - Toolbar.tsx mobile branch contains `data-testid="mobile-redo"`
    - Toolbar.tsx mobile branch contains `data-testid="mobile-clear"`
    - Toolbar.tsx mobile branch contains `clearGrid` (direct call, not handleClearGrid)
    - Toolbar.tsx mobile branch does NOT contain `window.confirm`
    - Toolbar.tsx mobile branch does NOT contain string "StoryGrid"
    - Toolbar.tsx mobile branch contains `w-11 h-11` on button elements
    - Toolbar.tsx mobile header element contains `gap-2`
    - Toolbar.tsx mobile branch contains `<ExportSplitButton isMobile`
    - Toolbar.tsx mobile branch contains `<TemplatesPopover />`
    - ExportSplitButton.tsx function signature contains `isMobile`
    - ExportSplitButton.tsx contains a mobile render path with `w-11 h-11`
    - ExportSplitButton.tsx desktop path is unchanged (no `isMobile` in desktop call from Toolbar)
    - phase22-mobile-header tests pass (exit code 0)
  </acceptance_criteria>
  <done>Mobile header renders 5 icon buttons at 44x44px with 8px gaps. ExportSplitButton has icon-only mobile form. Clear calls clearGrid without confirmation. All HEADER tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (Wave 1): Add overscroll-behavior and touch-action CSS rules</name>
  <files>src/index.css, src/Editor/CanvasArea.tsx</files>
  <read_first>
    - src/index.css (current @layer base rules, body rule at line 89-91)
    - src/Editor/CanvasArea.tsx (current style prop on main element, line 88)
    - src/test/phase22-touch-polish.test.tsx (the tests this task must make pass)
  </read_first>
  <behavior>
    - SCROLL-01: body element has overscroll-behavior: contain
    - SCROLL-01: CanvasArea main element has overscrollBehavior: 'contain' in inline style
    - SCROLL-02: Global CSS rule applies touch-action: manipulation to button, [role="button"], input, select, textarea, a
  </behavior>
  <action>
**`src/index.css` changes** (inside `@layer base`):

1. Add `overscroll-behavior: contain;` to the existing `body` rule (line 89-91). Result:
```css
body {
  @apply bg-background text-foreground;
  overscroll-behavior: contain;
}
```

2. Add a new rule block after the `body` rule (still inside `@layer base`):
```css
button,
[role="button"],
input,
select,
textarea,
a {
  touch-action: manipulation;
}
```

**`src/Editor/CanvasArea.tsx` changes:**

Update the `<main>` element's `style` prop (line 88). Change from:
```typescript
style={sheetOpen ? { touchAction: 'none' } : undefined}
```
To:
```typescript
style={{ overscrollBehavior: 'contain', ...(sheetOpen ? { touchAction: 'none' } : {}) }}
```

This ensures `overscrollBehavior: 'contain'` is always applied to the canvas area main element. When the sheet is open, `touchAction: 'none'` is also applied (overriding the global `manipulation` rule, which is correct — the canvas should not respond to touch when the sheet covers it).
  </action>
  <verify>
    <automated>cd /Users/radu/Developer/storygrid && npx vitest run src/test/phase22-touch-polish.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/index.css contains `overscroll-behavior: contain` inside body rule
    - src/index.css contains `touch-action: manipulation` in a rule targeting `button`
    - src/index.css contains `[role="button"]` in the touch-action rule selector
    - src/Editor/CanvasArea.tsx main element style contains `overscrollBehavior: 'contain'`
    - src/Editor/CanvasArea.tsx still applies `touchAction: 'none'` when sheetOpen is true
    - phase22-touch-polish tests pass (exit code 0)
    - Full test suite passes: `npx vitest run` exits 0
  </acceptance_criteria>
  <done>Global CSS prevents pull-to-refresh and 300ms tap delay. CanvasArea has belt-and-suspenders overscroll containment. All SCROLL tests pass. Full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase involves no trust boundary crossings. All changes are client-side CSS and JSX layout modifications. No user data is processed, stored, or transmitted. No new inputs are introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | touch-action CSS | accept | CSS is client-side only; tampering via DevTools has no security impact — it only affects the user's own UX |
| T-22-02 | Denial of Service | overscroll-behavior | accept | Progressive enhancement; unsupported browsers degrade gracefully to default behavior |

No high-severity threats identified. This phase adds no authentication, data handling, API calls, or cross-origin resources. All changes are presentation-layer CSS and component layout — ASVS L1 categories V2-V14 do not apply.
</threat_model>

<verification>
```bash
# Phase 22 tests pass
npx vitest run src/test/phase22-mobile-header.test.tsx src/test/phase22-touch-polish.test.tsx

# Full test suite passes
npx vitest run

# TypeScript compiles
npx tsc --noEmit
```
</verification>

<success_criteria>
1. Mobile header renders Undo, Redo, Templates, Export (split), Clear — all as icon-only buttons (HEADER-01)
2. Every mobile header button is 44x44px with 8px gaps between them (HEADER-02)
3. Body and canvas area have overscroll-behavior: contain (SCROLL-01)
4. All interactive elements have touch-action: manipulation globally (SCROLL-02)
5. Mobile Clear button calls clearGrid directly without window.confirm
6. ExportSplitButton renders icon-only form when isMobile=true
7. Desktop Toolbar branch is completely unchanged
8. All phase 22 tests and full test suite pass
</success_criteria>

<output>
After completion, create `.planning/phases/22-mobile-header-touch-polish/22-01-SUMMARY.md`
</output>

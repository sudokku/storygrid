---
phase: 23-bottom-sheet-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Editor/MobileSheet.tsx
  - src/store/editorStore.ts
  - src/Editor/__tests__/phase05.1-p01-foundation.test.tsx
autonomous: false
requirements:
  - SHEET-01
  - SHEET-02
  - SHEET-03
  - SHEET-04
tags:
  - ui
  - mobile
  - refactor

must_haves:
  truths:
    - "On mobile, a chevron toggle button opens the sheet when tapped while collapsed"
    - "On mobile, a chevron toggle button closes the sheet when tapped while full"
    - "Selecting a cell auto-expands the sheet to full height"
    - "When collapsed, a 60px tab strip is visible showing a chevron and a label ('Canvas Settings' or 'Cell Settings')"
    - "All content inside the sheet is scrollable in every snap state; no controls are cut off"
    - "The drag-pill gesture is entirely removed — dragging the strip does nothing"
    - "The 'half' snap state no longer exists in the type system or in the SNAP_TRANSLATE map"
  artifacts:
    - path: "src/Editor/MobileSheet.tsx"
      provides: "Refactored mobile sheet with toggle button, tab strip, two snap states, no drag"
      contains: "ChevronUp"
    - path: "src/store/editorStore.ts"
      provides: "SheetSnapState type narrowed to 'collapsed' | 'full'"
      contains: "'collapsed' | 'full'"
    - path: "src/Editor/__tests__/phase05.1-p01-foundation.test.tsx"
      provides: "Updated tests reflecting toggle + tab strip behavior; drag tests removed"
      contains: "Open panel"
  key_links:
    - from: "MobileSheet toggle button onClick"
      to: "setSheetSnapState"
      via: "ternary on current sheetSnapState"
      pattern: "setSheetSnapState\\(sheetSnapState === 'collapsed' \\? 'full' : 'collapsed'\\)"
    - from: "MobileSheet useEffect on selectedNodeId"
      to: "setSheetSnapState('full')"
      via: "auto-expand trigger"
      pattern: "setSheetSnapState\\('full'\\)"
    - from: "editorStore SheetSnapState type"
      to: "MobileSheet SNAP_TRANSLATE map keys"
      via: "type-level contract"
      pattern: "'collapsed' \\| 'full'"
---

<objective>
Redesign the mobile bottom sheet to use a toggle chevron button instead of the drag-pill gesture, with two snap states (collapsed/full), auto-expand on cell selection, and a persistent 60px tab strip showing a context label.

Purpose: Satisfy SHEET-01..04 — mobile users must reach every sheet control reliably without the unreliable drag gesture, and the sheet must never hide itself completely.

Output: Refactored `MobileSheet.tsx`, narrowed `SheetSnapState` type in `editorStore.ts`, and updated foundation tests that reflect the new toggle/tab-strip contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/23-bottom-sheet-redesign/23-CONTEXT.md
@.planning/phases/23-bottom-sheet-redesign/23-UI-SPEC.md
@.planning/phases/23-bottom-sheet-redesign/23-PATTERNS.md
@src/Editor/MobileSheet.tsx
@src/store/editorStore.ts
@src/Editor/__tests__/phase05.1-p01-foundation.test.tsx

<interfaces>
<!-- Key contracts the executor must preserve/change. Extracted from codebase. -->

Current `src/store/editorStore.ts` (lines 31, 58):
```ts
sheetSnapState: 'collapsed' | 'half' | 'full';
setSheetSnapState: (v: 'collapsed' | 'half' | 'full') => void;
```

Target (narrow both to):
```ts
sheetSnapState: 'collapsed' | 'full';
setSheetSnapState: (v: 'collapsed' | 'full') => void;
```

Initial value (`editorStore.ts` line 91) stays `'collapsed' as const` — already valid.
Setter implementation (`editorStore.ts` line 122) stays `(v) => set({ sheetSnapState: v })` — type-driven, no change.

Retained data-testids (Playwright compatibility):
- `data-testid="mobile-sheet"` on wrapper
- `data-sheet-snap={sheetSnapState}` on wrapper
- `data-testid="sheet-drag-handle"` on the 60px strip div (legacy name kept)
- `data-testid="exit-pan-mode"` unchanged
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor MobileSheet.tsx + narrow SheetSnapState type</name>
  <files>src/Editor/MobileSheet.tsx, src/store/editorStore.ts</files>
  <read_first>
    - `.planning/phases/23-bottom-sheet-redesign/23-CONTEXT.md` (full — user decisions are locked)
    - `.planning/phases/23-bottom-sheet-redesign/23-UI-SPEC.md` § Interaction Contract, § Animation Contract, § Accessibility Contract
    - `.planning/phases/23-bottom-sheet-redesign/23-PATTERNS.md` § Pattern Assignments (exact code excerpts for every change)
    - `src/Editor/MobileSheet.tsx` (entire file, 154 lines) — the target of the refactor
    - `src/store/editorStore.ts` lines 28–60 (type block) and 85–125 (initial state + actions)
    - `src/Editor/Toolbar.tsx` lines 43–86 — mobile-branch button pattern donor referenced in PATTERNS.md
  </read_first>
  <behavior>
    Type system:
    - `SheetSnapState` in both `editorStore.ts` and `MobileSheet.tsx` becomes `'collapsed' | 'full'` (no `'half'`).
    - TypeScript compile passes with no references to `'half'` anywhere.

    Rendering:
    - Sheet renders with `data-testid="mobile-sheet"`, `data-sheet-snap` reflects current state.
    - Sheet wrapper height is `100dvh` (not `100vh`) — verifiable via inline style string.
    - Sheet wrapper has class `motion-reduce:transition-none`.
    - Sheet wrapper transition is unconditional `transform 0.3s cubic-bezier(0.32, 0.72, 0, 1)` — no `isDragging` branch.
    - 60px strip div keeps `data-testid="sheet-drag-handle"` and height 60. No `touch-none cursor-grab select-none` classes. No `onPointerDown` / `onPointerUp`.
    - 60px strip contains exactly: one `<button>` (toggle) and one `<span>` (label). Nothing else.
    - Visual drag-pill div (`h-1 w-8 rounded-full bg-[#555]`) is removed.
    - TemplatesPopover / Undo / Redo / Clear buttons are removed from the sheet entirely.

    Toggle button:
    - Renders `<ChevronUp size={20} />` when `sheetSnapState === 'collapsed'`, `<ChevronDown size={20} />` when `'full'`.
    - `aria-label` is `"Open panel"` when collapsed, `"Close panel"` when full.
    - Dimensions: `min-w-[44px] h-11`, `flex items-center justify-center rounded-md`.
    - Classes include `text-[var(--muted-foreground)] active:text-[var(--foreground)] focus-visible:ring-2 focus-visible:ring-[var(--ring)] focus-visible:outline-none`.
    - `onClick` calls `setSheetSnapState(sheetSnapState === 'collapsed' ? 'full' : 'collapsed')`.

    Tab label:
    - `<span>` with classes `text-sm font-medium text-[var(--foreground)]`.
    - Text is `"Cell Settings"` when `selectedNodeId` is truthy, `"Canvas Settings"` otherwise.

    Auto-expand:
    - `useEffect` on `[selectedNodeId, setSheetSnapState]` calls `setSheetSnapState('full')` (not `'half'`) when `selectedNodeId` is truthy.

    Removed state / refs / handlers (no trace left):
    - `isDragging`, `setIsDragging`
    - `dragStartRef`
    - `handlePointerDown`, `handlePointerUp`
    - `DRAG_THRESHOLD`
    - Unused imports: `useRef`, `useCallback`, `useState` from React; `useGridStore`; `TemplatesPopover`; `Undo2`, `Redo2`, `Trash2`.

    Retained (no change):
    - `React.memo` wrapper around the component.
    - Scrollable content area `<div className="overflow-y-auto" style={{ height: 'calc(100% - 60px)', WebkitOverflowScrolling: 'touch', overscrollBehavior: 'contain' }}>`.
    - Pan-mode branch with `<button data-testid="exit-pan-mode">Exit Pan Mode</button>`.
    - `CanvasSettingsPanel` / `SelectedCellPanel` conditional rendering.
    - Sheet wrapper classes: `fixed bottom-0 left-0 right-0 z-40 bg-[var(--card)] rounded-t-2xl border-t border-[var(--border)] md:hidden`.
  </behavior>
  <action>
    Apply the exact code changes specified in `.planning/phases/23-bottom-sheet-redesign/23-PATTERNS.md`. Every substitution has a source+target excerpt there — follow them verbatim.

    Order of edits:
    1. `src/store/editorStore.ts` — narrow `SheetSnapState` on line 31 and setter signature on line 58 to `'collapsed' | 'full'`. Leave initial value (line 91) and setter impl (line 122) untouched.
    2. `src/Editor/MobileSheet.tsx` — full refactor per PATTERNS.md:
       - Replace import block with the minimal target imports (`useEffect` only from React; drop `useGridStore`, `TemplatesPopover`, `Undo2`/`Redo2`/`Trash2`; add `ChevronUp`, `ChevronDown`).
       - Narrow local `SheetSnapState` type and remove `half` key from `SNAP_TRANSLATE`.
       - Delete `DRAG_THRESHOLD`, `isDragging` state, `dragStartRef`, `handlePointerDown`, `handlePointerUp`, and all useGridStore subscriptions.
       - Change `setSheetSnapState('half')` in the auto-expand `useEffect` to `setSheetSnapState('full')`.
       - Replace sheet wrapper style: `height: '100dvh'`; unconditional transition `'transform 0.3s cubic-bezier(0.32, 0.72, 0, 1)'`. Append class `motion-reduce:transition-none`.
       - Replace the 60px strip div contents with the toggle button + span label exactly as in PATTERNS.md § "Tab strip (60px handle div) — full replacement". Keep `data-testid="sheet-drag-handle"` on the strip div. Remove drag-related classes and handlers.

    Do NOT introduce new shadcn components. The toggle is a plain `<button>` with lucide icons per UI-SPEC § Component Inventory.

    Do NOT add tooltip, `aria-expanded`, or alternate aria semantics — UI-SPEC § Accessibility explicitly forbids `aria-expanded`.

    Compile check after edits: `npm run typecheck` (or `tsc --noEmit`) must pass with zero errors — proves no stray `'half'` reference remains.
  </action>
  <verify>
    <automated>cd /Users/radu/Developer/storygrid && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `editorStore.ts` `SheetSnapState` is `'collapsed' | 'full'` (both declaration and setter arg).
    - `MobileSheet.tsx` contains `ChevronUp`, `ChevronDown`, `'Open panel'`, `'Close panel'`, `'Canvas Settings'`, `'Cell Settings'`, `'100dvh'`, `motion-reduce:transition-none`, and the auto-expand effect body `setSheetSnapState('full')`.
    - `MobileSheet.tsx` does NOT contain: `isDragging`, `dragStartRef`, `handlePointerDown`, `handlePointerUp`, `DRAG_THRESHOLD`, `useGridStore`, `TemplatesPopover`, `Undo2`, `Redo2`, `Trash2`, `touch-none`, `cursor-grab`, the visual pill div (`h-1 w-8 rounded-full bg-[#555]`), or the string `'half'`.
    - `npx tsc --noEmit` exits 0.
  </done>
  <acceptance_criteria>
    - [ ] `grep -n "'half'" src/Editor/MobileSheet.tsx src/store/editorStore.ts` returns no matches.
    - [ ] `grep -n "isDragging\|dragStartRef\|DRAG_THRESHOLD\|handlePointerDown\|handlePointerUp" src/Editor/MobileSheet.tsx` returns no matches.
    - [ ] `grep -n "TemplatesPopover\|Undo2\|Redo2\|Trash2\|useGridStore" src/Editor/MobileSheet.tsx` returns no matches.
    - [ ] `grep -n "ChevronUp\|ChevronDown" src/Editor/MobileSheet.tsx` matches both import and JSX usage.
    - [ ] `grep -n "Open panel\|Close panel" src/Editor/MobileSheet.tsx` returns both aria-label branches.
    - [ ] `grep -n "Canvas Settings\|Cell Settings" src/Editor/MobileSheet.tsx` returns both label branches.
    - [ ] `grep -n "setSheetSnapState('full')" src/Editor/MobileSheet.tsx` appears in the `useEffect` body (auto-expand).
    - [ ] `grep -n "100dvh" src/Editor/MobileSheet.tsx` matches.
    - [ ] `grep -n "motion-reduce:transition-none" src/Editor/MobileSheet.tsx` matches.
    - [ ] `grep -n "data-testid=\"sheet-drag-handle\"\|data-testid=\"mobile-sheet\"\|data-testid=\"exit-pan-mode\"" src/Editor/MobileSheet.tsx` — all three still present.
    - [ ] `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update foundation tests + add toggle/auto-expand/label coverage</name>
  <files>src/Editor/__tests__/phase05.1-p01-foundation.test.tsx</files>
  <read_first>
    - `src/Editor/__tests__/phase05.1-p01-foundation.test.tsx` (entire file — PATTERNS.md § Test Impact lists specific lines: 84, 89, 95–100, 102, 107, 114–119, 121–133, 135–153, 137, 155, 161, 168, 175, 200)
    - `src/Editor/MobileSheet.tsx` — the refactored component produced by Task 1 (canonical source of truth)
    - `.planning/phases/23-bottom-sheet-redesign/23-PATTERNS.md` § Test Impact (complete breakdown of which tests break and why, plus list of new tests to add)
    - `.planning/phases/23-bottom-sheet-redesign/23-UI-SPEC.md` § Interaction Contract (the behavior being tested)
  </read_first>
  <behavior>
    Tests removed (no longer applicable):
    - `touch-none` class presence check on the drag handle.
    - `sheet-undo`, `sheet-redo`, `sheet-clear` testid presence and wiring.
    - Drag up from `collapsed` → `half` transform assertion.
    - Drag up from `half` → `full` transform assertion.
    - Drag down from `full` → `half` and `half` → `collapsed`.
    - Any `setState({ sheetSnapState: 'half' })` seeding — `'half'` is no longer a valid value.

    Tests retained unchanged:
    - `renders with data-testid="mobile-sheet"`.
    - `starts in collapsed state` (initial value still `'collapsed'`).
    - `has md:hidden class`.
    - `has cubic-bezier transition string`.
    - `shows CanvasSettingsPanel when no cell selected`.
    - `shows SelectedCellPanel when a cell is selected`.
    - `shows Exit Pan Mode button` and its `setPanModeNodeId(null)` click wiring.
    - The Phase 22 D-02 removal test (still valid).

    Tests added (new coverage per UI-SPEC):
    - Toggle button renders `ChevronUp` icon when `sheetSnapState === 'collapsed'`.
    - Toggle button renders `ChevronDown` icon when `sheetSnapState === 'full'`.
    - Toggle aria-label is `"Open panel"` when collapsed, `"Close panel"` when full.
    - Clicking the toggle when collapsed calls `setSheetSnapState('full')`.
    - Clicking the toggle when full calls `setSheetSnapState('collapsed')`.
    - Tab strip shows `"Canvas Settings"` when `selectedNodeId` is null.
    - Tab strip shows `"Cell Settings"` when `selectedNodeId` is non-null.
    - Auto-expand: setting `selectedNodeId` to a truthy value triggers `setSheetSnapState('full')`.
    - `data-sheet-snap` attribute reflects the current store state.
  </behavior>
  <action>
    Open `src/Editor/__tests__/phase05.1-p01-foundation.test.tsx` and surgically update per PATTERNS.md § Test Impact:

    1. Delete the `touch-none` class assertion (line ~95–100).
    2. Delete the three testid-presence tests for `sheet-undo`, `sheet-redo`, `sheet-clear` (lines ~114–119).
    3. Delete both drag-gesture test blocks (lines ~121–133 and ~135–153). Also delete any helper seeding `sheetSnapState: 'half'`.
    4. Keep all tests listed as "remain valid" in PATTERNS.md § Test Impact verbatim.
    5. Add a new `describe('Phase 23 — toggle + tab strip', () => { ... })` block containing the new tests listed in § Test Impact. Use the existing test file's harness conventions (render helper, store mutations via `useEditorStore.setState` / `useGridStore.setState`, `fireEvent.click` for clicks).
    6. For toggle click tests: render the component, call `fireEvent.click(screen.getByRole('button', { name: 'Open panel' }))`, then assert `useEditorStore.getState().sheetSnapState === 'full'`. Mirror pattern for the close case (seed state to `'full'` first, then assert it becomes `'collapsed'`).
    7. For auto-expand: render with `selectedNodeId = null`, then `act(() => useEditorStore.setState({ selectedNodeId: 'some-id' }))`, then assert `useEditorStore.getState().sheetSnapState === 'full'`.
    8. For label tests: assert `screen.getByText('Canvas Settings')` or `screen.getByText('Cell Settings')` based on whether a node is selected.
    9. For icon assertion: lucide-react renders SVGs with a `lucide-chevron-up` / `lucide-chevron-down` class (or data attribute in newer versions) — alternatively assert via `container.querySelector('svg')` and checking aria-label on the parent button is sufficient if icon-class detection is brittle. Prefer aria-label assertion (stable).

    Run the tests repeatedly during edits to keep red/green tight.
  </action>
  <verify>
    <automated>cd /Users/radu/Developer/storygrid && npx vitest run src/Editor/__tests__/phase05.1-p01-foundation.test.tsx 2>&1 | tail -40</automated>
  </verify>
  <done>
    - Test file compiles and all tests in the file pass.
    - No references to `'half'`, `sheet-undo`, `sheet-redo`, `sheet-clear`, or `touch-none` remain in the file.
    - New tests cover: toggle icon swap, toggle aria-label swap, toggle click wiring (both directions), tab label (both branches), and auto-expand on cell select.
  </done>
  <acceptance_criteria>
    - [ ] `npx vitest run src/Editor/__tests__/phase05.1-p01-foundation.test.tsx` exits 0 with zero failures.
    - [ ] `grep -n "'half'\|sheet-undo\|sheet-redo\|sheet-clear\|touch-none" src/Editor/__tests__/phase05.1-p01-foundation.test.tsx` returns no matches.
    - [ ] File contains the string `'Open panel'` and `'Close panel'` (new aria-label tests).
    - [ ] File contains the string `'Canvas Settings'` and `'Cell Settings'` (new label tests).
    - [ ] File contains an assertion that `sheetSnapState` becomes `'full'` after a cell selection state mutation (auto-expand test).
    - [ ] `npx tsc --noEmit` still exits 0 with these test edits.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Mobile device visual verification</name>
  <what-built>
    Bottom sheet refactor: drag gesture removed; chevron toggle button opens/closes; auto-expand on cell select; 60px tab strip with contextual label ("Canvas Settings" / "Cell Settings"); `h-dvh` fixes iOS address-bar overflow; `motion-reduce:transition-none` honors reduced motion preference.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app on a mobile device (or Chrome DevTools device emulation at 390×844, iPhone 14).
    2. With no cell selected, confirm the sheet is collapsed: a 60px strip is visible at the bottom showing `▲ Canvas Settings`. No drag-pill bar is present.
    3. Tap the chevron. The sheet smoothly slides up to full height (animation duration ~300ms). The chevron flips to `▽`.
    4. Tap the chevron again. The sheet slides back down to the 60px strip.
    5. Select (tap) any cell in the canvas. Confirm: (a) the sheet auto-expands to full height, (b) the label changes to `Cell Settings`, (c) cell controls (SelectedCellPanel) are shown and the panel is fully scrollable to the bottom — no controls cut off.
    6. Deselect the cell (tap the canvas background). The sheet remains full (auto-expand is one-way by design). Label returns to `Canvas Settings`. Close via chevron.
    7. Attempt to drag the 60px strip with a swipe gesture — confirm nothing happens (drag is removed).
    8. On a real iOS device, confirm that scrolling the app canvas does not trigger pull-to-refresh, and the address bar showing/hiding does not cause the sheet to misalign (this validates `h-dvh`).
    9. In system settings, enable "Reduce Motion" (macOS: Accessibility > Display; iOS: Accessibility > Motion). Reload the page. Tap the toggle — the sheet should snap instantly with no transition.
    10. Header toolbar (Phase 22 work) must NOT regress — Export, Undo, Redo, Templates, Clear remain reachable in the mobile header, and none of those controls appear inside the new tab strip.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This is a pure UI refactor of an existing mobile layout component. No user-controlled input crosses a trust boundary; no network I/O; no persistence; no dynamic code evaluation. Rendered strings (`"Canvas Settings"`, `"Cell Settings"`, aria-labels) are hardcoded literals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Information Disclosure | `MobileSheet` label text | accept | Labels are static strings; no user data is interpolated. No risk. |
| T-23-02 | Tampering | `sheetSnapState` store value | accept | Client-side-only UI state with finite union type `'collapsed' \| 'full'`. TypeScript narrowing prevents invalid states. No security impact if a user mutates it via devtools — only affects their own UI. |
| T-23-03 | Denial of Service | Reduced-motion / rapid toggle taps | accept | `setSheetSnapState` is an O(1) set call; unconditional CSS transition cannot stack. No throttling required. |

**Summary:** No significant threats. UI-only refactor of an existing component, no new data flows, no auth surface, no injection vectors. Security enforcement block included per project policy.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run src/Editor/__tests__/phase05.1-p01-foundation.test.tsx` exits 0.
- `npx vitest run` (full suite) — no regressions in unrelated files.
- Manual mobile verification per Task 3 passes all 10 steps.
- Grep audit: no `'half'`, no removed-button testids, no drag handlers remain in `MobileSheet.tsx` or the updated test file.
</verification>

<success_criteria>
- SHEET-01: Sheet opens/closes via chevron toggle button; drag-pill gesture is entirely removed (no `onPointerDown`/`onPointerUp` on the strip).
- SHEET-02: Selecting a cell triggers `setSheetSnapState('full')` in the existing `useEffect`, auto-expanding the sheet.
- SHEET-03: Content area retains `overflow-y-auto` with `height: calc(100% - 60px)` — all controls reachable by scrolling in the `full` state. The `'half'` state (which could hide lower controls) is removed entirely, so "nothing cut off in any snap state" is guaranteed by type narrowing.
- SHEET-04: When collapsed, a 60px strip with a visible chevron button and context label is present — the sheet is never hidden.
- All retained tests continue to pass; new tests cover toggle, auto-expand, and labels.
- Mobile device verification confirms smooth animation, reduced-motion compliance, and no iOS address-bar overflow (`h-dvh`).
</success_criteria>

<output>
After completion, create `.planning/phases/23-bottom-sheet-redesign/23-01-SUMMARY.md`.
</output>
